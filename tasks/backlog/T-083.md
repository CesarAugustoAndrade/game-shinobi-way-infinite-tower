# T-083 · R0 turnIndex avanza + resetCombatFrontier en createCombatState

- **Type:** fix
- **Layer(s) touched:** domain
- **Ring:** R0 (`TurnClockSystem.ts`, `CombatWorkflowSystem.ts`)
- **Lado / Side:** `back`
- **Priority:** P0
- **Agent estimate:** S
- **Evaluation route:** `autonomous`

## Context
`runTurnStartClock` echoea `turnIndex`. Nadie hace `+ 1`. `readyOnTurn` no puede desbloquear. `createCombatState` no llama `resetCombatFrontier`.

## Spec
Al cerrar o al entrar al siguiente start-of-turn, `turnIndex` incrementa exactamente 1. `createCombatState` resetea skills vía `resetCombatFrontier` (readyOnTurn + CD). Dos llamadas consecutivas a `runTurnStartClock` no dejan T igual.

## Verification
- [ ] RING-GUARD
- [ ] `tsc --noEmit`
- [ ] AC1: `runTurnStartClock` devuelve `turnIndex === input.turnIndex + 1` → `npm test -- src/game/systems/__tests__/t083TurnIndex.test.ts`
- [ ] AC2: `createCombatState` deja skills ready (readyOnTurn undefined o ≤ 1) → mismo test

## Out of scope
No matar `currentCooldown` aún (dual-write OK). No tocar `useCombat` (T-082).
