# T-084 · R0 Wire ON_MOVE reactions + Fear/Smoke/ReadWindow en daño enemigo live

- **Type:** fix
- **Layer(s) touched:** domain
- **Ring:** R0 (`MarkSystem.ts`, `EnemyTurnSystem.ts`)
- **Lado / Side:** `back`
- **Priority:** P0
- **Agent estimate:** M
- **Evaluation route:** `autonomous`

## Context
`void collectRangeReactions(...)` se llama sin sources. `fireTripwireOnEnemyMove` no entra al move live. Fear/Smoke/ReadWindow solo existen en probes.

## Spec
Move voluntario enemigo dispara marks con trigger ON_MOVE (no solo `id === tripwire`) y aplica daño/stun del payload. El daño enemigo live pasa por `applyFearOutgoing` / `applySmokeOutgoing` / `applyReadWindowOutgoing` (o un helper genérico que los unifique).

## Verification
- [ ] RING-GUARD
- [ ] `tsc --noEmit`
- [ ] AC1: move enemigo con mark tripwire produce daño → `npm test -- src/game/systems/__tests__/t084LiveReactions.test.ts`
- [ ] AC2: Fear activo reduce el outgoing enemigo una vez → mismo test

## Out of scope
No unificar stun-mark vs stun-buff (documentar residual). No tocar `resolveSkill` hit math (T-088).
