# T-085 · R0 CombatState persiste pendingSupportWeights + pendingDiscover; draw live consume

- **Type:** fix
- **Layer(s) touched:** domain
- **Ring:** R0 (`combat-types.ts`, `PlayerTurnSystem.ts` upkeep/draw)
- **Lado / Side:** `back`
- **Priority:** P1
- **Agent estimate:** M
- **Evaluation route:** `autonomous`

## Context
`resolveSkill` encola `pendingSupportWeights` / Discover. `CombatState` no tiene esos campos. `buildUpkeepWeightContext` solo pasa `modeBonuses`.

## Spec
`CombatState` incluye `pendingSupportWeights` y `pendingDiscover`. `processUpkeep` / `buildUpkeepWeightContext` hace `consume` una vez y los mete en `WeightContext`. Draw de turno siguiente refleja el bag T-016.

## Verification
- [ ] RING-GUARD
- [ ] `tsc --noEmit`
- [ ] AC1: un pending support weight cambia `effectiveWeight` del draw de upkeep → `npm test -- src/game/systems/__tests__/t085LiveWeights.test.ts`

## Out of scope
No UI de Discover (T-087 puede mostrar si el campo existe; no es AC). No cutover `useSkill` (T-082).
