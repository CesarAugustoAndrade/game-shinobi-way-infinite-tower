# T-089 · R0 Dragon Flame: ATTACK 17 M/L + Burn 5×3; Sharingan 2T +50% + 1 charge

- **Type:** expand
- **Layer(s) touched:** domain (`dragon_flame` authoring + pure `resolveSkill` Mode enhance reuse)
- **Ring:** R0 (`src/game/constants/skills.ts` `DRAGON_FLAME`; reuse Mode enhance path in `ResolveSkillSystem.ts` — no new Mode machine). No React.
- **Lado / Side:** `back`
- **Bounded context:** combat-tactical-setup · **Subdomain:** core
- **Priority:** P1
- **Agent estimate:** S
- **Evaluation route:** `autonomous`
- **Depends on:** T-005 Mode board / `trySpendCharges`; T-007 `resolveSkill`; T-032 Fireball (`modeInteraction` +50% / 1 charge on `sharingan_2`). Complements Uchiha tunnel after Fireball/Phoenix. **Not blocked** by T-068–T-088.

## Génesis

**Why this gap:** SOUL §3.8 content honesty + §11 Uchiha elemental payoff. Canon catalog [`#catalogo`](../../docs/combat-tactical-setup-spec.html#catalogo) **dragon_flame**: ATTACK · **3 AP · 6 CP · CD 3** · MEDIUM · LONG · “**17 dmg + Burn 5×3**. **Sharingan 2T: +50%, consume 1 carga**.” Runtime row is still legacy ACTIVE-only packaging with wrong AP, mute % Burn, and **no** `cardRole` / `modeInteraction` / ranges — flavor promises dragon fire, not tactical Setup.

**Code evidence:**
- `DRAGON_FLAME` (`skills.ts` ~1861–1887): AP **2** (catalog **3**); base **17** ok; Burn **10×3 @70%** (catalog **Burn 5×3** certain on hit); **no** `cardRole`, **no** `modeInteraction`, **no** `allowedRanges`.
- Fireball (T-032) already proves pure resolve auto-enhance: `modeId: 'sharingan_2'`, `damageMultBonus: 0.5`, `consumeCharges: 1` at attempt (`skills.ts` FIREBALL; `ResolveSkillSystem` Mode path).
- Grep `tasks/**` for `dragon_flame` / Dragon Flame: **empty** — not in backlog/active/done.

**Already covered (do not duplicate):**
- **T-032** — Fireball 17 + Burn 5×2 + same 2T enhance (sibling, not this skill).
- **T-033** — Phoenix Flower multi-hit + Sharingan 2T +40%.
- **T-025** — Chidori Sharingan **3** route.
- **T-011** — Sharingan 2 Mode packaging.

**Discarded alternatives:**
- kawarimi Substitution first-hit ignore + band — M reaction mark; deferred again (needs ON_HIT reaction motor beyond tripwire ON_MOVE).
- suijinheki / water_clone / sand-adjacent shields — defense SIDE/SUPPORT; lower Uchiha tunnel leverage than Mode-enhanced ATTACK.
- basic_atk / heavy_kick floor ATTACK cardRole-only — low leverage vs Mode route reuse.
- Full remaining catalog lote (lightning_ball + adamantine + …) — L; SOUL prefers S/M slices.

**SOUL pillars served:** §3.1 ATTACK cobra; §3.4 Modes; §3.7 Uchiha tunnel; §3.8 honesty; §8 charges at attempt; catalog `#catalogo` dragon_flame; anti-visión no secret % without declared Mode route.

## Context

Sharingan 2-Tomoe Mode and Fireball’s Mode enhance path exist. Dragon Flame Bomb is the longer-burn Uchiha nuke sibling that should share the same 2T +50% / 1-charge contract while differing only in Burn duration (5×3 vs Fireball 5×2).

## Spec

### Must exist when done

1. **Authoring (`dragon_flame`)**
   - `cardRole: CardRole.ATTACK`.
   - Costs: **AP 3**, **CP 6**, **CD 3**, HP 0 (catalog).
   - `baseDamage: 17`; `hitCount` default 1.
   - `allowedRanges: [CombatRange.MEDIUM, CombatRange.LONG]`.
   - `modeInteraction` (mirror T-032 Fireball):
     - `modeId: 'sharingan_2'`
     - `family: MODE_FAMILY.SHARINGAN` (or equivalent)
     - `consumeCharges: 1`
     - `damageMultBonus: 0.5`
     - **No** `requireOn` (base legal when Mode OFF).
   - Burn honesty: **Burn 5 for 3** opportunities on hit (chance **1.0**); drop legacy 10×3 @70% as primary identity.
   - Tags: UCHIHA / NINJUTSU / FIRE as available enums.
   - Honest description: base 17 + Burn 5×3; with Sharingan 2-Tomoe ON +50% and 1 charge at attempt.

2. **`resolveSkill` behavior** (reuse existing Mode enhance; no skill-id special-case if possible)
   - Mode OFF: baseline damage under deterministic hit port; **no** charge spend; board unchanged.
   - Sharingan 2 ON with ≥1 charge: damage uses **+50%** (`floor(baseline * 1.5)` T-018/T-032 style); spend **1** charge at attempt; Mode stays ON if charges remain.
   - Sharingan **3** alone does **not** apply this row’s enhance unless authoring lists it — **v1 AC only requires 2T**.
   - On hit: apply Burn 5×3 (mark or effect path consistent with Fireball DoT).
   - Invalid AP/range: reject, consume nothing.

3. **Compose**
   - Prefer zero new `ModeInteraction` fields; mirror Fireball.
   - Do not retune Fireball/Phoenix/Chidori packages.

### Architectural constraints
- [x] R0 pure; immutability
- [x] Numbers only catalog/SOUL: 17 dmg, Burn 5×3, +50%, 1 charge, 2T, AP3/CD3/CP6, M/L
- [x] Base viable without Mode
- [x] No React / no live `useSkill` AC
- [x] No kawarimi / lightning_ball / other catalog rows in this task’s AC

## Verification

- **Evaluation route:** `autonomous`

### Objective AC (blocking)

- [ ] **RING-GUARD:** R0 only; no React/components/scenes/hooks/contexts imports in touched files.
- [ ] **`npm run typecheck`** passes.
- [ ] **AC1 — Authoring:** `dragon_flame` has `cardRole: ATTACK`, `apCost: 3`, `chakraCost: 6`, `cooldown: 3`, `baseDamage: 17`, `allowedRanges` includes MEDIUM and LONG, `modeInteraction` with `modeId: 'sharingan_2'`, `consumeCharges: 1`, `damageMultBonus: 0.5`, `requireOn` absent/false, Burn **5×3** contract → gate: `npm test -- src/game/systems/__tests__/t089DragonFlame` (`T-089 authoring`).
- [ ] **AC2 — Base without Mode:** empty Mode board; resolve Dragon Flame with deterministic hit → `ok: true`, `damageDealt` equals baseline (17 under test hit port), modes still empty → gate: same file `T-089 base`.
- [ ] **AC3 — 2T enhance + charge:** Sharingan 2 ON with **3** charges; resolve hit → damage = `floor(baseline * 1.5)`; Sharingan charges **2**; Mode still ON → gate: same file `T-089 sharingan enhance`.

### Subjective criteria
None.

## Out of scope

- Kawarimi Substitution; lightning_ball 3T; adamantine_chains; water_clone; suijinheki; basic_atk/heavy_kick floor; live UI; dual-compat toggle purge; editing SOUL.

## Notes for implementer

- Canon: HTML `#catalogo` `dragon_flame`; SOUL §8 charges at attempt; copy T-032 Fireball authoring/tests and only change Burn duration to **3** and skill id/name.
- Prefer same Burn application path Fireball uses so AC3 does not invent a second DoT pipeline.
