# T-091 · R0 Bug Swarm: SIDE 8 M/L + drain 6 CP + Poison 3×3 on impact

- **Type:** expand
- **Layer(s) touched:** domain (`bug_swarm` authoring + pure SIDE impact drain + Poison plant)
- **Ring:** R0 (`src/game/constants/skills.ts` `BUG_SWARM`; `ResolveSkillSystem.ts` impact chakra drain + DoT mark). No React.
- **Lado / Side:** `back`
- **Bounded context:** combat-tactical-setup · **Subdomain:** core
- **Priority:** P1
- **Agent estimate:** S
- **Evaluation route:** `autonomous`
- **Depends on:** T-007 resolve; T-004 multi-hit/impact consume rules (once per card on ≥1 hit); T-070/T-061 poison mark plant; T-045 flat enemy chakra drain on impact pattern. Complements Aburame/utility SIDE after Senbon Rain. **Not blocked** by T-089–T-090.

## Génesis

**Why this gap:** SOUL §3.1 SIDE arma + §9 DoT Poison family + once-per-card on impact + §3.8 honesty. Canon catalog [`#catalogo`](../../docs/combat-tactical-setup-spec.html#catalogo) **bug_swarm**: SIDE · **2 AP · 6 CP · CD 4** · MEDIUM · LONG · “**8 dmg**; **drena 6 CP** y aplica **Poison 3×3** al impactar.” Runtime is legacy ACTIVE: CHAKRA_DRAIN **25**, POISON **8×3**, **no** `cardRole` / ranges / honest packages — pure resolve does not guarantee catalog drain 6 + poison 3×3 on hit.

**Code evidence:**
- `BUG_SWARM` (`skills.ts` ~2150–2176): AP2/CP6/CD4; base **8**; effects CHAKRA_DRAIN **25** + POISON **8**×3; **no** `cardRole`, **no** `allowedRanges`, **no** markEffects.
- Poison plant on ≥1 hit exists (T-070 senbon_rain / T-061 patterns via marks `id: 'poison'`).
- Flat enemy chakra drain on impact exists for Hyuga CP consume (T-045 `impactMarkConsume.drainChakraPerStack`); **not** a fixed “drain 6 on hit” authoring for this skill.
- Grep tasks titles: **no** T-XXX owns `bug_swarm` / Parasitic Insects (only absent from backlog/done titles).

**Already covered (do not duplicate):**
- **T-070** — Senbon Rain multi-hit + Poison 3×2 (no chakra drain).
- **T-061** — Poison Coat delayed poison arm (SUPPORT).
- **T-045** — Gentle Fist CP-mark drain (not insects SIDE).
- **T-058** — Senbon Silence SIDE (no poison/drain package).

**Discarded alternatives:**
- fang_over_fang 2×7 + CLOSE-entry Setup — needs “entered CLOSE this turn” flag (M).
- kawarimi Substitution ON_HIT — M reaction motor.
- adamantine_chains PULL+Stun ATTACK — Uzumaki lote; larger control+movement slice.
- expansion +2 STR self-buff ATTACK — different clan beat.
- shadow_possession Bound SUPPORT — control mark without damage motor this fire.

**SOUL pillars served:** §3.1 SIDE; §3.5 Marks/DoT; §9 impact consume once-per-card; §3.8 honesty; catalog `#catalogo` bug_swarm.

## Context

Senbon Rain plants Poison without drain. Bug Swarm should be the Aburame SIDE that deals **8** at M/L and, on **impact (≥1 hit)**, both **drains 6 enemy CP** and plants **Poison 3 for 3** opportunities — currently wrong drain/poison numbers and no role/ranges.

## Spec

### Must exist when done

1. **Authoring (`bug_swarm`)**
   - `cardRole: CardRole.SIDE_ATTACK`.
   - Costs: **AP 2**, **CP 6**, **CD 4**, HP 0.
   - `baseDamage: 8`; `hitCount` default 1.
   - `allowedRanges: [CombatRange.MEDIUM, CombatRange.LONG]`.
   - On impact (≥1 hit): **drain 6** enemy chakra (floor 0) **and** plant enemy **Poison** mark: id `poison`, stacks **3**, duration **3**, family DOT (Poison 3×3).
   - Prefer explicit fields e.g. `impactChakraDrain: 6` + `markEffects` / impact plant (document choice in tests). Drop primary identity of CHAKRA_DRAIN 25 + POISON 8×3 effects-only path for pure resolve.
   - Tags: NINJUTSU / PHYSICAL / POISON as available.
   - Honest description: 8 dmg M/L; on hit drain 6 CP + Poison 3×3.

2. **`resolveSkill` SIDE**
   - Pay AP2/CP6/CD; on miss (0 hits): **no** drain, **no** poison plant; costs still committed if attempt was legal (SOUL attempt rules for own marks; enemy impact package only if hitsLanded ≥ 1).
   - On ≥1 hit: `enemyChakra` decreases by **6** (clamp ≥0); exactly one poison package as above (once per card, not per hit).
   - No Mode interaction; no Terrain/Heat; does **not** grant Shadow Clone charges.

3. **Compose**
   - Reuse poison mark id `poison` consistent with T-070/T-061.
   - Prefer extending existing impact hooks over skill-id hardcoding when practical.

### Architectural constraints
- [x] R0 pure; immutability
- [x] Catalog numbers only: 8 dmg, drain 6 CP, Poison 3×3, AP2/CP6/CD4, M/L
- [x] Impact package once per card if ≥1 hit
- [x] No React; no live UI AC
- [x] No fang_over_fang / kawarimi / adamantine in AC

## Verification

- **Evaluation route:** `autonomous`

### Objective AC (blocking)

- [ ] **RING-GUARD:** R0 only; no React/components/scenes/hooks/contexts imports in touched files.
- [ ] **`npm run typecheck`** passes.
- [ ] **AC1 — Authoring:** `bug_swarm` has `cardRole: SIDE_ATTACK`, AP2/CP6/CD4, `baseDamage: 8`, M/L ranges, impact drain **6** + Poison **3×3** contract → gate: `npm test -- src/game/systems/__tests__/t091BugSwarm` (`T-091 authoring`).
- [ ] **AC2 — Hit: drain + poison:** enemy chakra 20; resolve hit → `enemyChakra === 14`, enemy `poison` mark stacks 3 duration 3 present, `damageDealt` baseline 8 under hit port → gate: same file `T-091 impact`.
- [ ] **AC3 — Miss: no package:** resolve 0 hits → enemy chakra unchanged, no new poison mark from this skill → gate: same file `T-091 miss`.

### Subjective criteria
None.

## Out of scope

- fang_over_fang; kawarimi; adamantine_chains; expansion; shadow_possession; live UI; editing SOUL.

## Notes for implementer

- Canon: HTML `#catalogo` `bug_swarm`; SOUL §9 DoT + multi-hit once-per-card.
- Prefer `enemyChakra` on resolve state (already used by T-045). Poison mark id `poison` for cleanse/medical consistency (T-068/T-070).
