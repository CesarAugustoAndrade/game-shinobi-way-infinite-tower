# T-082 · R0–1 Live cutover: useSkill / useCombat delegan en resolveSkill

- **Type:** fix
- **Layer(s) touched:** domain · adapters-in · composition-root
- **Ring:** R0–1 (`PlayerTurnSystem.ts`, `useCombat.ts`)
- **Lado / Side:** `back`
- **Priority:** P0
- **Agent estimate:** L
- **Evaluation route:** `autonomous`
- **Depends on:** T-007, T-088 (hit math). Complements T-083/T-085.

## Context
T-007 dejó `resolveSkill` como SoT SOUL. Live sigue en `useSkill` (`useCombat.ts` ~337). El catálogo T-008–T-060 no llega al jugador.

## Spec
`PlayerTurnSystem.useSkill` es un adaptador: mapea pools/board → `resolveSkill` y proyecta el resultado a `CombatResult` / `CombatState`. Toggles MODE en `useCombat` no inventan buffs; llaman el mismo commit. Roles `cardRole` se respetan en vivo.

## Verification
- [ ] RING-GUARD: R0 no importa React/UI
- [ ] `tsc --noEmit` sin errores
- [ ] AC1: `useSkill` llama `resolveSkill` (no reimplementa hit) → `npm test -- src/game/systems/__tests__/t082LiveCutover.test.ts`
- [ ] AC2: una carta con `bandMove` muta rango en el path live → mismo test

## Out of scope
T-067–T-081 content. No rediseñar Mode/Mark. No montar UI (T-087). No unificar sim/auto en este slice si el adaptador live ya existe (anotar residual).
