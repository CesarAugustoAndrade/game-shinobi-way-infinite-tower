# T-088 · R0 resolveSkill ATTACK/SIDE usa calculateDamage + resolveSuccessfulHit

- **Type:** fix
- **Layer(s) touched:** domain
- **Ring:** R0 (`ResolveSkillSystem.ts` hit branch)
- **Lado / Side:** `back`
- **Priority:** P0
- **Agent estimate:** M
- **Evaluation route:** `autonomous`

## Context
`resolveSkill` tira `rng < 0.85` + `baseDamage`. Live usa `StatSystem.calculateDamage` → `resolveSuccessfulHit`. Dos físicas.

## Spec
Tras un hit conectado, el branch ATTACK/SIDE de `resolveSkill` llama el hit core Sprint B (`calculateDamage` + `resolveSuccessfulHit` o el mismo wrapper). Marks/mode mults entran como pre-mitigation multipliers, no como un segundo daño. Miss/evade sigue siendo el del core, no un 0.85 paralelo.

## Verification
- [ ] RING-GUARD
- [ ] `tsc --noEmit`
- [ ] AC1: un ATTACK con stats conocidos produce el mismo entero que `calculateDamage`+`resolveSuccessfulHit` en fixture → `npm test -- src/game/systems/__tests__/t088HitCore.test.ts`
- [ ] Tests T-018–T-060 existentes siguen verdes o se actualizan solo si el número cambia por unificar física (documentar Δ)

## Out of scope
No cutover live (T-082). No reescribir mitigation. No tocar UI preview (después podrá reusar el mismo core).
