# T-087 · R2 Montar paneles T-010 + ribbon Main + canPlaySkill con contexto T-006

- **Type:** fix
- **Layer(s) touched:** adapters-in
- **Ring:** R2 (`Combat.tsx`, `Hand.tsx`)
- **Lado / Side:** `presentación`
- **Priority:** P1
- **Agent estimate:** S
- **Evaluation route:** `autonomous`

## Context
`CombatModesPanel`, `TacticalSetupPanel`, `SkillHonestyPreview` existen pero no se montan. `Hand` no recibe `mainAttackId`. Toggles re-chequean range a mano.

## Spec
Combat monta Modes + Setup panels (datos ya en state). Hand recibe `mainAttackId` y pinta ribbon. Playability de toggles usa `canPlaySkill` / `buildSkillPlayContext` con `modeAlreadyOn` + `grantedRanges`. Cero fórmulas de daño nuevas en R2.

## Verification
- [ ] RING-GUARD: R2 no importa systems de fórmula salvo view-model / playability ya usados
- [ ] `tsc --noEmit`
- [ ] AC1: `Combat.tsx` importa y renderiza `CombatModesPanel` y `TacticalSetupPanel` → grep + `npm run build`

## Out of scope
No rediseñar Pattern A. No inventar Discover chrome completo si no hay `pendingDiscover` en state (T-085).
