# T-040 · R0 Gate Prep: SUPPORT reauthor + arm T-016 weight + T-017 HP discount

- **Type:** expand
- **Layer(s) touched:** domain (`gate_prep` authoring + pure SUPPORT resolve wiring of existing weight/discount motors)
- **Ring:** R0 (`src/game/constants/skills.ts` `GATE_PREP`; `ResolveSkillSystem.ts` SUPPORT; reuse `SupportWeightSystem` / `GatePrepDiscountSystem`). No React.
- **Lado / Side:** `back`
- **Bounded context:** combat-tactical-setup · **Subdomain:** core
- **Priority:** P1
- **Agent estimate:** S
- **Evaluation route:** `autonomous`
- **Depends on:** T-016 next-draw Gate weight +3 bag; T-017 Gate HP activation −50% discount; T-007 SUPPORT resolve. Complements Lee tunnel (Gate of Life / Limit / Lotus). **Not blocked** by T-031–T-039.

## Génesis

**Why this gap:** SOUL §11 Lee T1 **Gate Prep**: “−50% próximo coste activación HP; Gate weight **+3** próximo draw.” Canon catalog [`#catalogo`](../../docs/combat-tactical-setup-spec.html#catalogo) **gate_prep**: SUPPORT · 1 AP · **15 HP** · CD 5 · “**Gate Opening 2**: próximo coste de activación HP de GATES **−50%** y **GATES MODE weight +3** en el siguiente draw; consume al activar.” Motors for both halves already ship (T-016 skillId bag, T-017 `armGatePrepDiscount` / `activateMode` opts), but the skill row is still a Willpower **% buff** with **no** `cardRole`, so `resolveSkill` rejects `incomplete-authoring` and never arms either motor from a legal play.

**Code evidence:**
- `GATE_PREP` (`skills.ts` ~2699–2724): ACTIVE only; `hpCost: 15` ok; `effects` WIL +0.5×2 — **no** `cardRole`; anti-visión % packaging.
- `applySupportWeightOnPlay({ id: 'gate_prep' })` already returns Gate Mode skillId **+3** entries (`SupportWeightSystem.ts` ~49–51); only runs after a successful resolve that passes cardRole validation.
- `onGatePrepPlayed` / `armGatePrepDiscount` exist (`GatePrepDiscountSystem.ts`) and `activateMode` honors `opts.gateHpDiscount` (`CombatModeSystem.ts` ~146+); **`ResolveSkillState` has no pending discount field** and SUPPORT branch never calls arm.
- T-016/T-017 reviews explicitly noted: full `gate_prep` reauthor OOS; live play still cannot arm.

**Already covered (do not duplicate):**
- **T-016** — pure bag + Gate Prep **weight +3** by skill id (Drill Main +2 sibling).
- **T-017** — pure one-shot Gate HP activation **floor half**; fail preserves; non-Gate does not consume.
- **T-005 / T-011** — Gate Modes packaging / activate costs.
- **T-030 / T-031** — Lotus finishers (consume Gate charges).

**Discarded alternatives:**
- False Surroundings / Shunshin / Gentle Fist reauthors — real SOUL content gaps, but do not unlock two already-shipped motors the way Gate Prep packaging does.
- Re-implementing weight/discount math — forbidden; reuse T-016/T-017 APIs only.
- Halving Gate **upkeep** every turn — SOUL is **next activation** only (T-017 contract).
- Inventing Discover on Gate Prep — catalog tags DISCOVER but functional text is weight + HP discount only; do not invent Discover 3 here.

**SOUL pillars served:** §3.1 SUPPORT prepara; §3.2 transparent next-draw weight; §3.4 Mode peaje honesty; §3.7 Lee tunnel; §8 activation costs; catalog `#catalogo` gate_prep; SOUL §11 Gate Prep.

## Context

Lee Mode activation and next-draw support weights exist. Gate Prep is the SUPPORT that should arm both before Gate of Life / Limit — still blocked at authoring/resolve because the skill never became a legal hand SUPPORT.

## Spec

### Must exist when done

1. **Authoring (`gate_prep`)**
   - `cardRole: CardRole.SUPPORT`.
   - Costs: **AP 1**, **CP 0**, **HP 15**, **CD 5** (catalog).
   - `baseDamage: 0`.
   - `actionType: ACTIVE` (SUPPORT ↔ ACTIVE).
   - Drop primary Willpower **% buff** identity as the functional contract (anti-visión). Optional non-normative `effects[]` may remain only if they do not drive SUPPORT resolve (prefer delete).
   - Honest description: next GATES Mode **HP activation −50%** (one-shot); **Gate Mode skill weight +3** next draw; pays 15 HP on play.
   - Tags optional: LEE / GATES if enums exist; do not invent Discover filter unless already used elsewhere for this id.

2. **`resolveSkill` SUPPORT wiring**
   - On successful `gate_prep` play (after AP/HP/CD commit):
     - Enqueue T-016 weight entries via existing `applySupportWeightOnPlay` (Gate of Life + Gate of Limit **+3** each) into `pendingSupportWeights`.
     - Arm T-017 pending Gate HP discount on resolve state (e.g. `pendingGateHpDiscount: true` or embed `GatePrepDiscountState` on `ResolveSkillState`). Prefer calling `onGatePrepPlayed()` / `armGatePrepDiscount()` — no new discount math.
   - `damageDealt === 0`; no Mode activate; no charge spend.
   - If skill lacks afford HP 15 → reject as existing cost rules; do not arm bag/discount.

3. **Parity check (no new balance tables)**
   - Weight materialization still uses existing consume-at-draw path (T-016); this task does not change `effectiveWeight` formula.
   - Discount still applied only inside `activateMode` with pending flag (T-017); resolve only **arms**.

### Architectural constraints
- [x] R0 pure; immutability
- [x] Reuse T-016 / T-017 APIs; no duplicate half-HP or +3 constants outside those systems
- [x] Numbers only catalog: AP1 · 15 HP · CD5 · −50% next Gate HP activate · +3 Gate Mode weight next draw
- [x] No Gate upkeep change; no Lotus/Mode reauthor
- [x] No React / no live `useSkill` cutover AC
- [x] No False Surroundings / Smoke / Predict work

## Verification

- **Evaluation route:** `autonomous`

### Objective AC (blocking)

- [x] **RING-GUARD:** R0 only; no React/components/scenes/hooks/contexts imports in touched files.
- [x] **`npm run typecheck`** passes.
- [x] **AC1 — Authoring:** `gate_prep` has `cardRole: SUPPORT`, `apCost: 1`, `hpCost: 15`, `chakraCost: 0`, `cooldown: 5`, `baseDamage: 0`; no primary Willpower % buff identity → gate: `npm test -- src/game/systems/__tests__/t040GatePrepSupport` (`T-040 authoring`).
- [x] **AC2 — Resolve arms both motors:** resolve `gate_prep` with enough HP → `ok: true`, HP −15, `pendingSupportWeights` includes `gate_of_life` and `gate_of_limit` with **delta 3**, pending Gate HP discount **armed** → gate: same file `T-040 arm`.
- [x] **AC3 — Incomplete path gone + afford gate:** (a) resolve no longer returns `incomplete-authoring` solely for missing role. (b) HP below 15 → reject, bag empty, discount not armed → gate: same file `T-040 reject`.

### Subjective criteria
None.

## Out of scope

- Changing T-016 weight amounts or T-017 floor-half formula.
- Ascend/lateral Gate paths consuming the discount (document if already ignored).
- Primary/Hidden Lotus, Dynamic Entry, Morning Peacock.
- False Surroundings, Shunshin, Gentle Fist, Smoke, Predict.
- Live UI panels; editing SOUL.

## Notes for implementer

- Canon: SOUL §11 Lee Gate Prep; HTML `#catalogo` gate_prep; `#modes` HP activation; T-016/T-017 done specs.
- Minimal surface: authoring + `ResolveSkillState` pending discount field + SUPPORT post-commit arm; weight enqueue already generic on successful play.
- Prefer skill-id branch only for arming discount (`GATE_PREP_ID`) consistent with `SupportWeightSystem`.
- Encounter reset of pending discount already T-002/T-017 concern — if resolve state is ephemeral, arm echo on result is enough for pure AC.

## Close summary

- **Status:** done (`28f5a64` merged ff into `wt/jp-08121518`)
- **Shipped:** Gate Prep SUPPORT pays 15 HP. Arms T-016 Gate +3 next draw and T-017 pending HP discount. WIL % buff removed. Low HP rejects without arming.
- **Cycles:** 1. No redesign. No review/verify FIX.
- **Sim:** T-040 probe all true. T-016/T-017 still true. HP paid 15; weight +3; discount armed. Live WR unchanged. **Catalog-faithful. Motors now reachable from a legal play.**
- **Harvest:** no skill deltas.
- **Noted:** live `useSkill` still does not consume `pendingGateHpDiscount`.

