# T-047 research

In-tree T-014/T-003/T-013.

Extend `DiscoverSpec.matchMainAttackTags`. Build predicate at resolve from `weightContext.mainAttackId`. Plant `studied` on analyze commit only. Pen 0.2 while mark present.
