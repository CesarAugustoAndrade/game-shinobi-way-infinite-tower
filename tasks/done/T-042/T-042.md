# T-042 · R0 Mind Destruction: ATTACK + Read Mind Setup +50% (no consume)

- **Type:** expand
- **Layer(s) touched:** domain (`mind_destruction` authoring + pure ATTACK resolve Setup-bucket read of `read_mind`)
- **Ring:** R0 (`src/game/constants/skills.ts` `MIND_DESTRUCTION`; `ResolveSkillSystem.ts` ATTACK; optional thin mark-read helper). No React.
- **Lado / Side:** `back`
- **Bounded context:** combat-tactical-setup · **Subdomain:** core
- **Priority:** P1
- **Agent estimate:** S–M
- **Evaluation route:** `autonomous`
- **Depends on:** T-007 ATTACK resolve; T-004 Marks; T-041 False Surroundings plants non-consuming `read_mind`. Complements Yamanaka tunnel (T-036/T-037/T-041). **Not blocked** by Mode motors (this payoff is mark-only).

## Génesis

**Why this gap:** SOUL §11 Yamanaka T2–4 payoff surface and §3.5 **Marks legibles** (lectura sin consumir salvo declaración). Canon catalog [`#catalogo`](../../docs/combat-tactical-setup-spec.html#catalogo) **mind_destruction**: “**15 mental + Confusion 2 al 65%**; si **Read Mind** está activo, **+50% Setup dmg sin consumir la lectura**.” T-041 closed the setup plant (`read_mind` duration 2) and **explicitly OOS** this payoff. Legacy card is still ACTIVE-only mental 15 with Confusion **3 @100%**, **AP 2** (catalog **3**), **no** `cardRole`, and **no** Read Mind Setup mult.

**Code evidence:**
- `MIND_DESTRUCTION` (`skills.ts` ~1607–1634): `apCost: 2`, `baseDamage: 15`, `effects` CONFUSION duration **3** chance **1.0**; **no** `cardRole`; **no** mark-read contract; no Setup bucket mult.
- ATTACK `modeInteraction.damageMultBonus` path in `ResolveSkillSystem.ts` (~624–652) requires **Mode ON** (`autoModeOn && boundModeId`) before `requireMarkId` / `damageMultBonus` apply — Twin Lion/64 Palms pattern. Mark-only lectura (no Mode) is **not** wired for Yamanaka.
- T-041 OOS: “Mind Destruction ‘if Read Mind active +50% Setup dmg’ payoff (separate content task).” `read_mind` id stabilized for this task.

**Already covered (do not duplicate):**
- **T-041** — False Surroundings plants `read_mind` 2 + Confusion chance (setup only).
- **T-036** — Mind Transfer 70% Stun 2 / fail self-Stun.
- **T-037** — Hell Viewing 18 + Fear 1.
- **T-008 / T-014** — Mind Reading Discover 3 MENTAL.
- **T-022 / T-035** — Mode+mark payoffs (Byakugan/CP), different tunnel.

**Discarded alternatives:**
- Shunshin band move + next Offensive +1 DEX — real Uzumaki gap; lower priority than closing the handoff T-041 already named.
- Gentle Fist Byakugan + CP consume — Hyuga Main; multiplies Mode path already proven; does not unlock T-041’s mark.
- Treating Read Mind +50% as a secret STR/CALM debuff — anti-visión §4.
- Consuming Read Mind on payoff — catalog **sin consumir**.

**SOUL pillars served:** §3.1 ATTACK cobra; §3.5 Marks (lectura no consumible); §3.7 Yamanaka sin Mode; §9 Setup bucket vs Mode layers; catalog `#catalogo` mind_destruction; anti-visión no % secreto.

## Context

Yamanaka Discover, Transfer, Hell Viewing, and False Surroundings setup are specified/shipped. Mind Destruction is the mental ATTACK that should **read** the table: base 15 + 65% Confusion 2, and when `read_mind` is on the enemy, apply **+50% in the Tactical Setup damage bucket** without removing the mark. Runtime still ships a legacy Confusion 3 always-on card with wrong AP and no Setup read.

## Spec

### Must exist when done

1. **Authoring (`mind_destruction`)**
   - `cardRole: CardRole.ATTACK`.
   - Costs: **AP 3**, **CP 6**, **CD 4**, HP 0 (catalog).
   - `baseDamage: 15`; single hit; mental damage type may remain; AUTO / ANY ranges per catalog “1 AUTO hit”.
   - Confusion contract: **65%** chance, duration **2** (not legacy 100%/3 as primary identity). Prefer enemy `EffectType.CONFUSION` consistent with T-041 / EnemyTurn, or mark — **lock one** in tests.
   - Setup read contract (authoring and/or thin resolve field): when enemy has mark id **`read_mind`** (any stacks ≥1), apply **+50%** Setup-bucket damage mult; **do not** consume/remove `read_mind`.
   - Honest description: 15 mental; 65% Confusion 2; if Read Mind active +50% Setup (no consume).
   - Clan/stats requirements may remain. Do **not** auto-assign `mainAttackId` here.

2. **`resolveSkill` ATTACK**
   - After validate+commit: resolve hits under deterministic ports.
   - **Baseline (no Read Mind):** under test hit port, `damageDealt === 15` (or documented AUTO always-hit); roll Confusion @0.65 for duration 2.
   - **With enemy `read_mind` present:** damage uses **Setup bucket +50%** → expected **22** if pure base 15 and no other layers (`floor(15 * 1.5)` or project rounding policy locked in test). Prefer SOUL §8 separation: Setup % in its own bucket vs posture/crit/Mode (if other layers zero in fixture, 22 is enough).
   - After enhanced resolve: **`read_mind` still present** on enemy (duration/stacks unchanged by this skill). No Mode activation, no Mode charge spend, no Terrain.
   - Full miss (if miss possible): no damage, no Confusion plant; Read Mind untouched. Prefer AUTO always-hit for AC stability if that is project genjutsu policy.

3. **Mark-only Setup read (motor thinness)**
   - Must work **without** any Mode ON (Yamanaka sin Mode).
   - Do **not** overload Twin Lion’s `modeInteraction` path in a way that requires `modeId`/charges for this skill unless a new flag explicitly means “mark-only lectura.” Prefer: (a) dedicated field e.g. `setupRead?: { markId: 'read_mind'; damageMultBonus: 0.5; consume?: false }` **or** (b) extend resolve so `requireMarkId` + `damageMultBonus` apply when **no** modeId/family bind is authored. Document choice in test name.
   - Immutability: return new state; never mutate mark arrays in place.

### Architectural constraints
- [x] R0 pure; immutability
- [x] Numbers only catalog: AP3 · CP6 · CD4 · 15 dmg · Confusion 2 @65% · Read Mind +50% Setup no consume
- [x] No Mode requirement; no Terrain/Heat
- [x] No React / no live `useSkill` AC
- [x] No retune of T-041 plant rules; no Hell Viewing Fear; no Mind Transfer

## Verification

- **Evaluation route:** `autonomous`

### Objective AC (blocking)

- [ ] **RING-GUARD:** R0 only; no React/components/scenes/hooks/contexts imports in touched systems/constants.
- [ ] **`npm run typecheck`** passes.
- [ ] **AC1 — Authoring:** `mind_destruction` has `cardRole: ATTACK`, `apCost: 3`, `chakraCost: 6`, `cooldown: 4`, `baseDamage: 15`, Confusion 2 @0.65 contract, and explicit Read Mind Setup +50% no-consume contract; no primary legacy Confusion 3@1.0 / AP2-only identity → gate: `npm test -- src/game/systems/__tests__/t042MindDestruction` (`T-042 authoring`).
- [ ] **AC2 — Baseline resolve:** no `read_mind` on enemy; deterministic hit → `ok: true`, `damageDealt === 15` (test ports); Confusion applies only when rng succeeds at 0.65 policy locked in test → gate: same file `T-042 baseline`.
- [ ] **AC3 — Read Mind Setup +50% no consume:** enemy has `read_mind` duration ≥1; resolve hit → `damageDealt === 22` (or locked Setup-bucket rounding), and `read_mind` still on enemy after resolve (same duration/stacks) → gate: same file `T-042 setup read`.

### Subjective criteria
None.

## Out of scope

- False Surroundings / Mind Transfer / Hell Viewing / Mind Reading retune.
- Live EnemyTurn or `useCombat` wiring.
- Shunshin, Gentle Fist, Kunai Slash, Dancing Leaf, Kai packaging (other catalog rows).
- Dual-compat legacy Confusion 3 retention as primary identity.
- Editing SOUL; R2 badges.

## Notes for implementer

- Canon: SOUL §9 lectura marks; §11 Yamanaka; HTML `#catalogo` row `mind_destruction`; Marks/setup anchors `#setup` if present.
- Stable mark id: **`read_mind`** (T-041).
- SOUL §8: Mode enhanced 40–60% is separate from **Tactical Setup** % bucket — prefer Setup bucket labeling in any damage breakdown helper if one exists; do not invent UI.
- If existing rounding is not `floor(15*1.5)`, lock the actual pure-math result in AC3 and keep +50% contract.
- Fixture: plant `read_mind` via mark helper / T-041 resolve; no need for live combat scene.

## Close

- Reauthored `mind_destruction` as ATTACK AP3/CP6/CD4, 15 mental, Confusion 2 @65% on hit.
- New `Skill.setupRead` Setup-bucket: enemy `read_mind` → `floor(dmg * 1.5)` (22), no consume, no Mode.
- Cycles: 0. Review APPROVE. Verify 637 PASS.
- Improvements noted (not implemented): live `BattleSimulator` / `useCombat` do not apply `setupRead` or ATTACK `controlConfusion` (still `effects`-only). Candidate `/task-new` if live Yamanaka handoff should show 22 in combat.
