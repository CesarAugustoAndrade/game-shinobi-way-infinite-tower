# T-056 · R0 Explosive Tag: SIDE 11 C/M + PUSH 1 on impact

- **Type:** expand
- **Layer(s) touched:** domain (`explosive_tag` authoring + pure SIDE resolve + PUSH band move)
- **Ring:** R0 (`src/game/constants/skills.ts` `EXPLOSIVE_TAG`; `ResolveSkillSystem.ts` SIDE damage + existing `bandMove` PUSH path). No React.
- **Lado / Side:** `back`
- **Bounded context:** combat-tactical-setup · **Subdomain:** core
- **Priority:** P1
- **Agent estimate:** S
- **Evaluation route:** `autonomous`
- **Depends on:** T-007 resolve; T-023/T-026 `bandMove` PUSH forced move (no manual-move spend). Complements tool SIDEs after T-048/T-049/T-053. **Not blocked** by T-055 Stun.

## Génesis

**Why this gap:** SOUL §3.1 SIDE arma + §10 movement (card/forced move does not spend manual move; PUSH reactions only on real band change) + §3.8 content honesty. Canon catalog [`#catalogo`](../../docs/combat-tactical-setup-spec.html#catalogo) **explosive_tag**: SIDE · **1 AP · CD 2** · CLOSE · MEDIUM · “**11 dmg**; **PUSH 1** al impactar.” Runtime is ACTIVE-only **AP 2**, fire 11, **no** `cardRole`, **no** `allowedRanges`, **no** `bandMove` PUSH.

**Code evidence:**
- `EXPLOSIVE_TAG` (`skills.ts` ~708–726): `apCost: 2`; base 11; RANGED FIRE; **no** `cardRole`; **no** PUSH; **no** CLOSE/MEDIUM ranges.
- Air Palm / tools already use `bandMove: { kind: 'PUSH', steps: 1 }` + `skillForcedMove` in `ResolveSkillSystem` (~391–394); Explosive Tag never opts in.
- T-055 Génesis **discarded** “Explosive Tag PUSH 1 on hit” as the tool fire SIDE follow-up after Sweeping Kick.

**Already covered (do not duplicate):**
- **T-026** — Air Palm PUSH + Chakra Point (Hyuga SIDE).
- **T-023** — Blastback PUSH / Wire Reel PULL tool SIDEs (new v1 ids).
- **T-048 / T-049 / T-053** — Kunai / Shuriken tool chips (no PUSH).
- **T-055** — Sweeping Kick stun control (melee).

**Discarded alternatives:**
- Strong Fist 2×5 multi-hit ATTACK — multi-hit authoring.
- Wire Trap SUPPORT enemy mark — SUPPORT impact-consume.
- Senbon 35% Silence SIDE — control sibling after PUSH.
- Leaving AP2 fire ACTIVE without PUSH — fails catalog movement promise.

**SOUL pillars served:** §3.1 SIDE; §3.6/§10 board movement; §3.8 content honesty; catalog `#catalogo` explosive_tag; TOOL/FIRE tag path.

## Context

Tool SIDEs chip and setup. Explosive Tag is the cheap fire **blast-back** chip: **11** at C/M for **1 AP**, and on impact **PUSH** the enemy **1** band without spending the player’s manual move.

## Spec

### Must exist when done

1. **Authoring (`explosive_tag`)**
   - `cardRole: CardRole.SIDE_ATTACK`.
   - Costs: **AP 1**, **CP 0**, **CD 2**, HP 0.
   - `baseDamage: 11`; single hit; RANGED; FIRE; `allowedRanges: [CLOSE, MEDIUM]`.
   - Tags: TOOL / FIRE / MOVEMENT (or PHYSICAL+FIRE) as available.
   - `bandMove: { kind: 'PUSH', steps: 1 }` (or equivalent forced-move field already used by Air Palm / Blastback).
   - Honest description: 11 dmg C/M; PUSH 1 on impact.

2. **`resolveSkill` SIDE**
   - Pay costs; full hit → `damageDealt === 11` under test ports.
   - On **≥1 hit**: apply **PUSH 1** on enemy band (forced move); **`playerMoveUsedThisTurn` unchanged**.
   - On **full miss**: damage 0; **no** band change.
   - From **LONG** edge with PUSH: no band change (SOUL PUSH@LONG edge); play still ok if range allowed — note skill is C/M only so LONG play is illegal/disabled.
   - No Mode; no Stun; no Bleed required.

3. **Range legality**
   - CLOSE and MEDIUM legal; LONG not in `allowedRanges`.

### Architectural constraints
- [x] R0 pure; immutability
- [x] Catalog: AP1 · CD2 · 11 dmg · C/M · PUSH 1
- [x] Reuse existing PUSH/bandMove path; no new movement engine
- [x] No React / no live useSkill AC
- [x] No Air Palm / Kunai / Sweeping Kick retune

## Verification

- **Evaluation route:** `autonomous`

### Objective AC (blocking)

- [x] **RING-GUARD:** R0 only; no React/components/scenes/hooks/contexts imports in touched files.
- [x] **`npm run typecheck`** passes.
- [x] **AC1 — Authoring:** `explosive_tag` has `cardRole: SIDE_ATTACK`, AP1/CD2, `baseDamage: 11`, CLOSE+MEDIUM, PUSH 1 contract → gate: `npm test -- src/game/systems/__tests__/t056ExplosiveTag` (`T-056 authoring`).
- [x] **AC2 — Hit damage + PUSH:** resolve from CLOSE full hit → `damageDealt === 11`, enemy band one step farther (e.g. CLOSE→MEDIUM), `playerMoveUsedThisTurn` false if started false → gate: same file `T-056 push`.
- [x] **AC3 — Miss no push:** full miss → damage 0, band unchanged → gate: same file `T-056 miss`.

### Subjective criteria
None.

## Out of scope

- Strong Fist multi-hit; Wire Trap; Senbon Silence; Explosive Barrage.
- Range-reaction AI polish beyond existing PUSH collectors.
- Live UI; editing SOUL.

## Notes for implementer

- Canon: HTML `#catalogo` `explosive_tag`; SOUL §10 card/forced movement; mirror Air Palm `bandMove` PUSH.
- Single tag hit; do not invent multi-hit barrage here.

## Close

- Cycles: 1 (explore → plan auto-approve → implement → review APPROVE → verify PASS → merge ff).
- Summary: `explosive_tag` is SIDE AP1/CD2 CLOSE/MEDIUM 11 FIRE. On hit, PUSH 1 via `bandMove.requireHit` without spending the manual move. Miss stays put. Air Palm / Blastback miss-PUSH unchanged. Legacy AP2 unmarked fire packaging removed.
- Improvements noted: none in-scope.

