# T-010 · plan

**Ring:** R2 · **auto-approved**

R0 honesty helpers (role badge, preview sources, consume warning). SkillCard projects them. New CombatModesPanel + TacticalSetupPanel + SkillConfigInspector. Tests via `renderToStaticMarkup`.
