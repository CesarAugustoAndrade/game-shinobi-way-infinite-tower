# T-010 · exploration

**Ring:** R2 UI. View-models stay R0 (`combatSkillViewModel`).

- `SkillCard` badges are ACTIVE/TOGGLE/PASSIVE, not cardRole.
- No Modes/Setup panels. Preview is a single predictedDamage.
- No RTL; tests can use `react-dom/server` `renderToStaticMarkup`.
