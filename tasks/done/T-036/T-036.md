# T-036 · R0 Mind Transfer: SUPPORT 70% Stun 2 / fail self-Stun 1 (no Mode)

- **Type:** expand
- **Layer(s) touched:** domain (`mind_transfer` authoring + pure `resolveSkill` SUPPORT control + fail backlash)
- **Ring:** R0 (`src/game/constants/skills.ts` `MIND_TRANSFER`; `ResolveSkillSystem.ts` SUPPORT control resolution). No React.
- **Lado / Side:** `back`
- **Bounded context:** combat-tactical-setup · **Subdomain:** core
- **Priority:** P1
- **Agent estimate:** S–M
- **Evaluation route:** `autonomous`
- **Depends on:** T-001 `CardRole.SUPPORT`; T-007 `resolveSkill` SUPPORT branch; existing STUN buff shape used by enemy/player turn skip. Complements T-008/T-014 Mind Reading Discover (no Mode Yamanaka tunnel). **Not blocked** by T-031–T-035.

## Génesis

**Why this gap:** SOUL §11 Yamanaka — mental **sin Mode**; T1–2 **Mind Transfer**: “Stun 2 al **70%**; fallar = **self-Stun 1**.” Canon catalog [`#catalogo`](../../docs/combat-tactical-setup-spec.html#catalogo) **mind_transfer**: “**70% Stun 2** contra resistencia; si falla, **self-Stun 1**. **No hace daño** ni activa on-hit artifacts.” Pure resolve SUPPORT path does mark/Discover/Sealing only — no stun success/fail backlash. Legacy skill is ACTIVE-shaped with effects but no `cardRole` and no fail self-stun wire.

**Code evidence:**
- `MIND_TRANSFER` (`skills.ts` ~1832–1858): description already “Miss = self stun”; `effects` Stun 2 @0.7; **no** `cardRole`; baseDamage 0; **not** wired in `resolveSkill` SUPPORT branch (`ResolveSkillSystem.ts` ~622–629: marks / sealing / discover only).
- STUN buff shape already skips turns (`EnemyTurnSystem` / `PlayerTurnSystem` tests with `EffectType.STUN` buffs).
- Explicitly deferred: T-031/T-035 OOS “Yamanaka Mind Transfer.”

**Already covered (do not duplicate):**
- **T-008 / T-014** — Mind Reading Discover 3 MENTAL (different skill).
- **T-007** — SUPPORT resolve shell.
- **T-019** — Sealing Tag Mode drain / Silence (not Yamanaka stun).

**Discarded alternatives:**
- Hell Viewing Fear mark first — Attack payoff; Transfer is the control beat of the tunnel.
- Sharingan Predict recharge — needs charge-restore API (not present; larger).
- Smoke Bomb SIDE weight + impact mark — good S but Uchiha/tool; Yamanaka Mode-less tunnel is higher SOUL priority still open.
- Inventing damage on Mind Transfer — catalog **0 dmg**.

**SOUL pillars served:** §3.1 SUPPORT prepara/control; §3.7 Yamanaka sin Mode; §4 anti-visión (no Mode required); catalog `#catalogo` mind_transfer; §13 resolveSkill legal roles.

## Context

Yamanaka Mind Reading Discover exists. Mind Transfer is the high-risk control Support that must attempt enemy Stun 2 at 70% and, on fail, apply self-Stun 1 without dealing damage or requiring a Combat Mode — still legacy packaging with no pure resolve control path.

## Spec

### Must exist when done

1. **Authoring (`mind_transfer`)**
   - `cardRole: CardRole.SUPPORT`.
   - Costs: **AP 2**, **CP 7**, **CD 6**, HP 0 (catalog).
   - `baseDamage: 0`; no offensive hitCount required.
   - `allowedRanges`: ANY / omit / full bands as project convention for AUTO control (catalog ANY).
   - Stun contract in data: enemy Stun **duration 2**, **chance 0.7**; fail → self Stun **duration 1** (document field: dedicated `controlFailSelfStun` / effect pair / skill flag — pick one, test it).
   - Honest description: 70% Stun 2; on fail self-Stun 1; no damage; no Mode required.
   - Clan Yamanaka requirements may remain.

2. **`resolveSkill` SUPPORT control path**
   - On successful validate+commit (AP/CP/CD paid): **never** deal HP damage (`damageDealt === 0`, `enemyHp` unchanged from damage).
   - Roll once with `ports.rng` (or dedicated control roll port): if success (`rng < 0.7` or `<=` — **lock one** in tests):
     - Apply **enemy** STUN buff duration **2** on `enemyBuffs` (same shape as existing STUN buffs: `effect.type === EffectType.STUN`).
     - Do **not** apply self STUN.
   - On fail:
     - Apply **player** STUN buff duration **1** on `playerBuffs`.
     - Do **not** apply enemy STUN.
   - No Mode board mutation; no on-hit mark IMPACT path required (0 hits).
   - Invalid intent still consume-nothing.

3. **Compose**
   - Prefer a small pure helper `resolveControlSupport(skill, state, rng)` used only from SUPPORT branch when skill declares control stun — avoid ATTACK multi-hit path.
   - Do not implement Hell Viewing Fear or False Surroundings Confusion in this task’s AC.

### Architectural constraints
- [x] R0 pure; immutability
- [x] Numbers only catalog: 70% Stun 2 / fail self-Stun 1 / 0 dmg / AP2 CD6 CP7
- [x] No Mode requirement
- [x] No React / no live useSkill AC
- [x] No invent multi-target mind control possession

## Verification

- **Evaluation route:** `autonomous`

### Objective AC (blocking)

- [x] **RING-GUARD:** R0 only; no React/components/scenes/hooks/contexts imports in touched files.
- [x] **`npm run typecheck`** passes.
- [x] **AC1 — Authoring:** `mind_transfer` has `cardRole: SUPPORT`, `apCost: 2`, `chakraCost: 7`, `cooldown: 6`, `baseDamage: 0`, and declares 70% enemy Stun 2 + fail self-Stun 1 → gate: `npm test -- src/game/systems/__tests__/t036MindTransfer` (`T-036 authoring`).
- [x] **AC2 — Success stun:** resolve with deterministic rng success → `ok: true`, `damageDealt === 0`, enemyHp unchanged by damage, `enemyBuffs` has STUN duration 2, `playerBuffs` has no new STUN → gate: same file `T-036 success`.
- [x] **AC3 — Fail self-stun:** resolve with deterministic rng fail → `ok: true`, `damageDealt === 0`, `playerBuffs` has STUN duration 1, no enemy STUN from this play → gate: same file `T-036 fail`.

### Subjective criteria
None.

## Out of scope

- Hell Viewing Fear −20% next enemy damage; False Surroundings Confusion + MENTAL weight.
- Mind Reading Discover retune (T-014).
- Sharingan Predict Read Window + charge refill; Smoke Bomb.
- Live UI; resistance formulas beyond a single rng gate (catalog “contra resistencia” = use skill chance roll only unless an existing resistance port already exists — do not invent Calmness tables).
- Editing SOUL.

## Notes for implementer

- Canon: SOUL §11 Yamanaka; HTML `#catalogo` row `mind_transfer`.
- STUN buff shape: mirror `EnemyTurnSystem` tests (`effect: { type: EffectType.STUN, duration, chance: 1 }`).
- Document rng threshold: recommend `rng() < 0.7` → success (inject `() => 0` success, `() => 0.99` fail).

## Close summary

- **Status:** done (`76bf643` merged ff into `wt/jp-08121518`)
- **Shipped:** Mind Transfer SUPPORT 70% Stun 2. Fail → self-Stun 1. 0 damage. No Mode. `Skill.controlStun` + `resolveControlSupport` on SUPPORT branch. Threshold `rng() < 0.7`.
- **Cycles:** 1. No redesign. No review/verify FIX.
- **Sim:** T-036 probe all true. 0/0 dmg. Live Yamanaka/Mind Controller 100% (unchanged). Hyuga still 98.6% from T-035. **Catalog-faithful; control now wired. Live WR ceiling hides stun value (TTK ~1.5).**
- **Harvest:** no skill deltas (clean R0, no scores / deviations / fix cycles).
- **Noted:** live `useSkill` / Hell Viewing still OOS.

