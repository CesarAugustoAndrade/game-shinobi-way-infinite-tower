# T-051 exploration

**Ring:** R0. Route: `autonomous`.

`LEAF_WHIRLWIND` AP2 14, no role, ACC −15% @30%. ATTACK `markEffects` already plant on hit via `applySkillMarkEffects` (`perHit` or IMPACT). No resolver change. Re-classify: no.
