# T-039 · R0 Sharingan Predict: require Mode ON + Read Window −30 + restore 1 charge

- **Type:** expand
- **Layer(s) touched:** domain (`sharingan_predict` authoring + pure Mode charge restore + SUPPORT resolve)
- **Ring:** R0 (`src/game/constants/skills.ts` `SHARINGAN_PREDICT`; `CombatModeSystem.ts` charge restore; `ResolveSkillSystem.ts` SUPPORT). No React.
- **Lado / Side:** `back`
- **Bounded context:** combat-tactical-setup · **Subdomain:** core
- **Priority:** P1
- **Agent estimate:** S–M
- **Evaluation route:** `autonomous`
- **Depends on:** T-005 Mode board / charges; T-007 SUPPORT resolve; T-011 Sharingan Modes. Complements T-038 Smoke (Uchiha pressure down). **Not blocked** by T-031–T-038.

## Génesis

**Why this gap:** SOUL §11 Uchiha T1–2: **Smoke / Predict** “recarga lectura **sin** consumir Mode.” Canon catalog [`#catalogo`](../../docs/combat-tactical-setup-spec.html#catalogo) **sharingan_predict**: “**Requiere Sharingan ON**. **Read Window 1**: próximo Offensive enemigo **−30** puntos de impacto; **recarga 1 carga**, **sin sobrecap**.” Legacy is SPEED +25% evasion buff with **no** `cardRole`, **no** Mode gate, **no** charge restore API.

**Code evidence:**
- `SHARINGAN_PREDICT` (`skills.ts` ~2073–2099): ACTIVE only; SPEED buff 0.25×2; **no** `cardRole` / `modeInteraction` / Read Window mark.
- `CombatModeSystem` has `trySpendCharges` / activate at max — **no** restore/refill-while-ON helper (grep empty for tryAdd/restoreCharges). Ascent does `charges + 1` capped only on stage climb (~222).
- SUPPORT resolve does not require Mode ON for Predict; T-036 notes Predict needs charge-restore API.
- Deferred repeatedly: T-032–T-038 OOS Predict.

**Already covered (do not duplicate):**
- **T-005** — Mode ON board / spend / cap.
- **T-011** — Sharingan 2/3 Mode packaging.
- **T-038** — Smoke −25 first offensive + SIDE weight (sibling prep, not Predict).
- **T-025/T-034** — offensive Sharingan charge **spend**, not restore.

**Discarded alternatives:**
- SPEED evasion as Predict identity — anti-visión; catalog is Read Window + recharge.
- Require only `sharingan_2` and reject 3T ON — catalog “Sharingan ON” = family (2 or 3).
- Overcap refill past `maxCharges` — catalog **sin sobrecap**.
- Full enemy intent UI reveal — SOUL Elite “Sharingan lee la intención ya existente” is separate presentation; this task is mark + charges.

**SOUL pillars served:** §3.1 SUPPORT; §3.4 Modes charges no decay / recharge Support; §3.5 Marks; §11 Predict; catalog `#catalogo` sharingan_predict; `#modes`.

## Context

Sharingan Modes and SUPPORT resolve exist. Predict is the Mode-on Support that softens the next enemy offensive (−30 impact) and refunds one charge without ending the Mode — still a legacy evasion buff with no Mode contract.

## Spec

### Must exist when done

1. **Authoring (`sharingan_predict`)**
   - `cardRole: CardRole.SUPPORT`.
   - Costs: **AP 1**, **CP 3**, **CD 3**, HP 0.
   - `baseDamage: 0`.
   - `modeInteraction`: requires Sharingan family ON — prefer `requireOn: true` with `family: SHARINGAN` **or** accept either `sharingan_2` / `sharingan_3` ON (same family one-at-a-time). Document bind-to-ON Sharingan instance.
   - Optional explicit `restoreCharges: 1` (or equivalent) on modeInteraction / skill field.
   - `markEffects`: **read_window** (id preferred), duration **1**, encodes **−30** impact on next enemy Offensive (stacks `30` or fixed-by-id). Target enemy (or player-side “window” mark — lock one; enemy-targeted preferred for consistency with Smoke).
   - Drop primary SPEED evasion buff identity.
   - Honest description: requires Sharingan ON; Read Window 1 −30 next enemy offensive; restore 1 charge without overcap.

2. **Charge restore API (pure)**
   - Add `tryRestoreCharges(board, modeId, n, turnIndex?)` (name flexible): Mode must be ON; `charges = min(maxCharges, charges + n)`; never exceed def.maxCharges; does **not** end Mode; does **not** pull from COOLDOWN to ON.
   - If charges already at max: still ok resolve (no-op restore); mark still applies if Mode ON.

3. **`resolveSkill` SUPPORT**
   - If no Sharingan ON: reject `mode-required` (or equivalent), consume nothing.
   - If ON: pay AP/CP/CD; plant read_window mark; restore **1** charge on the ON Sharingan instance (cap max); `damageDealt === 0`.
   - Do not spend charges on Predict.

4. **Read Window −30 (minimal)**
   - Mark present after success with duration 1.
   - Enemy damage mult optional if pure path free (mirror Smoke T-038); else plant-only AC.

### Architectural constraints
- [x] R0 pure; immutability
- [x] No overcap; no Mode off/on cycle for restore
- [x] Family Sharingan ON (2 or 3)
- [x] No React / no live useSkill AC
- [x] No Smoke retune; no False Surroundings

## Verification

- **Evaluation route:** `autonomous`

### Objective AC (blocking)

- [x] **RING-GUARD:** R0 only; no React/components/scenes/hooks/contexts imports in touched files.
- [x] **`npm run typecheck`** passes.
- [x] **AC1 — Authoring:** `sharingan_predict` has `cardRole: SUPPORT`, AP1/CP3/CD3, `baseDamage: 0`, Mode require Sharingan ON, restore-1 contract, read_window mark −30 duration 1; no primary SPEED buff identity → gate: `npm test -- src/game/systems/__tests__/t039SharinganPredict` (`T-039 authoring`).
- [x] **AC2 — Reject without Mode:** empty board; resolve Predict → `ok: false` mode-required; pools/modes/marks unchanged → gate: same file `T-039 require`.
- [x] **AC3 — Restore + mark:** Sharingan 2 ON charges **1** (max 3); resolve Predict → `ok: true`, charges **2**, read_window present; second case charges already **3** → still ok, charges **3**, mark present → gate: same file `T-039 restore cap`.

### Subjective criteria
None.

## Out of scope

- Smoke Bomb (T-038); Fireball/Phoenix/Stream offense.
- Enemy intent full reveal UI; Elite telegraph scoring.
- False Surroundings Confusion + MENTAL weight.
- Live UI; editing SOUL.

## Notes for implementer

- Canon: SOUL §11 Uchiha Predict; HTML `#catalogo` sharingan_predict; `#modes` charges.
- Prefer family bind helper shared with Primary Lotus family-require if already merged; else ON check for `sharingan_2` OR `sharingan_3`.
- Restore is Support recharge of Mode **already ON** only (SOUL §9 Mode charge family).

## Close summary

- **Status:** done (`bcfb6ca` merged ff into `wt/jp-08121518`)
- **Shipped:** Predict SUPPORT requires Sharingan ON (2 or 3). Plants Read Window 1 (−30). Restores 1 charge via `tryRestoreCharges`, no overcap. SPEED +25% removed. Live EnemyTurn not wired.
- **Cycles:** 1. No redesign. No review/verify FIX.
- **Sim:** T-039 probe all true. Charges 1→2 / at-max 3. Next offensive 40 → 10 (helper). Live WR unchanged. **Catalog-faithful. Restore is healthy; −30 helper not overtuned vs Smoke −25.**
- **Harvest:** no skill deltas.
- **Noted:** EnemyTurnSystem does not call `applyReadWindowOutgoing` yet.

