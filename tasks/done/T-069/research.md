# T-069 research

1. **`requireHit` + ATTEMPT setup (recommended).** Once-per-card plant on ≥1 hit; consume on next ATTACK; ×1.1 in `markDamageMultiplier`.

2. **`perHit: true` like Aim/Launched.** 3 hits → 3 stacks. Multiplier is id-only so damage still ×1.1, but dishonest vs once-per-card.

3. **`consume: IMPACT` plant.** Next ATTACK ATTEMPT would not spend it.

**Recommendation:** option 1.
