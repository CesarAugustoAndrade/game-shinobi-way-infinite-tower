# T-038 · R0 Smoke Bomb: SUPPORT Smoke mark (−25 first offensive) + SIDE weight +1 next draw

- **Type:** expand
- **Layer(s) touched:** domain (`smoke_bomb` authoring + SUPPORT resolve mark + next-draw SIDE weight)
- **Ring:** R0 (`src/game/constants/skills.ts` `SMOKE_BOMB`; `SupportWeightSystem.ts` / `DeckSystem.ts` role next-draw bonus; `ResolveSkillSystem.ts` SUPPORT plant). No React.
- **Lado / Side:** `back`
- **Bounded context:** combat-tactical-setup · **Subdomain:** core
- **Priority:** P1
- **Agent estimate:** S–M
- **Evaluation route:** `autonomous`
- **Depends on:** T-003 `effectiveWeight`; T-016 pending support-weight bag; T-007 SUPPORT resolve + markEffects. Complements SOUL Uchiha Smoke beat / universal tool. **Not blocked** by T-031–T-037.

## Génesis

**Why this gap:** SOUL §11 Uchiha T1–2: **Smoke / Predict** “Baja presión.” Canon catalog [`#catalogo`](../../docs/combat-tactical-setup-spec.html#catalogo) **smoke_bomb**: “**Smoke 2**: primer Offensive enemigo **−25 puntos de impacto**; **SIDE weight +1** en el **próximo draw**. **No cambia Terrain**.” Legacy is SPEED +35% / ACC −20% stat buffs — anti-visión % and not the SOUL mark/weight contract.

**Code evidence:**
- `SMOKE_BOMB` (`skills.ts` ~758–780): ACTIVE only; **no** `cardRole`; SPEED/ACC % effects; **no** smoke mark; **no** next-draw SIDE weight.
- T-016 bag is **skillId-only** (`SupportWeightSystem.ts`); `effectiveWeight` only reads `supportBonuses[skill.id]` (`DeckSystem.ts` ~93) — no role-level +1 for all SIDE.
- SUPPORT resolve already enqueues T-016 entries and can plant marks (Sealing/Rotation patterns).
- Explicitly deferred: T-036/T-037 OOS Smoke Bomb.

**Already covered (do not duplicate):**
- **T-016** — Drill Main +2 / Gate Prep +3 skillId bag.
- **T-003** — weight formula slots.
- **T-023** — Off-Balance/Exposed mults (different marks; Smoke is −25 flat impact, not ×%).

**Discarded alternatives:**
- Keeping SPEED/ACC % as “smoke” — contradicts catalog mark + weight.
- Sharingan Predict first (require Mode + charge refill API missing) — larger than Smoke.
- Terrain/Heat interaction — **forbidden** SOUL v1 / catalog “No cambia Terrain.”
- Inventing full multi-band fog board state.

**SOUL pillars served:** §3.1 SUPPORT prepara; §3.2 transparent weights; §3.5 Marks; §3.6 no Terrain rewrite; §11 Smoke beat; catalog `#catalogo` smoke_bomb; anti-visión secret %.

## Context

Next-draw support weights and SUPPORT resolve exist. Smoke Bomb is the universal/Uchiha prep Support that should plant a legible **Smoke** setup (first enemy offensive −25 impact) and bias the **next** hand toward SIDE without permanent weight or Terrain changes — still a legacy evasion/accuracy % card.

## Spec

### Must exist when done

1. **Authoring (`smoke_bomb`)**
   - `cardRole: CardRole.SUPPORT`.
   - Costs: **AP 1**, **CP 0**, **CD 3**, HP 0 (catalog “—” CP).
   - `baseDamage: 0`.
   - `markEffects`: plant **smoke** (id `smoke` preferred), duration **2**, stacks/metadata encoding **−25 impact** on first enemy Offensive (document stacks `25` or fixed rule by id). Target enemy (or self-aura — prefer **enemy** or **board** rule locked in tests). Consume on first enemy offensive attempt/impact as appropriate.
   - Drop primary SPEED/ACC % buff identity (anti-visión).
   - Honest description: Smoke 2 −25 first enemy offensive impact; SIDE weight +1 next draw; no Terrain change.
   - Tag TOOL optional if SkillTag.TOOL exists.

2. **Next-draw SIDE weight +1**
   - On successful Smoke play, enqueue a **one-shot next-draw** bonus so every skill with `cardRole: SIDE_ATTACK` gains **+1** effective weight for **exactly one** draw materialization (same bag lifetime as T-016).
   - Prefer extending bag/WeightContext with `supportRoleBonuses` / role entries rather than enumerating all side skill ids at play time.
   - `effectiveWeight(sideSkill, ctx)` must increase by 1 when that role bonus is applied; ATTACK/SUPPORT/MODE unchanged by this bonus.
   - After consume/draw materialization, bag clears (T-016 one-shot rule).

3. **`resolveSkill` SUPPORT**
   - Pay costs; plant smoke mark on state; enqueue SIDE +1 weight entry; `damageDealt === 0`; no Terrain fields mutated (no terrain param required).

4. **Smoke −25 (minimal)**
   - Mark present after resolve with duration 2.
   - **Preferred if small:** pure helper or enemy-offensive path subtracts 25 from first enemy offensive damage once and consumes smoke. If enemy path is not pure-friendly, **AC requires mark plant only**; mult OOS documented.

### Architectural constraints
- [x] R0 pure; immutability
- [x] Numbers only catalog: −25 first offensive, SIDE +1 next draw, AP1 CD3
- [x] No Terrain/Heat interaction
- [x] No permanent baseWeight mutation
- [x] No React / no live useSkill AC
- [x] No Predict charge refill / False Surroundings

## Verification

- **Evaluation route:** `autonomous`

### Objective AC (blocking)

- [x] **RING-GUARD:** R0 only; no React/components/scenes/hooks/contexts imports in touched files.
- [x] **`npm run typecheck`** passes.
- [x] **AC1 — Authoring:** `smoke_bomb` has `cardRole: SUPPORT`, `apCost: 1`, `chakraCost: 0`, `cooldown: 3`, `baseDamage: 0`, smoke mark duration 2 (−25 contract), no primary SPEED/ACC % identity → gate: `npm test -- src/game/systems/__tests__/t038SmokeBomb` (`T-038 authoring`).
- [x] **AC2 — Resolve mark + weight enqueue:** resolve Smoke → `ok: true`, smoke mark present duration 2, pending bag materializes SIDE_ATTACK +1 (via role bonus or equivalent); `effectiveWeight` of a SIDE skill increases by 1 once, then not after second consume → gate: same file `T-038 weight mark`.
- [x] **AC3 — Role isolation:** with SIDE +1 bag applied, ATTACK skill weight unchanged vs empty bag → gate: same file `T-038 side only`.

### Subjective criteria
None.

## Out of scope

- Sharingan Predict Read Window + Mode charge refill.
- False Surroundings Confusion / MENTAL ATTACK weight.
- Hell Viewing / Mind Transfer retune.
- Live enemy AI UI; Terrain; editing SOUL.

## Notes for implementer

- Canon: SOUL §11 Uchiha Smoke; HTML `#catalogo` smoke_bomb; `#sorteo` weight transparency; SOUL §6 support term.
- Prefer `supportRoleBonuses?: Partial<Record<CardRole, number>>` on `WeightContext` + bag entries `{ role: SIDE_ATTACK, delta: 1 }` without breaking T-016 skillId path.
- Do not special-case skill id in `effectiveWeight` beyond data-driven bonuses.

## Close summary

- **Status:** done (`8f746b8` merged ff into `wt/jp-08121518`)
- **Shipped:** Smoke Bomb SUPPORT plants Smoke 2 (stacks 25). SIDE weight +1 next draw via `nextDrawRoleBonus` + role bag. `applySmokeOutgoing` −25 once. SPEED/ACC % removed. T-016 skillId path intact. Live EnemyTurn not wired.
- **Cycles:** 1. No redesign. No review/verify FIX.
- **Sim:** T-038 probe all true. T-016 still true. First offensive 40 → 15 (helper). Live WR ceiling unchanged. **Catalog-faithful. Helper −25 healthy; live apply OOS.**
- **Harvest:** no skill deltas.
- **Noted:** EnemyTurnSystem does not call `applySmokeOutgoing` yet.

