# T-050 · R0 Feint Strike: SIDE 7 AUTO CLOSE + Feint 2 (+15 next ATTACK)

- **Type:** expand
- **Layer(s) touched:** domain (`feint_strike` authoring + pure SIDE resolve + next-ATTACK flat setup mark)
- **Ring:** R0 (`src/game/constants/skills.ts` `FEINT_STRIKE`; `ResolveSkillSystem.ts` SIDE mark plant + ATTACK read of Feint). No React.
- **Lado / Side:** `back`
- **Bounded context:** combat-tactical-setup · **Subdomain:** core
- **Priority:** P1
- **Agent estimate:** S
- **Evaluation route:** `autonomous`
- **Depends on:** T-007 resolve; T-004/T-023 SIDE markEffects + attempt consume. Complements taijutsu setup SIDEs. **Not blocked** by T-048/T-049.

## Génesis

**Why this gap:** SOUL §3.1 SIDE arma + §9 Marks (own attempt consume). Canon catalog [`#catalogo`](../../docs/combat-tactical-setup-spec.html#catalogo) **feint_strike**: SIDE · **1 AP · CD 2** · CLOSE · “**7 dmg AUTO**; **Feint 2** da **+15 puntos de impacto** al **próximo ATTACK**.” Runtime is ACTIVE, **AP 2**, bare 7 AUTO, **no** `cardRole`, **no** Feint mark — no setup arming.

**Code evidence:**
- `FEINT_STRIKE` (`skills.ts` ~417–435): `apCost: 2`; AUTO; base 7; **no** `cardRole`; **no** `markEffects`; **no** next-ATTACK bonus.
- T-023 plants SIDE marks and reads Off-Balance/Exposed mults; no `feint` flat +15 path.
- T-049 discarded “Feint Strike +15 impact next ATTACK” as the follow-up after Kunai Throw.

**Already covered (do not duplicate):**
- **T-023** — Off-Balance +20% / Exposed +15% ranged (percent marks, different ids).
- **T-048 / T-049** — Kunai Slash/Throw tool chips.
- **T-004** — ATTEMPT consume on own marks.

**Discarded alternatives:**
- Leaf Whirlwind −1 SPD ATTACK — different role; Feint multiplies SIDE setup after tools.
- Byakugan Scan Mode pen — Hyuga SUPPORT.
- Kawarimi substitution reaction — larger reaction slice.
- Inventing “cannot be evaded” beyond AUTO always-hit — catalog AUTO already covers; do not invent new dodge rules.

**SOUL pillars served:** §3.1 SIDE; §3.5 Marks; §9 attempt consume; catalog `#catalogo` feint_strike.

## Context

SIDE tools plant setup marks. Feint Strike should be the cheap CLOSE AUTO chip that deals **7** and plants **Feint** for **2** so the **next ATTACK** gains **+15** impact (flat damage under pure resolve) — still a legacy AP2 unmarked ACTIVE.

## Spec

### Must exist when done

1. **Authoring (`feint_strike`)**
   - `cardRole: CardRole.SIDE_ATTACK`.
   - Costs: **AP 1**, **CP 0**, **CD 2**, HP 0.
   - `baseDamage: 7`; single hit; CLOSE; `attackMethod: AUTO` (deterministic hit under AUTO policy / test ports).
   - Tags: TAIJUTSU / PHYSICAL / SETUP as available.
   - `markEffects`: self (or own) mark id **`feint`**, duration **2**, `consume: ATTEMPT`, metadata **+15** flat to next ATTACK (stacks 15 or documented constant). Prefer plant on **player** as own setup (owner PLAYER, target PLAYER or enemy-neutral — match Off-Balance pattern: own ATTEMPT mark consumed when player attacks).
   - Honest description: 7 AUTO CLOSE; Feint 2 → +15 next ATTACK.

2. **`resolveSkill` SIDE**
   - Pay costs; AUTO/full hit → `damageDealt === 7`.
   - Plant `feint` duration 2 after successful play (prefer even on miss if AUTO always hits; if miss possible, plant only on ≥1 hit — **prefer always plant on ok resolve** for AUTO catalog, lock in test).
   - No Mode; no Bleed; no band move.

3. **Next ATTACK +15**
   - When resolving `cardRole === ATTACK` (not SIDE), if `feint` is among own ATTEMPT marks spent this attempt: add **+15** flat to `damageDealt` after hit aggregation (only if hitsLanded ≥ 1; on full miss still consume mark at attempt per SOUL own-mark rule).
   - SIDE plays do not consume Feint for the +15 path (only ATTACK).
   - Duration 2 if not consumed: survives one unused turn per existing mark clock (AC focuses on attempt consume +15).

### Architectural constraints
- [x] R0 pure; immutability
- [x] Catalog: AP1 · CD2 · 7 dmg · Feint 2 · +15 next ATTACK
- [x] Flat +15 honest integer (not secret %)
- [x] No React / no live useSkill AC
- [x] No Kunai/Scan/Kawarimi retune

## Verification

- **Evaluation route:** `autonomous`

### Objective AC (blocking)

- [ ] **RING-GUARD:** R0 only; no React/components/scenes/hooks/contexts imports in touched files.
- [ ] **`npm run typecheck`** passes.
- [ ] **AC1 — Authoring:** `feint_strike` has `cardRole: SIDE_ATTACK`, AP1/CD2, `baseDamage: 7`, CLOSE, AUTO, `feint` duration 2 contract → gate: `npm test -- src/game/systems/__tests__/t050FeintStrike` (`T-050 authoring`).
- [ ] **AC2 — Chip + plant:** resolve feint → `damageDealt === 7`, own `feint` mark duration 2 present → gate: same file `T-050 plant`.
- [ ] **AC3 — Next ATTACK +15 consume:** with `feint` present, resolve ATTACK base 10 full hit → `damageDealt === 25` (10+15) under ports; `feint` gone; without feint damage 10 → gate: same file `T-050 payoff`.

### Subjective criteria
None.

## Out of scope

- Leaf Whirlwind SPD debuff; Byakugan Scan; Kawarimi; Kunai pair retune.
- New dodge/evasion engine beyond AUTO.
- Live UI; editing SOUL.

## Notes for implementer

- Canon: HTML `#catalogo` `feint_strike`; SOUL §9 own attempt marks.
- “Puntos de impacto” locked as **+15 flat damage** on next ATTACK for pure AC (not ACC% invent).
- Prefer mark id `feint`; consume ATTEMPT like `off_balance`.

## Close

- Reauthored `feint_strike` as SIDE AP1/CD2 CLOSE AUTO 7. Plants Feint 2; next ATTACK +15 and consumes.
- Cycles: 0. Review APPROVE. Verify 661 PASS.
