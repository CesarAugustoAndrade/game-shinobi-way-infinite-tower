# T-034 · R0 Chidori Stream: Sharingan 3T +40% + 1 charge + Stun packaging

- **Type:** expand
- **Layer(s) touched:** domain (`chidori_stream` authoring + pure `resolveSkill` Mode enhance)
- **Ring:** R0 (`src/game/constants/skills.ts` `CHIDORI_STREAM`; reuse `modeInteraction.damageMultBonus` + `consumeCharges` on `sharingan_3`). No React.
- **Lado / Side:** `back`
- **Bounded context:** combat-tactical-setup · **Subdomain:** core
- **Priority:** P1
- **Agent estimate:** S
- **Evaluation route:** `autonomous`
- **Depends on:** T-005 Mode board; T-007 resolve; T-018 Mode enhance pattern; T-025 Chidori 3T Mode id. Complements Uchiha lightning after Chidori thrust. **Not blocked** by T-030–T-033.

## Génesis

**Why this gap:** SOUL §11 Uchiha enhanced Attacks under Sharingan 3T (Lightning/Chidori family). Canon catalog [`#catalogo`](../../docs/combat-tactical-setup-spec.html#catalogo) **chidori_stream**: “**18 dmg + 60% Stun 1**. **Sharingan 3T: +40% y consume 1 carga**.” T-025 owns Chidori thrust (2 charges + MEDIUM); Stream’s **1 charge +40%** control Attack is still legacy with **no** `cardRole` / `modeInteraction`.

**Code evidence:**
- `CHIDORI_STREAM` (`skills.ts` ~2323–2348): HIDDEN ACTIVE; AP **2** (catalog **4**); base 18; Stun chance **0.8** (catalog **60%**); **no** `cardRole`, **no** `modeInteraction`, **no** CLOSE range honesty, AUTO AoE flavor without SOUL multi-target contract.
- Resolve already applies `damageMultBonus` + `consumeCharges` when `modeId` is ON (Rasengan T-018; Fireball/Phoenix backlog T-032/T-033).
- Explicitly OOS elsewhere: T-032/T-033 “Chidori Stream”; T-025 Fireball/Phoenix lote (not Stream).

**Already covered (do not duplicate):**
- **T-025** — Chidori 3T MEDIUM + **2** charges (different skill, different charge count).
- **T-018** — Mode +% + charge auto path.
- **T-011** — Sharingan Mode packaging.

**Discarded alternatives:**
- Full 64 Palms Hyuga mark-stack scaling — M/L new math; defer after Uchiha Stream S slice.
- Sharingan Predict recharge SUPPORT — different verb (mark/read window).
- AoE multi-enemy Stream — catalog is single CLOSE control Attack; do not invent multi-target v1.
- `requireOn` — base Stream works without Mode.

**SOUL pillars served:** §3.1 ATTACK; §3.4 Mode routes; §3.7 Uchiha; §8 charges at attempt; catalog `#catalogo` chidori_stream.

## Context

Sharingan 3-Tomoe Mode and pure Mode enhance exist. Chidori Stream is the lightning control Attack that should stay playable at base, gain +40% and spend one 3T charge when enhanced, and declare honest Stun 60% packaging — currently a legacy AUTO with 80% stun and no Mode contract.

## Spec

### Must exist when done

1. **Authoring (`chidori_stream`)**
   - `cardRole: CardRole.ATTACK`.
   - Costs: **AP 4**, **CP 7**, **CD 4**, HP 0 (catalog).
   - `baseDamage: 18`; single hit (`hitCount` 1 or omit).
   - `allowedRanges: [CombatRange.CLOSE]` (catalog CLOSE only; no grantRanges unless catalog adds them — it does not).
   - `attackMethod`: MELEE or AUTO only if project treats AUTO as single-target; **do not** invent multi-enemy AoE resolution this task.
   - `modeInteraction`:
     - `modeId: 'sharingan_3'`
     - `family: MODE_FAMILY.SHARINGAN` (or `'SHARINGAN'`)
     - `consumeCharges: 1`
     - `damageMultBonus: 0.4`
     - **No** `requireOn`; **no** `grantRanges` (unlike Chidori thrust).
   - Stun packaging: `effects` Stun duration **1**, chance **0.6** (catalog 60%); drop legacy 0.8 as identity.
   - Honest description: base 18 + 60% Stun 1; with Sharingan 3-Tomoe ON +40% and 1 charge at attempt.

2. **`resolveSkill` behavior**
   - Mode OFF: baseline damage under deterministic hit port; no charge spend.
   - Sharingan 3 ON with ≥1 charge: damage = `floor(baseline * 1.4)` (T-018 total floor convention); spend **1** charge at attempt; Mode stays ON if charges remain.
   - Sharingan **2** alone does not apply this enhance (AC only 3T).
   - Do not change Chidori thrust `consumeCharges: 2` / `grantRanges`.

3. **Stun**
   - Authoring chance 0.6 is required. If pure resolve does not roll `effects[]` into buffs, AC does not require live Stun application this task (same honesty pattern as Burn on Fireball tasks) — document if skipped.

### Architectural constraints
- [x] R0 pure; immutability
- [x] Numbers only catalog: 18 dmg, +40%, 1 charge, 3T, AP4/CD4/CP7, Stun 60%×1
- [x] Base viable without Mode
- [x] No multi-target AoE invent
- [x] No React / no live useSkill AC
- [x] No 64 Palms / Predict / Fireball AC here

## Verification

- **Evaluation route:** `autonomous`

### Objective AC (blocking)

- [x] **RING-GUARD:** R0 only; no React/components/scenes/hooks/contexts imports in touched files.
- [x] **`npm run typecheck`** passes.
- [x] **AC1 — Authoring:** `chidori_stream` has `cardRole: ATTACK`, `apCost: 4`, `chakraCost: 7`, `cooldown: 4`, `baseDamage: 18`, CLOSE range, `modeInteraction` with `modeId: 'sharingan_3'`, `consumeCharges: 1`, `damageMultBonus: 0.4`, `requireOn` absent/false; Stun effect chance **0.6** duration 1 → gate: `npm test -- src/game/systems/__tests__/t034ChidoriStream` (`T-034 authoring`).
- [x] **AC2 — Base without Mode:** empty modes; deterministic hit → `damageDealt` equals baseline 18; modes empty → gate: same file `T-034 base`.
- [x] **AC3 — 3T enhance + charge:** Sharingan 3 ON charges **3**; resolve hit → damage `floor(18 * 1.4)` (or baseline×1.4 under test port); charges **2**; Mode still ON → gate: same file `T-034 sharingan enhance`.

### Subjective criteria
None.

## Out of scope

- Chidori thrust retune (T-025); Fireball/Phoenix (T-032/T-033); Sharingan Predict.
- 64 Palms mark-stack +10%/cap; Yamanaka Mind Transfer backlash; Smoke Bomb.
- Multi-enemy AoE Stream; live Stun RNG AC (unless already free on resolve).
- Live UI; editing SOUL.

## Notes for implementer

- Canon: SOUL §11 Uchiha; HTML `#catalogo` row `chidori_stream`; `#modes` Sharingan 3-Tomoe.
- Mirror T-018 / T-032 tests with `sharingan_3` fixture (not `sharingan_2`).
- Keep Chidori and Stream distinct: Stream never grants MEDIUM; Stream spends 1 not 2.

## Close summary

- **Status:** done (`d27fef2` merged ff into `wt/jp-08121518`)
- **Shipped:** Chidori Stream ATTACK 18 at CLOSE. Base legal without Mode. Sharingan 3-Tomoe ON: +40% and spend 1 charge. Stun 60%×1. MELEE, no MEDIUM grant. Chidori thrust unchanged.
- **Cycles:** 1. No redesign. No review/verify FIX.
- **Sim:** T-034 probe all true. Legacy 18/AP2/stun 80% → 18/AP4/stun 60%; 3T enhance 25. Live WR 100% all presets. **Catalog-faithful; base dmg/AP halved unless 3T ON (intended control Attack).**
- **Harvest:** no skill deltas (clean R0, no scores / deviations / fix cycles).
- **Noted:** live Stun RNG / `useSkill` still OOS. Predict / 64 Palms still backlog.

