# T-014 · exploration

**Ring:** R0 resolve + DeckSystem.discoverThree.

- `discoverThree` already samples ≤3 excluding source + hand; CD snapshots via `snapshotSkill`.
- `resolveSkill` SUPPORT only applies marks. State has no hand/pendingDiscover.
- T-008 Rehearsal/Mind Reading author `discover: { count: 3 }`.
