# T-026 · R0 Air Palm: SIDE chip + PUSH 1 + Chakra Point per hit (no Mode charge)

- **Type:** expand
- **Layer(s) touched:** domain (Air Palm authoring + pure resolve per-hit mark + forced PUSH)
- **Ring:** R0 (`src/game/constants/skills.ts` `air_palm`; `ResolveSkillSystem.ts` SIDE mark/per-hit + movement). No React.
- **Lado / Side:** `back`
- **Bounded context:** combat-tactical-setup · **Subdomain:** core
- **Priority:** P1
- **Agent estimate:** S–M
- **Evaluation route:** `autonomous`
- **Depends on:** T-004 Marks (`addMark`, impact/attempt families); T-006 `resolveForcedMove` PUSH; T-007 `resolveSkill` / multi-hit. Feeds T-022 Twin Lion CP read. Complements T-023 skill band move pattern. **Not blocked** by T-016–T-025.

## Génesis

**Why this gap:** SOUL §11 Hyuga beat **Air Palm**: “Chip, **PUSH**, **CP por hit** **sin** gastar cargas.” Twin Lion (T-022) reads enemy `chakra_point` marks; without Air Palm (or equivalent Side) the Hyuga setup tunnel has no honest CP applicator. Canon: [`docs/combat-tactical-setup-spec.html#setup`](../../docs/combat-tactical-setup-spec.html#setup), [`#tablero`](../../docs/combat-tactical-setup-spec.html#tablero), SOUL §9 multi-hit / marks, §10 forced movement no voluntary spend, §11 Hyuga T1–2.

**Code evidence:**
- `air_palm` (`skills.ts` ~1188–1212): legacy ACTIVE only — **no** `cardRole`, **no** `markEffects`, **no** PUSH/`bandMove`, description is generic chip with no CP/PUSH contract.
- `resolveSkill` applies `markEffects` only on SUPPORT (`applySupportMarks` ~203–218, ~405); SIDE/ATTACK path rolls damage but does **not** plant marks or apply per-hit CP; `perHitEffects` only increments a counter (~427–429).
- Movement only if caller sets `intent.movement` (~438–452) — skill does not declare PUSH.
- T-022 Out of scope: “Full Hyuga catalog reauthor (**Air Palm CP apply**, Rotation, 64 Palms).”
- T-023 owns Wire/Blastback/Backstep tools, **not** Air Palm.
- No backlog T-XXX titles mention Air Palm.

**Already covered (do not duplicate):**
- **T-004** — mark board / multi-hit rolls.
- **T-006** — forced PUSH boundary rules.
- **T-007** — resolve skeleton.
- **T-022** — Twin Lion **reads** CP (consumer); this task **applies** CP.
- **T-023** — generic SIDE markEffects + band move for the three universal tools (share helpers if present).

**Discarded alternatives:**
- Rotation (kaiten) require Byakugan + charge shield — separate Mode-gated SUPPORT/utility slice.
- 64 Palms full reauthor — larger ATTACK lote.
- Inventing CP stacks without hits landing — SOUL says **por hit** (only successful hits).
- Spending Byakugan charges on Air Palm — SOUL says **sin** gastar cargas.

**SOUL pillars served:** §3.1 SIDE verb; §3.5 Marks setup; §3.7 Hyuga tunnel; §9 per-hit; §10 forced move; enables T-022 honesty.

## Context

Byakugan Mode and Twin Lion payoff tasks assume enemy Chakra Point marks exist. Air Palm is the SOUL-named Side that applies them on hit and PUSHes without Mode cost. This task reauthors Air Palm and wires pure `resolveSkill` so each landed hit plants/stacks `chakra_point`, band PUSHes once per card (not per hit), and Mode charges are untouched.

## Spec

### Must exist when done

1. **Authoring (`air_palm`)**
   - `cardRole: CardRole.SIDE_ATTACK`.
   - Honest description: chip damage; **PUSH 1** after resolve; **Chakra Point** on enemy **per landed hit**; does **not** spend Byakugan charges.
   - Encode marks for resolve, e.g.:
     - `markEffects` or `perHit`-scoped mark apply for `chakra_point` (duration per SOUL/HTML typical 1–2; align Twin Lion / existing `chakra_point` duration used on Twin Lion authoring — prefer **same id `chakra_point`** and duration **2** if Twin Lion uses 2).
     - Band move: PUSH foe 1 (same pattern as T-023 `bandMove` / skill-declared forced move if that field lands; else id-hardmap only for `air_palm`).
   - Keep existing rough costs (AP/CP/CD) unless SOUL table contradicts — SOUL §11 does not retune Air Palm AP numbers; do not invent retune.

2. **resolveSkill behavior**
   - Role SIDE: deal damage via existing multi-hit path (`hitCount` default 1 unless authored multi).
   - For each **landed** hit: add/stack enemy-targeted `chakra_point` mark (player-owned setup mark on enemy). Misses plant **no** CP for that hit.
   - After hits (card once): apply **PUSH 1** without setting `playerMoveUsedThisTurn` (forced move). Boundary PUSH@LONG → no band change, no invented extra effect.
   - **Never** call `trySpendCharges` / Mode board mutation for Air Palm (even if Byakugan ON).

3. **Compose with T-022/T-023**
   - Prefer shared SIDE mark plant + band-move helpers from T-023 if merged; if not, implement for Air Palm without blocking on T-023.
   - CP mark id must be readable by Twin Lion fixtures (`chakra_point`).

### Architectural constraints
- [x] R0 pure; immutability
- [x] No Mode charge spend
- [x] No invented CP on miss
- [x] No React / no live `useSkill` requirement for AC
- [x] No Terrain/Heat

## Verification

- **Evaluation route:** `autonomous`

### Objective AC (blocking)

- [x] **RING-GUARD:** R0 only; no React/components/scenes/hooks/contexts imports.
- [x] **`npm run typecheck`** passes.
- [x] **AC1 — Authoring SIDE + CP contract:** `air_palm` has `cardRole === SIDE_ATTACK` and declares chakra_point (markEffects/perHit) + PUSH intent in typed fields or stable test-visible mapping → gate: `npm test -- src/game/systems/__tests__/t026AirPalm` (`T-026 authoring`).
- [x] **AC2 — Hit plants CP + PUSH; no charge spend:** Fixture Byakugan ON charges 4; range CLOSE or MEDIUM as needed; resolve Air Palm with 1 hit landed → marks include `chakra_point`; range one band farther if not already LONG; Mode charges still **4** → gate: same file `T-026 hit push no spend`.
- [x] **AC3 — Miss no CP; multi-hit stacks:** (a) all hits miss → no new `chakra_point`; (b) if `hitCount >= 2` with 2 hits landed → CP stacks or two instances per MarkSystem rules, **2** successful apply path (document stack merge) → gate: same file `T-026 miss and multi`.

### Subjective criteria
None.

## Out of scope

- Twin Lion enhance/consume (T-022).
- Rotation/kaiten Byakugan gate + reflect; 64 Palms.
- Wire/Blastback/Backstep (T-023); Tripwire (T-024); Chidori (T-025).
- Live `useSkill` cutover; R2 Setup panel polish.
- Editing SOUL.

## Notes for implementer

- Per-hit CP: loop `hitsLanded` times `addMark` with `chakra_point`, or one mark with stacks += hitsLanded if MarkFamily.STAT merge — pick one consistent with T-004 STAT merge and Twin Lion “read CP”.
- PUSH once per card after multi-hit (SOUL Side arms position once, not N pushes).
- `hitCount` may stay 1; AC3(b) can use a fixture skill clone with hitCount 2 if Air Palm stays single-hit.
- Coordinate mark id with Twin Lion / skillsCombatV1New `chakra_point` on Twin Lion row.

## Close summary

- **Status:** done (`814be45` merged ff into `wt/jp-08121518`)
- **Shipped:** Air Palm SIDE_ATTACK plants `chakra_point` (duration 2, IMPACT, STAT) per landed hit and PUSHes 1 band without spending Byakugan. IMPACT plants after `consumeOnImpact` so the new CP is not self-consumed. Miss plants nothing. 2 hits STAT-merge to stacks 2.
- **Cycles:** 1. No redesign. No review/verify FIX.
- **Sim:** T-026 probe all true. Hyuga live WR 100% → 99.8% (1 loss vs CASTER). **Healthy** — PUSH can strand melee follow-ups; not overtuned.
- **Harvest:** no skill deltas (clean R0, no scores / deviations / fix cycles).
- **Noted:** live `useSkill` still not on this path (explicitly out of scope).
