# T-033 · R0 Phoenix Flower: 3-hit Attack + Sharingan 2T +40% + 1 charge

- **Type:** expand
- **Layer(s) touched:** domain (`phoenix_flower` authoring + pure multi-hit + Mode enhance already used by Rasengan/Fireball)
- **Ring:** R0 (`src/game/constants/skills.ts` `PHOENIX_FLOWER`; reuse `ResolveSkillSystem` `hitCount` + `modeInteraction.damageMultBonus` / `consumeCharges`). No React.
- **Lado / Side:** `back`
- **Bounded context:** combat-tactical-setup · **Subdomain:** core
- **Priority:** P1
- **Agent estimate:** S
- **Evaluation route:** `autonomous`
- **Depends on:** T-005 Mode board; T-007 multi-hit resolve; T-018 Mode enhance pattern; T-032 Fireball (sibling 2T, different mult). Complements Uchiha Main/chip Attack. **Not blocked** by T-030–T-032 merge.

## Génesis

**Why this gap:** SOUL §11 Uchiha T2–3: **Kunai / Burn** “Side afina; **Phoenix Flower = Attack normal**.” Canon catalog [`#catalogo`](../../docs/combat-tactical-setup-spec.html#catalogo) **phoenix_flower**: “**3×5** dmg; si impacta, Burn 4×2. **Main inicial Uchiha**. **Sharingan 2T: +40%, consume 1 carga**.” T-032 owns Fireball 2T +50%; Phoenix multi-hit 2T +40% is still legacy single-hit with no Mode contract.

**Code evidence:**
- `PHOENIX_FLOWER` (`skills.ts` ~126–160): BASIC ACTIVE; AP **1** (catalog **2**); single `baseDamage: 14` (catalog **3×5**); Burn 10×2 @50% (catalog **4×2** on hit); **no** `cardRole`, **no** `hitCount`, **no** `modeInteraction`, **no** MEDIUM·LONG ranges.
- Resolve already supports multi-hit (`hitCount`) and Mode `damageMultBonus` + `consumeCharges` (Rasengan T-018; Fireball T-032 backlog).
- T-032 OOS explicitly: “Phoenix Flower (3×5, +40% 2T) — separate sibling.” T-025 OOS Phoenix lote.

**Already covered (do not duplicate):**
- **T-018** — Mode +% + 1 charge auto path.
- **T-032** — Fireball 2T +50% (same Mode, different packaging).
- **T-025** — Chidori 3T path.
- **T-008** — twelve new techniques (Phoenix is reauthor of existing, not new id).

**Discarded alternatives:**
- Bundling Fireball+Phoenix+Predict — L; keep S slice.
- Role SIDE_ATTACK — catalog/SOUL: Phoenix is **Attack normal** / Main-eligible ATTACK, not Side.
- `requireOn` — base multi-hit works without Mode; enhance only when 2T ON.
- Sharingan 3T enhance — catalog says **2T** +40%.

**SOUL pillars served:** §3.1 ATTACK; §3.4 Mode routes; §3.7 Uchiha; §8 charges at attempt; §9 multi-hit; §11 Phoenix beat; catalog `#catalogo` phoenix_flower.

## Context

Uchiha 2-Tomoe Mode enhance exists on pure resolve. Phoenix Flower is the early multi-hit fire Attack (Main-eligible) that should volley 3 hits, apply Burn on impact, and optionally spend one 2T charge for +40% — still a legacy single 14-dmg card.

## Spec

### Must exist when done

1. **Authoring (`phoenix_flower`)**
   - `cardRole: CardRole.ATTACK`.
   - `hitCount: 3`; `baseDamage: 5` (catalog 3×5).
   - Costs: **AP 2**, **CP 5**, **CD 2**, HP 0.
   - `allowedRanges: [CombatRange.MEDIUM, CombatRange.LONG]`.
   - `modeInteraction`:
     - `modeId: 'sharingan_2'`
     - `family: MODE_FAMILY.SHARINGAN` (or `'SHARINGAN'`)
     - `consumeCharges: 1`
     - `damageMultBonus: 0.4`
     - **No** `requireOn`.
   - Burn on hit: reauthor effect/DoT to **Burn 4 duration 2** (catalog); apply when ≥1 hit lands if that is existing multi-hit effects policy — do not invent per-hit burn stacks unless catalog says per hit (it says “si impacta”).
   - Honest description: 3-hit volley; Burn on impact; with Sharingan 2-Tomoe ON +40% and 1 charge at attempt. Main-eligible Uchiha Attack (do not auto-set `mainAttackId` in this task).

2. **`resolveSkill` behavior**
   - Mode OFF: 3 independent hits; total baseline under deterministic full-hit port = 3×5 (or sum of per-hit rolls); no charge spend.
   - Sharingan 2 ON with ≥1 charge: total damage uses **+40%** (`damageMultBonus: 0.4`, floor convention consistent with T-018/T-021 on **total** multi-hit damage); spend **1** charge at attempt; Mode remains ON if charges remain.
   - MEDIUM/LONG legal; CLOSE without grant must fail range validation if ranges are enforced (same as other ranged Attacks).

3. **Compose**
   - Mirror Fireball/Rasengan fields; only packaging deltas (hitCount, 0.4, costs).
   - Do not retune Fireball (T-032) or Chidori (T-025).

### Architectural constraints
- [x] R0 pure; immutability
- [x] Numbers only catalog: 3×5, +40%, 1 charge, 2T, AP2/CD2/CP5, Burn 4×2
- [x] Base viable without Mode
- [x] No React / no live useSkill AC
- [x] No Predict / Stream / Fireball AC in this task

## Verification

- **Evaluation route:** `autonomous`

### Objective AC (blocking)

- [x] **RING-GUARD:** R0 only; no React/components/scenes/hooks/contexts imports in touched files.
- [x] **`npm run typecheck`** passes.
- [x] **AC1 — Authoring:** `phoenix_flower` has `cardRole: ATTACK`, `hitCount: 3`, `baseDamage: 5`, `apCost: 2`, `chakraCost: 5`, `cooldown: 2`, MEDIUM+LONG ranges, `modeInteraction` with `modeId: 'sharingan_2'`, `consumeCharges: 1`, `damageMultBonus: 0.4`, `requireOn` absent/false → gate: `npm test -- src/game/systems/__tests__/t033PhoenixFlower` (`T-033 authoring`).
- [x] **AC2 — Base multi-hit no Mode:** empty modes; deterministic 3 hits → `hitsLanded: 3`, `damageDealt` equals baseline 15 (or documented 3×5 sum), modes empty → gate: same file `T-033 base multi`.
- [x] **AC3 — 2T enhance + charge:** Sharingan 2 ON charges **3**; resolve full hits → damage = `floor(baseline * 1.4)`; charges **2**; Mode still ON → gate: same file `T-033 sharingan enhance`.

### Subjective criteria
None.

## Out of scope

- Great Fireball retune (T-032); Chidori Stream; Sharingan Predict (Read Window + recharge charge).
- Auto-assign Uchiha starter Main to Phoenix (Skill Config / content kit — separate if needed).
- Full Burn tick simulation if pure resolve does not apply `effects[]` — authoring honesty required.
- 64 Palms; Yamanaka; Lee Lotus tasks (T-030/T-031).
- Live UI; editing SOUL.

## Notes for implementer

- Canon: SOUL §11 Uchiha; HTML `#catalogo` row `phoenix_flower`.
- Scale floor on **total** multi-hit like Peacock/T-021 style when mult applies after hits sum, or document same approach as existing multi-hit+Mode skills in repo.
- Prefer not special-casing skill id; data-driven `modeInteraction` only.

## Close summary

- **Status:** done (`d7e1aa8` merged ff into `wt/jp-08121518`)
- **Shipped:** Phoenix Flower ATTACK 3×5 at MEDIUM/LONG. Base legal without Mode. Sharingan 2-Tomoe ON: +40% and spend 1 charge. Burn 4×2 certain on impact. Fireball/Chidori/resolve unchanged.
- **Cycles:** 1. No redesign. No review/verify FIX.
- **Sim:** T-033 probe all true. Legacy 14/AP1/burn~10 → 15/AP2/burn 8; 2T enhance 21. Live WR 100% all presets. **Catalog-faithful; base dmg/AP halved unless 2T ON (intended Main chip + Mode payoff).**
- **Harvest:** no skill deltas (clean R0, no scores / deviations / fix cycles).
- **Noted:** live `useSkill` / auto Uchiha Main still OOS. Predict / Stream still backlog.

