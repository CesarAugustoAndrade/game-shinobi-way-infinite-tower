# T-020 · R0 Uzumaki Barrage: clones ON +2 hits + 1 charge (red exception)

- **Type:** expand
- **Layer(s) touched:** domain (resolve multi-hit Mode exception + Barrage authoring fields)
- **Ring:** R0 (`src/game/systems/ResolveSkillSystem.ts`, optional `ModeInteraction` field; `skillsCombatV1New.ts` Barrage row). No React.
- **Lado / Side:** `back`
- **Bounded context:** combat-tactical-setup · **Subdomain:** core
- **Priority:** P1
- **Agent estimate:** S
- **Evaluation route:** `autonomous`
- **Depends on:** T-005 charges; T-007 `resolveMultiHit` / `resolveSkill`; T-008 Barrage authoring. Complements T-018 (Rasengan damage %, not hit count).

## Génesis

**Why this gap:** SOUL §11 Uzumaki: **Uzumaki Barrage** — “Multi-hit; **1 carga → +2 golpes** (excepción en rojo).” SOUL §12 table: `3×4`; with clones −1 charge +2 hits. Canon: [`docs/combat-tactical-setup-spec.html#nuevas`](../../docs/combat-tactical-setup-spec.html#nuevas), SOUL §8 charges at attempt, §9 multi-hit independent rolls, §11 Barrage exception.

**Code evidence:**
- Barrage authored `hitCount: 3`, `modeInteraction: { modeId: 'shadow_clone', consumeCharges: 1 }` (`skillsCombatV1New.ts` ~295–319) — **no** `bonusHits` field; description promises +2 hits.
- `resolveSkill` always uses `skill.hitCount` only (`ResolveSkillSystem.ts` ~346); **no** Mode-ON hitCount bump; charge spend only if `intent.modeCharges` set (~277–287).
- T-018 scopes Rasengan **+50% damage** and marks Barrage multi-hit exception as **not** that task’s AC.
- Not booked in T-013–T-019.

**Already covered (do not duplicate):**
- **T-008** — Barrage row + base 3 hits.
- **T-007** — multi-hit rolls.
- **T-018** — Rasengan damage payoff (parallel Mode-ON motor; different outcome).

**Discarded alternatives:**
- Folding into T-018 mid-flight — separate AC surface (hits vs damage %).
- Inventing +2 hits for other skills — only Barrage SOUL exception.
- Live `useSkill` cutover.
- Full Uchiha Chidori MEDIUM table.

**SOUL pillars served:** §3.4 Mode routes; §3.7 Uzumaki tunnel; §8 charge at attempt; §9 multi-hit; §12 Barrage contract.

## Context

Barrage is multi-hit SIDE with clone Mode interaction authored but resolve never increases hit count or auto-spends the charge for the red exception. This task wires pure resolve: when Shadow Clones is ON and 1 charge can be spent at attempt, Barrage rolls **5** hits (3+2); otherwise **3** hits and no charge spend.

## Spec

### Must exist when done

1. **Authoring**
   - Barrage declares the red exception explicitly, e.g. `modeInteraction.bonusHits: 2` (or project-equivalent on ModeInteraction) with existing `consumeCharges: 1` and `modeId: 'shadow_clone'`. Base `hitCount` remains **3**.
   - Description already states +2 hits; keep honest.

2. **resolveSkill behavior**
   - For `uzumaki_barrage` (or SIDE/ATTACK with authored `bonusHits` + Mode id):
     - If Mode **ON** and spend of `consumeCharges` succeeds at **attempt**: effective `hitCount = baseHitCount + bonusHits` (3+2=5); board charges reduced.
     - If Mode OFF or spend fails: `hitCount = base` (3); charges unchanged; no free bonus hits.
   - Each hit still independent via `resolveMultiHit`.
   - Do not apply Rasengan-style +50% damage unless also authored (Barrage AC is hit count only).

3. **Compose with charge auto-spend**
   - Prefer same auto-spend path T-018 introduces if present; if T-018 unmerged, implement spend+bonusHits for Barrage without requiring `intent.modeCharges` / without double-spend.

### Architectural constraints
- [x] R0 pure; reuse `resolveMultiHit` / `trySpendCharges`
- [x] Numbers only SOUL (+2 hits, 1 charge)
- [x] Immutability
- [x] Red exception must remain visible in authoring (not secret)
- [x] Do not implement Chidori/Fireball/Peacock tables

## Verification

- **Evaluation route:** `autonomous`

### Objective AC (blocking)

- [ ] **RING-GUARD:** R0 only; no React/components/scenes imports.
- [ ] **`npm run typecheck`** passes.
- [ ] **AC1 — Clones ON: 5 rolls + charge:** Fixture Shadow Clones ON charges ≥1; resolve Barrage with rollHit counting invocations; **5** rolls; charges decremented by 1 → gate: `npm test -- src/game/systems/__tests__/t020UzumakiBarrageHits` (`T-020 on plus hits`).
- [ ] **AC2 — Clones OFF: 3 rolls:** Empty/OFF Mode board; resolve Barrage → **3** rolls; no charge mutation → gate: same file `T-020 off base`.
- [ ] **AC3 — Authoring:** Barrage has `hitCount === 3` and declared bonusHits **2** (or equivalent field) with shadow_clone consume 1 → gate: same file `T-020 authoring`.

### Subjective criteria
None.

## Out of scope

- Rasengan +50% damage (T-018).
- Mode weight +4 (T-015); Discover; turn clock; Sealing Tag (T-019).
- Live `useSkill` cutover; Uchiha catalog lote.
- Morning Peacock consume-all / +15%/charge.
- Editing SOUL.

## Notes for implementer

- Effective hit count: `const hits = (skill.hitCount ?? 1) + (modeOn && spent ? (mi.bonusHits ?? 0) : 0)`.
- Spend before multi-hit so attempt timing matches SOUL; if spend fails, do not add bonusHits.
- Align Mode id with T-011 `shadow_clone` / `MODE_FAMILY.CLONES`.
- Coordinate with T-018 if both touch auto-spend — share helper, avoid double-spend.

## Close summary

- **Status:** done (`b97d97d` merged ff into `wt/jp-08121518`)
- **Shipped:** `ModeInteraction.bonusHits`; Barrage authors `bonusHits: 2`; resolveSkill after T-018 spend success uses `hitCount = base + bonusHits` (3+2=5). OFF / spend-fail stays 3. No Rasengan damage %.
- **Cycles:** 1. No redesign. No review/verify FIX.
- **Sim:** T-020 probe all true. Live WR 100% (Hyuga 99.8%). **Healthy contract; no live-balance shift.**
- **Harvest:** no skill deltas (clean R0, no scores / deviations / fix cycles).
- **Noted:** live `useSkill` still does not consume this resolve path (explicitly out of scope).
