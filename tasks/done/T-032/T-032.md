# T-032 · R0 Great Fireball: Sharingan 2T +50% + 1 charge (Uchiha Attack)

- **Type:** expand
- **Layer(s) touched:** domain (`fireball` authoring + pure `resolveSkill` Mode enhance already used by Rasengan)
- **Ring:** R0 (`src/game/constants/skills.ts` `FIREBALL`; no new Mode machine — reuse `modeInteraction.damageMultBonus` + `consumeCharges` in `ResolveSkillSystem.ts`). No React.
- **Lado / Side:** `back`
- **Bounded context:** combat-tactical-setup · **Subdomain:** core
- **Priority:** P1
- **Agent estimate:** S
- **Evaluation route:** `autonomous`
- **Depends on:** T-005 Mode board / `trySpendCharges`; T-007 `resolveSkill`; T-011/T-018 pattern (`damageMultBonus` + `consumeCharges`); T-025 Chidori is 3T sibling. Complements Uchiha tunnel after Chidori. **Not blocked** by T-030/T-031.

## Génesis

**Why this gap:** SOUL §11 Uchiha T2–4: **Fireball / Chidori** “Enhanced + consumo.” Canon catalog [`#catalogo`](../../docs/combat-tactical-setup-spec.html#catalogo) **fireball**: “**17 dmg** + Burn 5×2. **Sharingan 2T: +50% y consume 1 carga**.” Chidori (T-025) owns 3T MEDIUM + 2 charges; Fireball’s **2T enhance** is still legacy packaging with **no** `cardRole` / `modeInteraction`.

**Code evidence:**
- `FIREBALL` (`skills.ts` ~1306–1341): ACTIVE only; AP **2** (catalog **3**); base **15** (catalog **17**); BURN 15×3 @80% (catalog Burn **5×2**); **no** `cardRole`, **no** `modeInteraction`, **no** `allowedRanges` for MEDIUM·LONG honesty.
- Rasengan already proves resolve auto-enhance: `damageMultBonus: 0.5` + `consumeCharges: 1` when Mode ON (`skills.ts` ~1239–1244; `ResolveSkillSystem.ts` auto Mode path; T-018 tests).
- Explicitly deferred as lote: T-018/T-019/T-020/T-025/T-030/T-031 OOS “Fireball / Phoenix / Predict.”

**Already covered (do not duplicate):**
- **T-018** — clones +50% + 1 charge pattern (clone Mode, not Sharingan).
- **T-025** — Chidori Sharingan **3** + 2 charges + MEDIUM grant.
- **T-011** — Sharingan 2 Mode skill packaging.
- **T-015** — Mode weightModifiers (not Fireball damage).

**Discarded alternatives:**
- Full Uchiha lote (Fireball + Phoenix + Predict + Stream) — L; SOUL prefers S/M slices that combine with implemented surface.
- `requireOn: true` for Fireball — catalog base works **without** Mode; enhance only when 2T ON (like Rasengan, unlike Peacock).
- Inventing Fireball consume of Sharingan **3** — catalog says **2T**.
- Phoenix Flower 3×5 +40% — separate sibling.

**SOUL pillars served:** §3.1 ATTACK cobra; §3.4 Mode routes; §3.7 Uchiha tunnel; §8 charges at attempt; §11 Fireball beat; catalog `#catalogo` fireball; anti-visión no secret % without Mode declaration.

## Context

Sharingan 2-Tomoe Mode and pure resolve Mode enhance exist. Great Fireball is a core Uchiha Attack that should read 2T for +50% and one charge at attempt while remaining playable as a base fire nuke — currently a legacy row without tactical role or Mode contract.

## Spec

### Must exist when done

1. **Authoring (`fireball`)**
   - `cardRole: CardRole.ATTACK`.
   - Costs: **AP 3**, **CP 6**, **CD 3**, HP 0 (catalog).
   - `baseDamage: 17` (catalog); `hitCount` default 1.
   - `allowedRanges: [CombatRange.MEDIUM, CombatRange.LONG]` (catalog MEDIUM · LONG).
   - `modeInteraction`:
     - `modeId: 'sharingan_2'`
     - `family: MODE_FAMILY.SHARINGAN` (or `'SHARINGAN'`)
     - `consumeCharges: 1`
     - `damageMultBonus: 0.5`
     - **No** `requireOn` (base legal when Mode OFF).
   - Burn honesty: reauthor `effects` (or mark/DoT path if project convention) to **Burn 5 for 2** opportunities (catalog “Burn 5×2”); drop legacy 15×3 @80% as primary identity — chance may be **1.0** if catalog omits miss chance, or keep explicit chance only if HTML implies; default **certain on hit** unless catalog states otherwise.
   - Description: base Fireball; with Sharingan 2-Tomoe ON +50% and 1 charge at attempt.

2. **`resolveSkill` behavior** (reuse existing; no special-case skill id if possible)
   - Mode OFF: resolve deals baseline (17 under deterministic full-hit port); **no** charge spend; board unchanged.
   - Sharingan 2 ON with ≥1 charge: damage total uses **+50%** (`damageMultBonus: 0.5`, same floor style as T-018); spend **1** charge at attempt; charges decrease by 1 (Mode stays ON if charges remain).
   - Sharingan **3** ON alone does **not** auto-apply this row’s enhance unless authoring also lists it — **v1 AC only requires 2T** per catalog. (Do not invent dual-stage Fireball.)
   - Invalid AP/range still reject with consume-nothing rules already in resolve.

3. **Compose**
   - Prefer zero new ModeInteraction fields; mirror Rasengan.
   - Do not change Chidori 3T packaging (T-025).

### Architectural constraints
- [x] R0 pure; immutability
- [x] Numbers only catalog/SOUL: 17 dmg, +50%, 1 charge, 2T, AP3/CD3/CP6
- [x] Base viable without Mode
- [x] No React / no live `useSkill` AC
- [x] No Phoenix / Predict / Stream in this task’s AC

## Verification

- **Evaluation route:** `autonomous`

### Objective AC (blocking)

- [x] **RING-GUARD:** R0 only; no React/components/scenes/hooks/contexts imports in touched files.
- [x] **`npm run typecheck`** passes.
- [x] **AC1 — Authoring:** `fireball` has `cardRole: ATTACK`, `apCost: 3`, `chakraCost: 6`, `cooldown: 3`, `baseDamage: 17`, `allowedRanges` includes MEDIUM and LONG, `modeInteraction` with `modeId: 'sharingan_2'`, `consumeCharges: 1`, `damageMultBonus: 0.5`, and `requireOn` absent/false → gate: `npm test -- src/game/systems/__tests__/t032Fireball` (`T-032 authoring`).
- [x] **AC2 — Base without Mode:** empty Mode board; resolve Fireball with deterministic hit → `ok: true`, `damageDealt` equals baseline (17 under test hit port), modes still empty → gate: same file `T-032 base`.
- [x] **AC3 — 2T enhance + charge:** Sharingan 2 ON with **3** charges; resolve Fireball hit → damage = `floor(baseline * 1.5)` (T-018 convention); Sharingan charges **2**; Mode still ON → gate: same file `T-032 sharingan enhance`.

### Subjective criteria
None.

## Out of scope

- Phoenix Flower (3×5, +40% 2T); Sharingan Predict (Read Window + recharge); Chidori Stream (+40% 3T).
- Full Burn DoT tick simulation if pure resolve does not already apply `effects[]` — authoring fields required; live DoT tick parity only if already on resolve path without new systems.
- Primary/Hidden Lotus (T-030/T-031); Dynamic Entry (T-029); 64 Palms; Yamanaka tunnel.
- Live `useSkill` / BattleSimulator cutover; R2 badges.
- Editing SOUL.

## Notes for implementer

- Canon: SOUL §11 Uchiha; HTML `#catalogo` row `fireball`; `#modes` Sharingan 2-Tomoe.
- Mirror T-018 tests; use `sharingan_2` instance ON like T-025 fixtures.
- Burn field reauthor is packaging honesty; do not invent a second burn pipeline if `effects` already flow elsewhere.

## Close summary

- **Status:** done (`15208ef` merged ff into `wt/jp-08121518`)
- **Shipped:** Great Fireball ATTACK 17 at MEDIUM/LONG. Base legal without Mode. Sharingan 2-Tomoe ON: +50% and spend 1 charge. Burn 5×2 certain on hit. Rasengan/Chidori/resolve unchanged.
- **Cycles:** 1. No redesign. No review/verify FIX.
- **Sim:** T-032 probe all true. Legacy 15/AP2/burn~36 → 17/AP3/burn 10; 2T enhance 25. Live WR 100% all presets. **Catalog-faithful; base slightly undertuned vs legacy efficiency unless 2T ON (intended).**
- **Harvest:** no skill deltas (clean R0, no scores / deviations / fix cycles).
- **Noted:** live `useSkill` / R2 badges still OOS. Phoenix / Predict / Stream still backlog.
