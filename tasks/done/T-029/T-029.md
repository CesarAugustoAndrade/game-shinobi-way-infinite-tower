# T-029 · R0 Dynamic Entry: MEDIUM→CLOSE rush + Gate charges intact

- **Type:** expand
- **Layer(s) touched:** domain (`dynamic_entry` authoring + pure `resolveSkill` skill-declared self-approach)
- **Ring:** R0 (`src/game/constants/skills.ts` `dynamic_entry`; `ResolveSkillSystem.ts` band-move path shared with T-023 if present). No React.
- **Lado / Side:** `back`
- **Bounded context:** combat-tactical-setup · **Subdomain:** core
- **Priority:** P1
- **Agent estimate:** S
- **Evaluation route:** `autonomous`
- **Depends on:** T-006 (`resolveForcedMove` / range bands); T-007 `resolveSkill`. Complements T-017 Gate Prep, T-021 Peacock, T-027 ascent — does **not** spend Gate charges. **Not blocked** by T-019–T-028.

## Génesis

**Why this gap:** SOUL §11 Lee beat **Dynamic Entry**: “**MEDIUM→CLOSE**; **cargas intactas**.” Positions for Lotus/Peacock without paying Mode charges or voluntary move. Canon: [`docs/combat-tactical-setup-spec.html#tablero`](../../docs/combat-tactical-setup-spec.html#tablero), [`#modes`](../../docs/combat-tactical-setup-spec.html#modes), SOUL §10 forced/card movement does not spend manual move, §11 Lee T2–3.

**Code evidence:**
- `dynamic_entry` (`skills.ts` ~295–316): legacy ACTIVE, **no** `cardRole`, **no** `allowedRanges` override → `AttackMethod.MELEE` defaults to **CLOSE only** (`RangeSystem.ts` ~39–42), so MEDIUM cast is illegal.
- Description is “guaranteed first strike / crit” — not SOUL rush-close.
- `resolveSkill` movement only if `intent.movement` set (~495+); no skill-id band close for Dynamic Entry.
- No backlog title owns Dynamic Entry (T-021 OOS “Primary Lotus”; T-023 owns three universal tools only; T-026 Air Palm PUSH foe).

**Already covered (do not duplicate):**
- **T-006** — forced move boundaries / voluntary flag.
- **T-007** — resolve order (movement after hits).
- **T-017** — Gate Prep HP discount (not rush).
- **T-021** — Peacock consume/close Gate.
- **T-023** — Wire/Blastback/Backstep bandMove pattern (reuse helper if merged).

**Discarded alternatives:**
- Inventing “guaranteed crit / first strike” as SOUL norm — not in §11 Dynamic Entry line; do not re-center task on crit.
- Spending Gate charges on entry — SOUL says **intactas**.
- LONG→CLOSE multi-step free teleport — only SOUL **MEDIUM→CLOSE** (one band closer to CLOSE when cast from MEDIUM; from CLOSE stay CLOSE with damage).
- Primary Lotus charge scale — separate (T-021 sibling).

**SOUL pillars served:** §3.1 SIDE/ATTACK verb; §3.6 AP/posture/range; §3.7 Lee tunnel; §10 card movement; §11 Dynamic Entry.

## Context

Lee Mode motors (Gates, Gate Prep, Peacock) assume the player can reach CLOSE without burning charges. Dynamic Entry is the SOUL rush that is legal at MEDIUM, deals chip/kick damage, forces engagement to CLOSE, and never touches Mode charges. This task reauthors the row and wires pure resolve so MEDIUM plays close the band without voluntary-move spend.

## Spec

### Must exist when done

1. **Authoring (`dynamic_entry`)**
   - `cardRole: CardRole.SIDE_ATTACK` (chip/rush arming CLOSE; not Mode/Support). If project convention prefers ATTACK for named kick with solid damage, SIDE_ATTACK is preferred SOUL “Side afina / positions”; either is OK if documented — **default SIDE_ATTACK**.
   - `allowedRanges: [CombatRange.MEDIUM, CombatRange.CLOSE]` (must include MEDIUM).
   - Honest description: flying kick; cast from MEDIUM or CLOSE; **closes to CLOSE** after play; **does not spend Gate charges**.
   - Optional `bandMove: { kind: 'SELF_APPROACH' | 'PULL', steps: 1 }` or id hard-map only for `dynamic_entry` — engagement becomes one band closer (MEDIUM→CLOSE). Same pure helper as T-023 if available.
   - Keep existing rough AP/damage/CD unless they block MEDIUM legality; do not invent retune beyond range + role + move contract.

2. **`resolveSkill` behavior**
   - Validate range via authored `allowedRanges` (MEDIUM legal).
   - Resolve damage via existing SIDE/ATTACK multi-hit path (hitCount default 1).
   - After hits (card once): if current range is **not** CLOSE, apply forced approach so result range is **one step toward CLOSE** (MEDIUM→CLOSE). From CLOSE: range stays CLOSE.
   - **Must not** set `playerMoveUsedThisTurn`.
   - **Never** call `trySpendCharges` / mutate Mode board for this skill — even if `gate_of_life` or `gate_of_limit` is ON with charges > 0.

3. **Compose**
   - Prefer shared skill-declared band-move path from T-023; if unmerged, hard-map `dynamic_entry` only.
   - LONG is **not** in allowedRanges for this skill (no free LONG→MEDIUM→CLOSE chain this task).

### Architectural constraints
- [x] R0 pure; immutability
- [x] No Gate charge spend
- [x] Forced/card move, not voluntary AP move
- [x] No invented crit-guaranteed rules
- [x] No React / no live `useSkill` required for AC

## Verification

- **Evaluation route:** `autonomous`

### Objective AC (blocking)

- [x] **RING-GUARD:** R0 only; no React/components/scenes/hooks/contexts imports.
- [x] **`npm run typecheck`** passes.
- [x] **AC1 — Authoring MEDIUM + role:** `dynamic_entry` has hand-playable `cardRole` (SIDE_ATTACK or ATTACK) and `allowedRanges` includes `MEDIUM` and `CLOSE` → gate: `npm test -- src/game/systems/__tests__/t029DynamicEntry` (`T-029 authoring`).
- [x] **AC2 — MEDIUM→CLOSE, no charge spend:** Fixture range MEDIUM; Gate Mode ON charges 3; resolve Dynamic Entry successfully → result `range === CLOSE`; Mode charges still **3**; `playerMoveUsedThisTurn` not true → gate: same file `T-029 rush no spend`.
- [x] **AC3 — CLOSE cast stays CLOSE:** Range already CLOSE; resolve ok; range remains CLOSE; still no charge spend → gate: same file `T-029 close stay`.

### Subjective criteria
None.

## Out of scope

- Primary Lotus / Hidden Lotus Gate payoffs (charge scale + close Mode).
- Morning Peacock (T-021); Gate Prep (T-017); Mode ascent (T-027).
- Wire/Blastback/Backstep content (T-023) except shared move helper reuse.
- Live `useSkill` cutover; R2.
- Editing SOUL.

## Notes for implementer

- Approach = engagement PULL-equivalent (foe closer): reuse `resolveForcedMove(..., 'PULL')` if that maps MEDIUM→CLOSE; if not, set range to CLOSE when steps would reach it.
- Do not add LONG to allowedRanges in this slice.
- Crit bonus may remain as legacy packaging; AC must not require “guaranteed first strike” behavior.

## Close summary

- **Status:** done (`dba7bd3` merged ff into `wt/jp-08121518`)
- **Shipped:** Dynamic Entry SIDE_ATTACK legal at MEDIUM/CLOSE. `SELF_APPROACH` maps to PULL so MEDIUM→CLOSE; CLOSE stays CLOSE. No Gate spend (no `modeId`). Voluntary move unused.
- **Cycles:** 1. No redesign. No review/verify FIX.
- **Sim:** T-029 probe all true. Live WR 100% all presets. **Healthy contract; no live-balance shift.**
- **Harvest:** no skill deltas (clean R0, no scores / deviations / fix cycles).
- **Noted:** live `useSkill` still not on this path (explicitly out of scope).

