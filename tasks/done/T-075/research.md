# T-075 research

1. **Distinct `exposed_10` (recommended).** Catalog 10% ≠ T-023 15%. Plant on SIDE hit after misdirect consume. ATTACK consumes `exposed_10`; ×1.10 if RANGED.

2. **Reuse `exposed` with stacks=10.** Would fight T-023 ×1.15 id check.

3. **Plant Exposed on attempt (even miss).** Spec prefers on-hit; document miss consume without plant.

**Recommendation:** option 1 + on-hit plant.
