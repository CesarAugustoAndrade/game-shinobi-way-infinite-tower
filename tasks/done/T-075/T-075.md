# T-075 · R0 Henge: SUPPORT Misdirect 2 (SIDE weight +2 next draw; next SIDE applies Exposed 10%)

- **Type:** expand
- **Layer(s) touched:** domain (`henge` authoring + pure SUPPORT plant + SIDE consume → Exposed 10% setup)
- **Ring:** R0 (`src/game/constants/skills.ts` `HENGE`; `ResolveSkillSystem.ts` SUPPORT/SIDE marks; `SupportWeightSystem` / `nextDrawRoleBonus`). No React.
- **Lado / Side:** `back`
- **Bounded context:** combat-tactical-setup · **Subdomain:** core
- **Priority:** P1
- **Agent estimate:** S
- **Evaluation route:** `autonomous`
- **Depends on:** T-007 resolve; T-038/T-073 `nextDrawRoleBonus` SIDE bag; T-023 `exposed` setup mult path. Complements academy SUPPORT after Bunshin Decoy. **Not blocked** by T-074.

## Génesis

**Why this gap:** SOUL §3.1 SUPPORT prepara + §3.2 transparent weights + §3.5 Marks / Setup + anti-visión secret % + §3.8 honesty. Canon catalog [`#catalogo`](../../docs/combat-tactical-setup-spec.html#catalogo) **henge**: SUPPORT · **1 AP · 1 CP · CD 4** · ANY · “**Misdirect 2**: **SIDE weight +2** en el próximo draw y el próximo **SIDE** aplica **Exposed 10%**.” Runtime is ACTIVE-only DEX **+25%** duration-2 buff — wrong axis, anti-visión %, **no** `cardRole`, **no** misdirect mark, **no** next-draw SIDE bias, **no** Exposed arming.

**Code evidence:**
- `HENGE` (`skills.ts` ~230–249): AP1/CP1/CD4; base 0; MENTAL; `effects` DEX 0.25×2; **no** `cardRole`; **no** `markEffects`; **no** `nextDrawRoleBonus`.
- Weight bag already supports role deltas (`nextDrawRoleBonus` on `smoke_bomb` SIDE +1; `SupportWeightSystem`).
- T-023 `exposed` → ×1.15 on RANGED ATTACK (`ResolveSkillSystem` `markDamageMultiplier` ~390). Catalog wants **10%** from Misdirect’s Exposed plant — distinct magnitude (×1.10).
- T-074 Génesis **discarded** “henge Misdirect SIDE +2 + Exposed on next SIDE” as the academy setup follow-up after Bunshin.

**Already covered (do not duplicate):**
- **T-038 / T-073** — Smoke/Mist SIDE +1 + pressure marks (not Misdirect + Exposed).
- **T-023** — Blastback plants full Exposed +15% (v1 tool; not henge).
- **T-074** — Bunshin Decoy −20 (no weight, no Exposed).

**Discarded alternatives:**
- kawarimi Substitution first-hit ignore + band — larger reaction M.
- sand_shield Shield 45 — pure absorb (Mud Wall sibling).
- demon_slash 15 + Bleed 6×3 — ATTACK bleed floor.
- air_bullet 13 PUSH + −1 ACC — wind SIDE.
- Keeping DEX % as “transformation” — anti-visión vs catalog Misdirect contract.

**SOUL pillars served:** §3.1 SUPPORT; §3.2 weighted draw; §3.5 Marks/Setup; §3.8 honesty; catalog `#catalogo` henge; anti-visión secret %.

## Context

Bunshin only cuts the first enemy swing. Henge should **bias the next hand toward SIDE** and arm **Misdirect**: the next **SIDE** you play applies **Exposed 10%** Setup (next RANGED ATTACK ×1.10), then Misdirect is spent.

## Spec

### Must exist when done

1. **Authoring (`henge`)**
   - `cardRole: CardRole.SUPPORT`.
   - Costs: **AP 1**, **CP 1**, **CD 4**, HP 0.
   - `baseDamage: 0`; ANY range; tags NINJUTSU / MENTAL / SETUP as available.
   - `nextDrawRoleBonus: { role: CardRole.SIDE_ATTACK, delta: 2 }`.
   - `markEffects`: self mark id **`misdirect`**, duration **2**, `consume: ATTEMPT`, applies when consuming skill is **`cardRole === SIDE_ATTACK`** (not ATTACK/SUPPORT/MODE).
   - Remove primary DEX +0.25×2 effect identity.
   - Honest description: Misdirect 2; SIDE weight +2 next draw; next SIDE applies Exposed 10%.

2. **`resolveSkill` SUPPORT (henge)**
   - Pay costs; plant `misdirect`; enqueue SIDE +2 next-draw role bonus; `damageDealt === 0`.

3. **Next SIDE → Exposed 10%**
   - When resolving a **SIDE** with `misdirect` among ATTEMPT spent: consume misdirect even on full miss.
   - On that SIDE with **≥1 hit** (prefer on-hit plant; document if attempt-always): plant setup mark for **+10%** next **RANGED ATTACK** Setup.
     - Prefer reusing `exposed` only if mult can be **×1.10** for this plant (e.g. stacks metadata); otherwise plant id **`exposed_10`** / `misdirect_exposed` and extend `markDamageMultiplier` to ×1.10 for RANGED ATTACK.
   - Do **not** use full T-023 ×1.15 unless stacks/id distinguish 10% (catalog is **10%**, not 15%).
   - SIDE without misdirect unchanged; ATTACK does not consume misdirect.

### Architectural constraints
- [x] R0 pure; immutability
- [x] Catalog: AP1 · CP1 · CD4 · SIDE +2 next draw · next SIDE → Exposed 10%
- [x] Reuse weight bag + mark ATTEMPT consume; no permanent baseWeight mutation
- [x] No React; no bunshin / smoke / blastback retune

## Verification

- **Evaluation route:** `autonomous`

### Objective AC (blocking)

- [x] **RING-GUARD:** R0 only; no React/components/scenes/hooks/contexts imports in touched files.
- [x] **`npm run typecheck`** passes.
- [x] **AC1 — Authoring:** `henge` has `cardRole: SUPPORT`, AP1/CP1/CD4, `baseDamage: 0`, `nextDrawRoleBonus` SIDE +2, self `misdirect` duration 2 SIDE-only ATTEMPT; no primary DEX 0.25 identity → gate: `npm test -- src/game/systems/__tests__/t075Henge` (`T-075 authoring`).
- [x] **AC2 — Weight bag:** resolve henge → SIDE_ATTACK effectiveWeight +2 once; ATTACK weight unchanged by this bonus → gate: same file `T-075 weight`.
- [x] **AC3 — SIDE applies Exposed 10%:** with misdirect, resolve SIDE hit → misdirect gone; Exposed-10% setup mark present; subsequent RANGED ATTACK base 10 → damage **11** (×1.10 floor); full SIDE miss still consumes misdirect and does not plant Exposed-10% if on-hit rule chosen (document) → gate: same file `T-075 exposed`.

### Subjective criteria
None.

## Out of scope

- kawarimi Substitution; sand_shield; demon_slash; air_bullet; counter_stance.
- Broadening Exposed 10% to MELEE; changing T-023 ×1.15 default for Blastback plants.
- Live UI; editing SOUL.

## Notes for implementer

- Canon: HTML `#catalogo` `henge`; SOUL §6 posture/weight honesty + §9 attempt consume.
- Smoke uses SIDE +1; Henge is **+2** — do not collapse deltas.
- Prefer distinct mark id for 10% Setup so Blastback’s 15% Exposed stays intact.

## Close

- Cycles: 1 (explore → plan auto-approve → implement → review APPROVE → verify PASS → merge).
- Summary: `henge` is SUPPORT AP1/CP1/CD4. Plants Misdirect 2. SIDE +2 next draw. Next SIDE hit plants `exposed_10` (×1.10 RANGED ATTACK). Miss consumes misdirect without plant. DEX +25% removed.
- Improvements noted: none.

