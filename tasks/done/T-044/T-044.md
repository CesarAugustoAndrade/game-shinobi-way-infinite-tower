# T-044 · R0 Dancing Leaf: SUPPORT force CLOSE + Lotus Opening (+25% GATES finisher, LOTUS weight +2)

- **Type:** expand
- **Layer(s) touched:** domain (`dancing_leaf` authoring + pure SUPPORT resolve reposition + finisher mark + next-draw LOTUS weight)
- **Ring:** R0 (`src/game/constants/skills.ts` `DANCING_LEAF`; `ResolveSkillSystem.ts` SUPPORT; `SupportWeightSystem.ts` / pending bag for LOTUS +2). No React.
- **Lado / Side:** `back`
- **Bounded context:** combat-tactical-setup · **Subdomain:** core
- **Priority:** P1
- **Agent estimate:** S–M
- **Evaluation route:** `autonomous`
- **Depends on:** T-006/T-029 band move; T-007 resolve; T-016 support weight bag; T-021/T-030/T-031 GATES finishers. Complements Lee §11. **Not blocked** by T-042/T-043.

## Génesis

**Why this gap:** SOUL §11 Lee prep into Lotus/Peacock: position + arm finisher. Canon catalog [`#catalogo`](../../docs/combat-tactical-setup-spec.html#catalogo) **dancing_leaf**: SUPPORT · **1 AP · 1 CP · CD 3** · “**Mover a CLOSE**; **Lotus Opening 2** da **+25%** al próximo finisher **GATES** y **+2 weight a LOTUS** en el siguiente draw.” SOUL §10 card movement does not spend manual move; §9 setup is legible marks, not secret STR/DEX %.

**Code evidence:**
- `DANCING_LEAF` (`skills.ts` ~459–481): ACTIVE-only dual **STR +0.5 / DEX +0.3 ×1** buffs — anti-visión %; **no** `cardRole`; **no** force-CLOSE; **no** Lotus Opening mark; **no** LOTUS weight enqueue.
- GATES finishers already reauthored: `primary_lotus` / `hidden_lotus` / `morning_peacock` with `requireFamily: GATES` + `consumeAllCharges` / charge scale — but nothing arms +25% Setup/finisher mult from Dancing Leaf.
- T-016 bag supports `skillId` deltas (Gate Prep +3); no `dancing_leaf` → `primary_lotus`/`hidden_lotus` +2 path.
- T-043 OOS/discard: “Dancing Leaf Lotus Opening — Lee-specific; do after Shunshin.”

**Already covered (do not duplicate):**
- **T-029** — Dynamic Entry MEDIUM→CLOSE rush (SIDE damage), charges intact.
- **T-017 / T-040** — Gate Prep HP discount + Mode weight +3.
- **T-021 / T-030 / T-031** — Peacock / Primary / Hidden Lotus finisher contracts.
- **T-016** — generic next-draw skillId bag motor.

**Discarded alternatives:**
- Gentle Fist Byakugan+CP — Hyuga Main; different tunnel.
- Kai cleanse / Analyze Discover — universal SUPPORT; lower Lee-tunnel urgency after T-043 handoff.
- Keeping STR/DEX % as “positioning” — anti-visión §4.
- Giving +2 weight to Peacock as “LOTUS” — catalog says **LOTUS**; target **primary_lotus** + **hidden_lotus** only unless HTML expands.

**SOUL pillars served:** §3.1 SUPPORT prepara; §3.2 transparent weights; §3.5 Marks/setup; §3.6 rango; §3.7 Lee tunnel; catalog `#catalogo` dancing_leaf; `#tablero` card move.

## Context

Lee Gate modes and finishers resolve under pure `resolveSkill`. Dancing Leaf should be the cheap SUPPORT that **snaps the user to CLOSE** without burning manual move, plants **Lotus Opening** for the next GATES finisher (+25%), and biases the **next hand** toward Lotus cards (+2 weight each) — still a legacy dual % buff with no role.

## Spec

### Must exist when done

1. **Authoring (`dancing_leaf`)**
   - `cardRole: CardRole.SUPPORT`.
   - Costs: **AP 1**, **CP 1**, **CD 3**, HP 0 (catalog).
   - `baseDamage: 0`; ranges ANY.
   - Movement: after play, player range becomes **CLOSE** (from LONG or MEDIUM in one resolve; if already CLOSE, stay CLOSE). Does **not** set `playerMoveUsedThisTurn`. Prefer multi-step SELF_APPROACH until CLOSE **or** direct set — lock one in tests; both must leave voluntary move unused.
   - **Lotus Opening**: self mark id **`lotus_opening`**, duration **2**, `consume: ATTEMPT` (or impact if finishers always hit — prefer **ATTEMPT** so miss still spends), metadata **+25%** next GATES finisher.
   - Next-draw: enqueue **+2** weight for **`primary_lotus`** and **`hidden_lotus`** (skillId bag entries; one-shot bag clear on next draw like T-016).
   - Remove primary STR/DEX % buff identity.
   - Honest description: move to CLOSE; Lotus Opening 2 → +25% next GATES finisher; LOTUS weight +2 next draw.

2. **`resolveSkill` SUPPORT**
   - Pay costs; `damageDealt === 0`.
   - Range → CLOSE; voluntary move flag unchanged.
   - Plant `lotus_opening` on self duration 2.
   - Enqueue LOTUS weight +2 for primary + hidden lotus ids via existing pending bag (extend `applySupportWeightOnPlay` for this skill id or author explicit weight entries).
   - No Mode activation/charges; no Terrain.

3. **Lotus Opening payoff**
   - When a later ATTACK with GATES finisher contract resolves (`modeInteraction.requireFamily === GATES` and/or `consumeAllCharges` on a GATES Mode — include **Primary Lotus**, **Hidden Lotus**, **Morning Peacock**), if `lotus_opening` is spent at attempt: apply **+25%** damage mult (Setup/finisher bucket; fixture with other mults zero → `floor(baseTotal * 1.25)` locked in test).
   - Non-GATES ATTACK/SIDE do **not** consume or benefit from `lotus_opening`.
   - Duration 2: if not consumed, survives one duration tick per reloj rules already owned by T-002/T-004 — AC only needs attempt consume path; duration tick may reuse existing mark clock.

### Architectural constraints
- [x] R0 pure; immutability
- [x] Catalog numbers only: AP1 · CP1 · CD3 · CLOSE · +25% · LOTUS +2
- [x] No secret STR/DEX % identity
- [x] No React / no live useSkill AC
- [x] No retune of Lotus/Peacock base packages beyond reading the new mark

## Verification

- **Evaluation route:** `autonomous`

### Objective AC (blocking)

- [ ] **RING-GUARD:** R0 only; no React/components/scenes/hooks/contexts imports in touched files.
- [ ] **`npm run typecheck`** passes.
- [ ] **AC1 — Authoring:** `dancing_leaf` has `cardRole: SUPPORT`, AP1/CP1/CD3, `baseDamage: 0`, CLOSE move contract, `lotus_opening` duration 2, LOTUS weight +2 contract; no primary STR+0.5/DEX+0.3 identity → gate: `npm test -- src/game/systems/__tests__/t044DancingLeaf` (`T-044 authoring`).
- [ ] **AC2 — CLOSE + mark + weight bag:** resolve from MEDIUM or LONG → `range === CLOSE`, `playerMoveUsedThisTurn` unchanged if false, self `lotus_opening` duration 2 present, pending bag includes +2 for `primary_lotus` and `hidden_lotus` → gate: same file `T-044 setup`.
- [ ] **AC3 — Finisher +25% consume:** with Gate Mode ON fixture and `lotus_opening` present, resolve Primary Lotus (or Peacock) under test ports → damage reflects **×1.25** vs same resolve without mark; `lotus_opening` gone after attempt → gate: same file `T-044 finisher`.

### Subjective criteria
None.

## Out of scope

- Shunshin DEX (T-043); Gentle Fist; Kai; Analyze; Kunai Bleed.
- Changing Primary/Hidden Lotus base dice or charge math.
- Live UI; Gate Prep retune; editing SOUL.

## Notes for implementer

- Canon: SOUL §11 Lee; HTML `#catalogo` `dancing_leaf`; `#tablero` movement; finishers T-021/T-030/T-031.
- Prefer mark id `lotus_opening`.
- LOTUS weight targets: `primary_lotus`, `hidden_lotus` only (+2 each).
- Peacock gets Opening damage +25% but not the LOTUS weight line unless catalog equates it — do not invent Peacock weight.

## Close

- Reauthored `dancing_leaf` as SUPPORT AP1/CP1/CD3. SELF_APPROACH×2 snaps to CLOSE. Self `lotus_opening` 2 ATTEMPT. LOTUS +2 via `nextDrawSkillBonuses`.
- GATES finishers consume Opening and `floor(dmg * 1.25)`. Non-GATES do not consume.
- Cycles: 0. Review APPROVE. Verify 643 PASS.
- Note: live BattleSimulator / useCombat do not apply `lotus_opening` (OOS).
