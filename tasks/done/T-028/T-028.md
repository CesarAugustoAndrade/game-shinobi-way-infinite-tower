# T-028 · R0 Rotation (kaiten): require Byakugan + 1 charge → self shield + reflect

- **Type:** expand
- **Layer(s) touched:** domain (`kaiten` authoring + pure `resolveSkill` SUPPORT/utility Mode-gated self marks + charge spend)
- **Ring:** R0 (`src/game/constants/skills.ts` `kaiten` / `ROTATION`; `ResolveSkillSystem.ts`; optional thin MarkSpec self-target). No React.
- **Lado / Side:** `back`
- **Bounded context:** combat-tactical-setup · **Subdomain:** core
- **Priority:** P1
- **Agent estimate:** S–M
- **Evaluation route:** `autonomous`
- **Depends on:** T-004 Marks (`addMark`, SHIELD family); T-005 `trySpendCharges` / Mode ON; T-007 `resolveSkill` SUPPORT path; T-011 Byakugan Mode packaging. Complements T-022 Twin Lion (no charge spend) and T-026 Air Palm (applies CP). **Not blocked** by T-019–T-027.

## Génesis

**Why this gap:** SOUL §11 Hyuga beat **Rotation**: “**Require Byakugan**; **1 carga** → **shield + reflect**.” Twin Lion (T-022) explicitly does **not** spend Byakugan charges; Rotation is the charge-spender defensive beat in the same tunnel. Canon: [`docs/combat-tactical-setup-spec.html#modes`](../../docs/combat-tactical-setup-spec.html#modes), [`#setup`](../../docs/combat-tactical-setup-spec.html#setup), SOUL §8 charges at attempt, §9 Shield family, §11 Hyuga T2–3.

**Code evidence:**
- `kaiten` (`skills.ts` ~1327–1352): legacy ACTIVE, **no** `cardRole`, **no** `modeInteraction`, **no** `markEffects`; `effects[]` SHIELD 50 + REFLECTION 0.6 only — pure `resolveSkill` SUPPORT path never applies `effects[]` reflection/shield as Tactical Setup marks.
- `applySupportMarks` (`ResolveSkillSystem.ts` ~219–234) always targets **ENEMY** — self shield cannot land even if markEffects were authored.
- Charge spend only via `modeInteraction.consumeCharges` / intent on ATTACK/SIDE auto path (~364–399); SUPPORT does not spend Byakugan charges.
- T-022 / T-026 Out of scope both name **Rotation** as a separate slice.

**Already covered (do not duplicate):**
- **T-004** — mark board / SHIELD family stacking.
- **T-005** — charge spend / Mode ON board.
- **T-007** — SUPPORT resolve skeleton + attempt charge port.
- **T-011** — Byakugan Mode skill packaging.
- **T-022** — Twin Lion enhance (no Rotation charge).
- **T-026** — Air Palm CP apply (no kaiten).

**Discarded alternatives:**
- Inventing new shield HP / reflect % not in SOUL table or existing row — **forbidden**; keep **existing** kaiten packaging numbers (shield **50**, reflection **0.6**, duration **1**) as current-fact honesty, or plant marks whose stacks/ids mirror those values for pure-state observability.
- 64 Palms full reauthor — separate ATTACK lote.
- Soft-play Rotation without Byakugan (Peacock-style hard require) — SOUL says **Require** Byakugan; hard reject if OFF.
- Spending >1 charge or closing Mode — SOUL is **1 carga** only.

**SOUL pillars served:** §3.1 SUPPORT prepare; §3.4 Mode charges; §3.5 Marks setup; §3.7 Hyuga tunnel; §8 charge at attempt; §9 Shield.

## Context

Byakugan Mode and Hyuga payoff tasks assume defensive charge use exists. Rotation is the SOUL-named skill that spends one Byakugan charge for a short self shield + reflect. This task reauthors `kaiten` as SUPPORT (utility, baseDamage 0), gates on Byakugan ON, spends 1 charge on successful commit, and plants **player-targeted** setup marks so pure state can observe shield/reflect without live combat damage reflection wiring.

## Spec

### Must exist when done

1. **Authoring (`kaiten` / ROTATION)**
   - `cardRole: CardRole.SUPPORT` (defensive prepare; 0 damage — not SIDE/ATTACK).
   - Honest description: requires **Byakugan ON**; spends **1** charge; grants **shield + reflect** (duration 1).
   - `modeInteraction: { modeId: 'byakugan', family: HYUGA, requireOn: true, consumeCharges: 1 }`.
   - Encode self setup via `markEffects` (or equivalent) for:
     - shield mark id stable e.g. `rotation_shield` (or `shield`) — `MarkFamily.SHIELD`, target **self/player**, duration **1**, stacks/value **50** if stacks used as absorb points (match existing effect value).
     - reflect mark id stable e.g. `rotation_reflect` — duration **1**; document value **0.6** in description; if Mark has no float field, store as stacks scaled (e.g. stacks 60 = 0.60) **or** keep effect value only in authoring description + a typed optional field — prefer **stacks = 60** representing 60% with test reading stacks, without inventing new combat math this task must not wire live incoming-damage reflect.
   - Remove reliance on mute `effects[]` alone for SOUL contract (may clear or leave non-normative if dual would double-apply — pure path must not double).

2. **`resolveSkill` behavior**
   - Role SUPPORT for `kaiten`:
     - **Validate:** Byakugan ON (`requireOn`); if OFF → `ok: false`, consume nothing.
     - **Commit** AP/CP/CD per existing T-007.
     - **Spend 1 charge** on `byakugan` at attempt (`trySpendCharges`); if spend fails after requireOn true (0 charges edge) → prefer reject before commit or document fail-closed no marks.
     - **Plant self marks** (owner PLAYER, target PLAYER) for shield + reflect; **do not** plant them on ENEMY.
   - Extend `applySupportMarks` (or kaiten-only branch) so mark specs can target self: e.g. optional `MarkSpec.targetActor: 'self' | 'enemy'` default enemy for backward compat with existing SUPPORT enemy marks.

3. **Compose with Hyuga tasks**
   - Charge spend is **only** Rotation here — Air Palm (T-026) and Twin Lion (T-022) remain no-spend / impact-consume as specified.
   - Do not require CP marks for Rotation.

### Architectural constraints
- [x] R0 pure; immutability
- [x] Hard require Byakugan; 1 charge only
- [x] Self marks, not enemy Terrain
- [x] No invented shield/reflect magnitudes beyond existing kaiten 50 / 0.6 packaging
- [x] No live `useSkill` / incoming-damage reflect pipeline required for AC (marks on state suffice)
- [x] No React

## Verification

- **Evaluation route:** `autonomous`

### Objective AC (blocking)

- [x] **RING-GUARD:** R0 only; no React/components/scenes/hooks/contexts imports.
- [x] **`npm run typecheck`** passes.
- [x] **AC1 — Require Byakugan:** Empty/OFF Mode board; resolve `kaiten` → `ok: false`; pools/marks/charges unchanged → gate: `npm test -- src/game/systems/__tests__/t028RotationKaiten` (`T-028 require on`).
- [x] **AC2 — Charge + self setup:** Byakugan ON charges 4; resolve `kaiten` ok → charges **3**; marks include player-targeted shield + reflect (ids as authored); enemy not the sole target of both → gate: same file `T-028 spend self marks`.
- [x] **AC3 — Authoring:** `kaiten.cardRole === SUPPORT`, `modeInteraction.requireOn` + `consumeCharges === 1` + `modeId === 'byakugan'` → gate: same file `T-028 authoring`.

### Subjective criteria
None.

## Out of scope

- Wiring live combat **incoming** damage to consume SHIELD / apply REFLECTION % (R1 pipeline later if missing).
- 64 Palms; Twin Lion (T-022); Air Palm (T-026); Morning Peacock (T-021).
- Mode family ascent (T-027); Chidori (T-025); SIDE tools (T-023).
- Live `useSkill` cutover; R2 Setup panel art.
- Editing SOUL.

## Notes for implementer

- Prefer `MarkSpec` optional target defaulting to enemy so Tripwire/Sealing/etc. stay stable.
- Order: validate requireOn → commit resources → spend charge → add self marks (mirrors SOUL attempt consume).
- If 1→0 charges ends Mode (T-005), assert board ends COOLDOWN when starting from 1 charge in one fixture (optional fourth assert inside AC2 file).
- Reflection observability: stacks/id on Mark is enough; do not invent a full damage-reflect formula in this task.

## Close summary

- **Status:** done (`c882817` merged ff into `wt/jp-08121518`)
- **Shipped:** `kaiten` SUPPORT requires Byakugan ON, spends 1 charge, plants player-targeted `rotation_shield` (50) + `rotation_reflect` (60). `MarkSpec.targetActor` defaults to enemy. Last charge ends Byakugan COOLDOWN.
- **Cycles:** 2 (1 verify FIX: restore non-normative `effects[]` so `getCardCategory` stays defensive).
- **Sim:** T-028 probe all true. Hyuga live WR 99.8% → 100%. **Healthy.**
- **Harvest:** no skill deltas beyond the category lesson (leave mute `effects[]` when they gate classification).
- **Noted:** live incoming-damage SHIELD/reflect still not wired (explicitly out of scope).

