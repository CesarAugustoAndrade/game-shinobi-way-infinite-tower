# T-072 research

1. **Author-only + ATTACK-only consume (recommended).** Plant `exposed` 2 ATTEMPT `requireHit`. Reuse ×1.15 RANGED. Filter consume to ATTACK so SIDE does not spend (spec). MELEE still consumes, no mult (T-023).

2. **Author-only, leave consume `return true`.** SIDE would consume Exposed — contradicts spec.

3. **New mark id `barrage_exposed`.** Spec forbids inventing.

**Recommendation:** option 1.
