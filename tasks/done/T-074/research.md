# T-074 research

1. **Mist sibling, cheaper, no weight (recommended).** Plant `decoy` 1/20 enemy. `applyDecoyOutgoing` −20 consume. No `nextDrawRoleBonus`. No Mode.

2. **Reuse `mist` id.** Would collide with T-073; spec wants distinct `decoy`.

3. **Plant only, no helper.** Meets AC; −20 untestable.

**Recommendation:** option 1.
