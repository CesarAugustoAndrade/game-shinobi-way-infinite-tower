# T-073 · R0 Hidden Mist: SUPPORT Mist 2 (−20 first enemy offensive) + SIDE weight +1 next draw

- **Type:** expand
- **Layer(s) touched:** domain (`hidden_mist` authoring + pure SUPPORT plant + next-draw SIDE weight)
- **Ring:** R0 (`src/game/constants/skills.ts` `HIDDEN_MIST`; `ResolveSkillSystem.ts` SUPPORT mark plant; `SupportWeightSystem` / `nextDrawRoleBonus` path). No React.
- **Lado / Side:** `back`
- **Bounded context:** combat-tactical-setup · **Subdomain:** core
- **Priority:** P1
- **Agent estimate:** S
- **Evaluation route:** `autonomous`
- **Depends on:** T-007 resolve; T-038 Smoke Bomb pattern (`smoke` mark + `nextDrawRoleBonus` SIDE +1); T-016 support-weight bag. Complements water defense SUPPORT after fire multi-hit wave. **Not blocked** by T-072.

## Génesis

**Why this gap:** SOUL §3.1 SUPPORT prepara + §3.5 Marks (legible, not Terrain) + §3.2 transparent weights + anti-visión secret % + §3.8 honesty. Canon catalog [`#catalogo`](../../docs/combat-tactical-setup-spec.html#catalogo) **hidden_mist**: SUPPORT · **2 AP · 5 CP · CD 5** · ANY · “**Mist 2**: Offensive enemigo **−20 puntos de impacto**; **SIDE weight +1** en el próximo draw. **Es Mark, no Terrain**.” Runtime is ACTIVE-only with SPEED **+50%** and ACC **−30%** duration-3 % effects — wrong axis, anti-visión %, **no** `cardRole`, **no** mist mark, **no** next-draw SIDE weight.

**Code evidence:**
- `HIDDEN_MIST` (`skills.ts` ~1849+): AP2/CP5/CD5; base 0; WATER; `effects` SPEED 0.5×3 + ACC −0.3×3; **no** `cardRole`; **no** `markEffects`; **no** `nextDrawRoleBonus`.
- Smoke sibling already ships: `smoke_bomb` SUPPORT + `smoke` mark stacks 25 + `nextDrawRoleBonus: { role: SIDE_ATTACK, delta: 1 }` (`skills.ts` ~929–958); `SupportWeightSystem` enqueues role bonus (~90–91).
- T-068 / T-071 / T-072 Génesis **discarded** “hidden_mist Mist 2 (smoke sibling)” as the water pressure SUPPORT follow-up.

**Already covered (do not duplicate):**
- **T-038** — Smoke Bomb −25 first offensive + SIDE +1 (different id/magnitude/costs).
- **T-066** — Flash Bomb Blinded −2 ACC (stat mark, not impact flat).
- **T-064 / T-065** — Shield SUPPORTs (absorb, not mist).

**Discarded alternatives:**
- bunshin Decoy 1 (−20 first offensive, no SIDE weight) — basic academy decoy.
- henge Misdirect SIDE weight +2 + Exposed on SIDE — setup SUPPORT.
- kawarimi Substitution first-hit ignore + band — larger reaction M.
- sand_shield Shield 45 — pure absorb sibling of Mud Wall.
- Keeping SPEED/ACC % as “mist” — anti-visión and contradicts catalog Mark-not-Terrain.

**SOUL pillars served:** §3.1 SUPPORT; §3.2 weighted draw honesty; §3.5 Marks; §3.6 no Terrain rewrite; §3.8 content honesty; catalog `#catalogo` hidden_mist; anti-visión secret %.

## Context

Smoke Bomb arms −25 impact + SIDE bias. Hidden Mist is the **water** pressure twin: **Mist 2** subtracts **20** from the first enemy Offensive impact and enqueues the same **SIDE +1** next-draw role bonus — still a Mark, never Room Terrain.

## Spec

### Must exist when done

1. **Authoring (`hidden_mist`)**
   - `cardRole: CardRole.SUPPORT`.
   - Costs: **AP 2**, **CP 5**, **CD 5**, HP 0.
   - `baseDamage: 0`; ANY range (default / no restrictive `allowedRanges` unless required).
   - Tags: NINJUTSU / WATER / DEFENSE / MARK as available.
   - `nextDrawRoleBonus: { role: CardRole.SIDE_ATTACK, delta: 1 }` (mirror Smoke).
   - `markEffects`: plant id **`mist`** (or `hidden_mist`), duration **2**, stacks **20** (or fixed rule by id encoding −20 impact on first enemy Offensive), `targetActor: 'enemy'` (or same ownership model as `smoke`).
   - Remove primary SPEED +0.5 / ACC −0.3 % effect identity.
   - Honest description: Mist 2 −20 first enemy offensive impact; SIDE weight +1 next draw; does not change Terrain.

2. **`resolveSkill` SUPPORT**
   - Pay costs; plant mist mark; enqueue SIDE +1 next-draw role bonus via existing Smoke/T-016 path; `damageDealt === 0`.
   - No Terrain / Heat fields mutated.

3. **Mist −20 (minimal)**
   - Mark present after resolve with duration 2 and −20 contract visible in authoring/tests.
   - **Preferred if free:** enemy Offensive damage path subtracts 20 once and consumes mist (mirror smoke −25 helper if it exists). If smoke impact subtract is still partial, **AC requires plant + weight enqueue**; document impact apply as shared follow-up only if smoke already implements it in-tree.

### Architectural constraints
- [x] R0 pure; immutability
- [x] Catalog: AP2 · CP5 · CD5 · Mist 2 −20 · SIDE +1 next draw · Mark not Terrain
- [x] Reuse Smoke / `nextDrawRoleBonus` / SupportWeightSystem; no permanent baseWeight mutation
- [x] No React; no smoke_bomb / flash_bomb retune

## Verification

- **Evaluation route:** `autonomous`

### Objective AC (blocking)

- [x] **RING-GUARD:** R0 only; no React/components/scenes/hooks/contexts imports in touched files.
- [x] **`npm run typecheck`** passes.
- [x] **AC1 — Authoring:** `hidden_mist` has `cardRole: SUPPORT`, AP2/CP5/CD5, `baseDamage: 0`, mist mark duration 2 (−20 contract), `nextDrawRoleBonus` SIDE +1; no primary SPEED 0.5 / ACC −0.3 identity → gate: `npm test -- src/game/systems/__tests__/t073HiddenMist` (`T-073 authoring`).
- [x] **AC2 — Resolve mark + weight:** resolve → mist mark present duration 2; SIDE_ATTACK effectiveWeight +1 for one draw bag consume; ATTACK weight unchanged by this bonus → gate: same file `T-073 weight mark`.
- [x] **AC3 — No Terrain / zero damage:** resolve does not mutate Terrain; `damageDealt === 0` → gate: same file `T-073 support only`.

### Subjective criteria
None.

## Out of scope

- bunshin Decoy; henge Misdirect; kawarimi Substitution; sand_shield; water_dragon Mist-payoff.
- Broadening mist into board fog / band-wide vision rules.
- Live UI; editing SOUL; retuning smoke magnitude.

## Notes for implementer

- Canon: HTML `#catalogo` `hidden_mist`; SOUL §9 Marks + anti-Terrain; mirror `smoke_bomb` authoring shape with stacks **20** and costs AP2/CP5/CD5.
- Prefer mark id `mist` distinct from `smoke` so both can coexist if both plant.
- poison_fog catalog later reads “contra Mist activo” — do not implement that interaction here (OOS; note only).

## Close

- Cycles: 1 (explore → plan auto-approve → implement → review APPROVE → verify PASS → merge).
- Summary: `hidden_mist` is SUPPORT AP2/CP5/CD5. Plants enemy Mist 2 (stacks 20). SIDE +1 next draw. SPEED/ACC % removed. `applyMistOutgoing` cuts 20 once. No Terrain.
- Improvements noted: live EnemyTurn does not yet call applyMistOutgoing (same as smoke).

