# T-073 research

1. **Smoke sibling + applyMistOutgoing (recommended).** Plant `mist` 2/20 enemy; SIDE +1; helper −20 then consume. Distinct from `smoke`.

2. **Generalize applySmokeOutgoing to any id.** Touches T-038 helper; skip.

3. **Plant only, no apply helper.** Meets AC but leaves −20 untestable.

**Recommendation:** option 1.
