# T-045 research

In-tree (T-022/T-026/T-035).

## Options

1. **Mode path + `impactMarkConsume`** (chosen). Distinct from consume-all.
2. Reuse 64 Palms `damagePerMarkStackBonus` + cap 0.2. Rejected: that path is Mode-gated and consume-all.

## Rounding lock

Mode then CP: `floor(12 * 1.4) = 16`; Mode OFF + 2 CP: `floor(12 * 1.2) = 14`; both: `floor(12 * 1.4 * 1.2) = 20`.
