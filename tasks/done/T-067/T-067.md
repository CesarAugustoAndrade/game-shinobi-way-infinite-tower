# T-067 · R0 Focused Breathing: SUPPORT +8 CP + next CP upkeep −2 (once)

- **Type:** expand
- **Layer(s) touched:** domain (`focused_breathing` authoring + pure SUPPORT chakra grant + one-shot CP upkeep discount)
- **Ring:** R0 (`src/game/constants/skills.ts` `FOCUSED_BREATHING`; `ResolveSkillSystem.ts` SUPPORT sustain; thin upkeep-discount helper akin to Gate Prep). No React.
- **Lado / Side:** `back`
- **Bounded context:** combat-tactical-setup · **Subdomain:** core
- **Priority:** P1
- **Agent estimate:** S–M
- **Evaluation route:** `autonomous`
- **Depends on:** T-007 resolve; Mode upkeep cost surface (T-005/CombatModeSystem); T-017 Gate Prep one-shot discount pattern. Complements Mode sustain after control SUPPORT. **Not blocked** by T-066.

## Génesis

**Why this gap:** SOUL §3.1 SUPPORT prepara (sustain) + §7–§8 Mode upkeep (CP/HP peaje; no same-turn regen finance of upkeep) + §3.8 content honesty. Canon catalog [`#catalogo`](../../docs/combat-tactical-setup-spec.html#catalogo) **focused_breathing**: SUPPORT · **1 AP · CD 2** · ANY · “**Recupera 8 CP**; el **próximo upkeep de CP cuesta 2 menos**, una sola vez.” Runtime is ACTIVE-only with `CHAKRA_REGEN` **10** tick identity — wrong magnitude, **no** immediate +8 pool grant, **no** one-shot CP upkeep discount, **no** `cardRole`.

**Code evidence:**
- `FOCUSED_BREATHING` (`skills.ts` ~553–573): AP1/CP0/CD2; `effects` CHAKRA_REGEN **10**×1; **no** `cardRole`; pure SUPPORT path does not apply regen effects as instant CP.
- `ResolveSkillSystem` can refund/adjust `pools.chakra` (Kai cleanse) and arms `pendingGateHpDiscount` (T-017) — no `pendingCpUpkeepDiscount` sibling yet.
- Mode upkeep costs live on `ModeDefinition.upkeep` / CombatModeSystem (~409); no breathing discount consumer.
- T-066 Génesis **discarded** “focused_breathing +8 CP + next CP upkeep −2” as the sustain slice after Flash Bomb.

**Already covered (do not duplicate):**
- **T-017** — Gate Prep next Gate **HP** activation −50% (HP axis, not CP upkeep).
- **T-046** — Kai cleanse + optional CP refund (cleanse, not sustain breathing).
- **T-005** — Mode board / upkeep payment skeleton.

**Discarded alternatives:**
- basic_medical heal 25 + cleanse Poison/Bleed — larger sustain+cleanse.
- kawarimi Substitution reaction — M reaction slice.
- senbon_rain 4×2 Poison SIDE — tool multi-hit.
- Keeping CHAKRA_REGEN 10 as “breathing” — contradicts catalog 8 CP + upkeep −2 once.

**SOUL pillars served:** §3.1 SUPPORT sustain; §3.4 Modes peaje; §7–§8 upkeep order; §3.8 honesty; catalog `#catalogo` focused_breathing; anti-visión wrong packaging.

## Context

Modes charge CP upkeep. Focused Breathing should be the **cheap SUPPORT** that tops up **8 CP now** and softens the **next CP upkeep payment by 2** (one-shot), mirroring Gate Prep’s pending-discount pattern on the CP axis.

## Spec

### Must exist when done

1. **Authoring (`focused_breathing`)**
   - `cardRole: CardRole.SUPPORT`.
   - Costs: **AP 1**, **CP 0**, **CD 2**, HP 0.
   - `baseDamage: 0`; ANY; tags CHAKRA/SUSTAIN as available.
   - Contract: grant **+8** chakra on play; arm **one-shot** next **CP** Mode upkeep **−2** (min 0 per resource).
   - Remove primary `CHAKRA_REGEN` 10 identity (no double regen tick from this card on pure path).
   - Honest description: +8 CP; next CP upkeep −2 once.

2. **`resolveSkill` SUPPORT**
   - Pay AP (and CD); `damageDealt === 0`.
   - `pools.chakra` increases by **8** (cap at max if maxChakra exists on pools; if no max field, uncapped +8 in pure state).
   - Arm pending flag e.g. `pendingCpUpkeepDiscount: 2` (or boolean + constant 2) on `ResolveSkillState` / thin helper module (T-017 style).
   - No marks required unless implementer prefers a self mark for observability — prefer explicit pending field like Gate Prep.

3. **One-shot CP upkeep discount**
   - Pure helper: given Mode upkeep `TypedCost` with `chakra: N` and pending discount D=2 → effective chakra cost `max(0, N-2)`; HP upkeep unchanged.
   - Consume pending after **one** successful CP upkeep application (or one upkeep phase attempt that reads it — document).
   - If no Modes ON / no CP upkeep that phase: pending may remain until a CP upkeep is actually charged (prefer: consume only when a CP cost is reduced).
   - Wire into Mode upkeep payment path used by pure tests (CombatModeSystem or turn upkeep helper); AC may test helper + resolve arm without full combat sim if payment hook is unit-tested.

### Architectural constraints
- [x] R0 pure; immutability
- [x] Catalog: AP1 · CD2 · +8 CP · next CP upkeep −2 once
- [x] Does not finance **same-turn** upkeep with this regen if upkeep already passed (SOUL §7); discount applies to a **future** upkeep phase
- [x] No React; no Gate Prep / Flash Bomb retune

## Verification

- **Evaluation route:** `autonomous`

### Objective AC (blocking)

- [x] **RING-GUARD:** R0 only; no React/components/scenes/hooks/contexts imports in touched files.
- [x] **`npm run typecheck`** passes.
- [x] **AC1 — Authoring:** `focused_breathing` has `cardRole: SUPPORT`, AP1/CP0/CD2, `baseDamage: 0`; no primary CHAKRA_REGEN 10 identity → gate: `npm test -- src/game/systems/__tests__/t067FocusedBreathing` (`T-067 authoring`).
- [x] **AC2 — +8 CP:** resolve with chakra 5 → chakra **13** (or +8), damage 0, pending CP upkeep discount armed → gate: same file `T-067 grant`.
- [x] **AC3 — Upkeep −2 once:** with pending armed, pure discount (or upkeep pay) on a Mode with upkeep CP 4 → pays **2** CP (or cost shows 2); second upkeep without re-arm pays full → gate: same file `T-067 discount`.

### Subjective criteria
None.

## Out of scope

- Basic Medical heal/cleanse; Kawarimi; Senbon Rain; Gate Prep retune; full PlayerTurnSystem integration beyond pure upkeep helper; live UI; editing SOUL.

## Notes for implementer

- Canon: HTML `#catalogo` `focused_breathing`; SOUL §7 upkeep before regen; mirror `GatePrepDiscountSystem` as `FocusedBreathingDiscountSystem` or shared pending field on resolve state.
- Instant +8 is **not** turn-start regen; do not implement as multi-turn CHAKRA_REGEN buff.

## Close

- Cycles: 1 (explore → plan auto-approve → implement → review APPROVE → verify PASS → merge).
- Summary: `focused_breathing` is SUPPORT AP1/CP0/CD2. Grants +8 CP and arms `pendingCpUpkeepDiscount: 2`. `applyModeUpkeep` cuts the first CP upkeep by 2 once. Legacy CHAKRA_REGEN 10 removed. PlayerTurnSystem live consume left out of scope.
- Improvements noted: live turn-start clock does not yet read `pendingCpUpkeepDiscount` (spec out of scope).
