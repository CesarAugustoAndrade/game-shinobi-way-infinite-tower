# T-063 · R0 Iaido: ATTACK 12 CLOSE + Cloaked +50% Setup (force crit, attempt consume)

- **Type:** expand
- **Layer(s) touched:** domain (`iaido` authoring + pure ATTACK resolve Cloak setup mult)
- **Ring:** R0 (`src/game/constants/skills.ts` `IAIDO`; `ResolveSkillSystem.ts` ATTACK cloak spent → Setup ×1.5). No React.
- **Lado / Side:** `back`
- **Bounded context:** combat-tactical-setup · **Subdomain:** core
- **Priority:** P1
- **Agent estimate:** S
- **Evaluation route:** `autonomous`
- **Depends on:** T-007 resolve; T-004 ATTEMPT marks; T-062 Cloaked mark id / force-crit package (fixture `cloaked` ok if T-062 not merged). Complements weapon stealth payoff. **Not blocked** by T-062 merge.

## Génesis

**Why this gap:** SOUL §3.1 ATTACK cobra + §9 own-mark attempt consume (Cloak named) + §3.8 content honesty. Canon catalog [`#catalogo`](../../docs/combat-tactical-setup-spec.html#catalogo) **iaido**: ATTACK · **2 AP · 1 CP · CD 3** · CLOSE · “**12 dmg**; con **Cloak** activo obtiene **+50% Setup dmg** además del auto-crit y **consume Cloak al intento**.” Runtime is ACTIVE-only base 12 with **critBonus 40** “first action” flavor — wrong axis, no `cardRole`, no Cloak setup bucket, no attempt consume of `cloaked`.

**Code evidence:**
- `IAIDO` (`skills.ts` ~783–802): AP2/CP1/CD3; base 12; MELEE; `critBonus: 40`; Aggressive stance mult; **no** `cardRole`; **no** CLOSE `allowedRanges`; **no** Cloak/`setupRead`/spent-mark mult.
- `ResolveSkillSystem` has ATTEMPT consume + `lotus_opening` ×1.25 and enemy `setupRead` mult (~700–710, ~889–901) but **no** Iaido/`cloaked` Setup ×1.5 path.
- T-062 Génesis/notes **pair** Iaido as Cloak payoff after `cloak_invis` SUPPORT; T-062 owns general ATTACK force-crit +1 DEX on `cloaked`.

**Already covered (do not duplicate):**
- **T-062** — Cloak SUPPORT plants `cloaked` 2; any ATTACK force-crit +1 DEX attempt consume.
- **T-059** — Sword Slash ATTACK + Bleed (weapon, no Cloak).
- **T-050** — Feint flat +15 next ATTACK (not Setup %).

**Discarded alternatives:**
- mud_wall Shield 35 SUPPORT — real gap; lower leverage than closing the Cloak→Iaido tunnel now that T-062 exists.
- basic_atk / heavy_kick floor ATTACK cardRole-only — low leverage.
- senbon_rain multi-hit Poison SIDE — tool multi-hit; different family.
- Keeping critBonus 40 as “quick draw” — contradicts catalog Cloak Setup contract.

**SOUL pillars served:** §3.1 ATTACK; §3.5 Marks; §3.8 content honesty; §9 Cloak attempt consume; catalog `#catalogo` iaido; pairs T-062.

## Context

T-062 arms **Cloaked**. Iaido should be the **weapon ATTACK** that converts Cloak into a **Setup-bucket +50%** payoff (in addition to T-062 force-crit / +1 DEX), still consuming the mark at **attempt**.

## Spec

### Must exist when done

1. **Authoring (`iaido`)**
   - `cardRole: CardRole.ATTACK`.
   - Costs: **AP 2**, **CP 1**, **CD 3**, HP 0.
   - `baseDamage: 12`; `hitCount` default 1; CLOSE (`allowedRanges: [CLOSE]` or equivalent).
   - Tags WEAPON/PHYSICAL/PRECISION as available.
   - Declare Cloak setup contract: when player `cloaked` is spent/present for this ATTACK → **Setup damage mult +50%** (`×1.5` on dealt damage after base hit aggregation, Setup bucket — not Mode enhanced %).
   - Remove primary identity of **critBonus 40** first-action (Cloak/T-062 owns auto-crit).
   - Honest description: 12 CLOSE; with Cloak +50% Setup + auto-crit path; consume Cloak on attempt.

2. **Baseline without Cloak**
   - `resolveSkill(iaido)` with no `cloaked`: normal ATTACK costs, base damage path, **no** force-crit from this skill alone, mark board unchanged for cloak.

3. **With Cloaked package**
   - Fixture or T-062 plant: player mark id **`cloaked`** duration ≥1.
   - On `resolveSkill(iaido)`:
     - **Consume** `cloaked` at **attempt** (even full miss) — share T-062 ATTEMPT filter (ATTACK-only); do not double-remove.
     - If hits land: damage includes **×1.5 Setup** vs same ports without cloak (document order: after hit sum, with other mark mults; independent of Mode).
     - Force-crit: rely on T-062 package when present; if T-062 not merged yet, Iaido may call `forceCrit` when `cloaked` spent so AC3 is testable alone.
   - SIDE/SUPPORT do not use this Iaido Setup path.

### Architectural constraints
- [x] R0 pure; immutability
- [x] Catalog: AP2 · CP1 · CD3 · CLOSE · 12 · Cloak +50% Setup · attempt consume
- [x] No critBonus-40 first-action identity; no React
- [x] No mud_wall / Sword Slash / Cloak SUPPORT retune

## Verification

- **Evaluation route:** `autonomous`

### Objective AC (blocking)

- [x] **RING-GUARD:** R0 only; no React/components/scenes/hooks/contexts imports in touched files.
- [x] **`npm run typecheck`** passes.
- [x] **AC1 — Authoring:** `iaido` has `cardRole: ATTACK`, AP2/CP1/CD3, base 12, CLOSE contract, Cloak +50% Setup declaration; no primary `critBonus: 40` identity → gate: `npm test -- src/game/systems/__tests__/t063Iaido` (`T-063 authoring`).
- [x] **AC2 — Baseline:** resolve iaido without `cloaked` → damage path base (deterministic hit, no 1.5), no cloak mark required → gate: same file `T-063 base`.
- [x] **AC3 — Cloak payoff:** with `cloaked`, resolve iaido hit → `cloaked` gone after attempt; damage ≥ floor(base×1.5) under same hit ports vs AC2 baseline; full miss still consumes `cloaked` → gate: same file `T-063 cloak`.

### Subjective criteria
None.

## Out of scope

- Cloak SUPPORT authoring (T-062); mud_wall Shield; basic_atk/heavy_kick; senbon_rain; live UI; editing SOUL.

## Notes for implementer

- Canon: HTML `#catalogo` `iaido`; SOUL §9 Cloak attempt; Setup bucket separate from Mode enhanced (SOUL §8 techos).
- Prefer mark id `cloaked` shared with T-062. `lotus_opening`-style spent mult is a fine pattern if `setupRead` remains enemy-only.

## Close

- Cycles: 1 (explore → plan auto-approve → implement → review APPROVE → verify PASS → merge ff).
- Summary: `iaido` is ATTACK AP2/CP1/CD3 CLOSE 12. With spent Cloak: Setup ×1.5 after T-062 crit (12→28). Miss still consumes. critBonus 40 removed.
- Improvements noted: Setup gated on `skill.id === 'iaido'` (reviewer NIT).
