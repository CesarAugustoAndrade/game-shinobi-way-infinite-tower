# T-020 · Arte de skills (catálogo completo)

## #a1

### Maker summary

**Changed**
- `scripts/extract-skills-meta.mjs` + `scripts/generate-skill-icons.mjs` — extract 114 skill ids, generate card tiles.
- `src/game/constants/skillArtManifest.ts` — auto-generated catalog (114 entries, all with `src`).
- `artRegistry.ts` — registers `skill:<id>` for all skills; `getSkillArt()` cascade.
- **114 SVG** under `public/assets/icons/skills/{id}.svg`.
- **8 painted PNGs** copied to `public/assets/skill_*.png` and preferred for: basic_atk, shuriken, fireball, primary_lotus, shadow_clone, gentle_fist, mind_destruction, chidori.
- `SkillCard.tsx` — removed hard-coded name heuristics + **removed external pinimg hotlink**; uses registry only.
- `Loot.tsx` + `ScrollDiscovery.tsx` (+ CSS) — skill art via `ArtIcon` + `getSkillArt`.
- Training scene is **stat training** (no skill tiles to wire); no dead hook added.

**Evidence**
- Skills with src: **114/114** (0 missing)
- Painted: 8 · SVG tiles: 106
- `npx tsc --noEmit` ✅
- `npm test` ✅ 341
- `npm run build` ✅
- `npm run lint:css` ✅

**Open questions**
- Full combat-art painted PNGs for the remaining 106 can replace SVGs in place (same paths/keys).
- `skills.ts` still only tags `image:` on 3 skills; display uses registry (source of truth).

### Reviews (lead self-score — solo cycle)

#### ARQUITECTURA — Score: 90
Registry + generated manifest; no React in game constants; SkillCard no longer embeds asset map/hotlink.

#### SISTEMA — Score: 90
Every skill id resolvable with src; combat/loot/scroll wired; no skill falls to mystery/external.

#### PRESENTACION — Score: 86
Coherent element-tinted arcade tiles + 8 painted cards; no broken external images. Not full Neo Geo painted set yet.

#### BALANCE — Score: 95
Display-only.

### Gate
**PASS** (90 / 90 / 86 / 95)
