# Log — T-007 · Rediseño RegionMap retro-arcada (UI + assets)

section: presentation · run: 2026-06-29-A · **modo: interactivo** (el humano puntúa el visual)

Referencia objetivo (provista por el humano, versionada): `docs/references/regionmap-retro-reference.png`.
Ejecución por fases: **F1** = restyle de UI (loop-maker) · **F2** = fondos pixel-art (vía `/generar-asset`).

---

## <a name="f1a1"></a>Fase F1 (restyle de UI) · Attempt 1 — 2026-06-30T22:51:12Z — gate: PASS (90/90/88/90)

### Maker (Changed / Evidence)
- **`RegionMap.tsx` + `LocationCardDisplay.tsx` + `exploration.css`** (+ `DangerLevelBar`/`WealthLevelBar`/`ActivityIcons`): restyle retro-arcada replicando la referencia — título "LAND OF WAVES" pixel con glow + flourish ✦ + subtítulo italic; 3 tarjetas verticales con borde neón (cian desbloqueadas, **magenta la bloqueada**), badge numérico en caja, leyenda de clasificación (`WILDERNESS`/`SETTLEMENT`/`? UNKNOWN TERRITORY`), apodo italic del biome; barras SEGMENTADAS **DANGER verde** / **WEALTH naranja→amarillo** con valor a la derecha; `ROOMS N+`; fila de iconos de actividad; **barra de estado inferior color-codeada** (`★ REST POINT` magenta, `★ MERCHANT AVAILABLE` dorado, `UNKNOWN TERRITORY` magenta); card bloqueada con glitch morado + `?`; botón `ENTER LOCATION` neón; `REGION PROGRESS` magenta + `0% (0/10)` + footer de atajos. Textura CRT/scanline.
- **Slot de fondo pixel-art** cableado: `<img src="/assets/location_${card.location.id}.png">` con **fallback** (icono sobre fondo oscuro vía `onError`) → sin 404 hasta la Fase 2.

**Evidence:** tsc 0 · lint:css 0 · build OK. Verificado por el LEAD vía Playwright (dev :3012, navegación menú→char-select→region-map): el RegionMap renderiza fiel a la referencia, los sidebars del juego intactos, sin 404 de assets (solo favicon). Screenshots: `t007-f1-regionmap.png`, `t007-f1-fullpage.png`.

### Scoring (interactivo)
- **PRESENTACION ≥85 (PASS)** — el humano juzgó el screenshot vs la referencia: estructura, neón, barras, badges y footers replican el objetivo; el arte de fondo es el gap conocido de F2 (no cuenta en contra).
- **ARQUITECTURA 90 / SISTEMA 90 / BALANCE 90** — propuestos por el LEAD sobre evidencia dura y aceptados: cambio puramente aditivo de presentación, sin tocar lógica de juego ni balance, gates verdes, slot con fallback (sin 404).

### Decisión LEAD
**PASS (90/90/88/90).** F1 cierra. T-007 sigue `active` (falta F2). Arranca **F2**: generar los fondos pixel-art de las ubicaciones vía `/generar-asset` (Codex), siguiendo la dirección `combat-art`/pixel-arcade y la referencia. Pendiente: confirmar la convención de nombre (`location_<id>.png`) vs un esquema por biome/tipo, ya que los `id` de ubicación son procedurales.

---

## <a name="f2a1"></a>Fase F2 (fondos pixel-art de ubicaciones vía /generar-asset) · 2026-07-01T11:02:09Z — gate: PASS (90/90/88/90)

**Decisión del humano:** alcance = **todos los biomes de Waves** (~14); commit **F1+F2 juntos**.

### Trabajo (LEAD + Codex)
- **Fix de cableado (causa raíz):** el slot de F1 usaba `location_${location.id}.png`, pero `id` lleva sufijo aleatorio (`location-<configId>-<random>`) → nunca casaría. Re-keyed a **biome-slug**: `location_${biome.toLowerCase().replace(/[^a-z0-9]+/g,'_')}.png` (biome es estable y ya está en Location). `LocationCardDisplay.tsx`.
- **14 fondos generados** (Codex 0.139.0, pixel-art 16-bit, 1024×576 opaco): 1 de validación (`foggy_shoreline`, ~90k tok, QA-OK) + 13 en 2 `codex exec` PARALELAS en background (7+6, ~19k tok/asset) con el de validación como `-i` de acabado. Biomes: foggy_shoreline, coastal_harbor, rural_village, dense_forest, underground_cavern, river_banks, shipwreck, great_bridge, fortified_camp, ruined_estate, secret_harbor, underwater_temple, fortified_mansion, mist_covered_bridge.
- **Versionado:** `.gitignore` `!/assets/location_*.png`. `.playwright-mcp/` añadido a gitignore (artefactos QA).
- Memoria del skill `generar-asset` ampliada (`~/.claude/skills/generar-asset/LEARNINGS.md`: lote de 14 fondos + clave-por-biome).

### Evidence / QA
- Gates: tsc 0 · lint:css 0 · build OK. QA visual (Read) de 4 assets (foggy_shoreline, underwater_temple, underground_cavern + rural_village in-card): excelentes, estilo consistente, on-theme. Codex auto-verificó opacidad/tamaño de los 14.
- **In-game (Playwright, dev :3012):** el fondo renderiza dentro de la card (scrim + apodo + barras encima), fiel a la referencia; **0 errores 404** (solo favicon). Screenshot: `t007-f2-regionmap.png`.

### Scoring (interactivo)
- **PRESENTACION ≥85 (PASS)** — el humano juzgó los fondos + integración fieles a la referencia.
- **ARQUITECTURA 90 / SISTEMA 90 / BALANCE 90** — assets + fix de clave por biome + excepción gitignore; sin tocar lógica de juego; sin 404; fallback intacto.

### Decisión LEAD
**PASS (90/90/88/90). T-007 COMPLETO (F1+F2).** Marcado `passed`. Commit F1+F2 juntos (autorizado por el humano). Siguiente pendiente del backlog: **T-008** (Motor de Eventos 2.0).
