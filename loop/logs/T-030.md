# T-030 · Secret locations se descubren de verdad

## #a1 — PASS

### Maker summary

**Problem:** Secret locations stayed `isDiscovered: false` forever → `drawLocationCards` weight 0.

**Done**
1. **RegionSystem pure API**
   - `discoverSecretByRequirement`
   - `discoverSecretsFromEventFlags` (unlockCondition.requirement ↔ eventFlags)
   - `discoverSecretsFromCompletedLocation` (secretPaths → target; resolves config id vs `location-<id>-uuid`)
2. **Wire**
   - `useLocationCards`: sync on path choice + location complete; log “Secret location revealed…”
   - `useActivityHandlers`: after event outcome with new flags, sync region
   - `useExploration` / `App`: pass `player` + `setRegion`
3. **Content:** Waves `mist_ambush_cache` outcomes set `sunken_ship_discovered` / `hidden_cove_discovered`
4. **Tests:** 3 unit tests in RegionSystem.test.ts (weight gate, flags unlock + draw, secret path)

**Gates:** tsc · test 358 · build OK

### Reviews (lead self-score)

| Lente | Score | Note |
|-------|-------|------|
| ARQUITECTURA | 92 | pure RegionSystem; hooks only orchestrate |
| SISTEMA | 91 | dual unlock path flags + secretPaths; tests |
| PRESENTACION | 88 | log feedback on reveal; map cards can show secrets |
| BALANCE | 90 | secrets optional higher-reward routes; not forced |

### Gate
**PASS**
