# Log — T-014 · Overhaul Cinemático de la Pantalla de Combate

section: presentation · run: 2026-07-02-A · **modo: interactivo** (el humano puntúa) · maker en Sonnet, LEAD en Fable.
Flujo pactado: mockup ASCII aprobado ANTES de implementar. Topic marcado por el humano como prioritario ("prestale bastante atención").

---

## <a name="a1"></a>Attempt 1 — 2026-07-04T10:02:15Z — gate: FAIL (interactivo; PRE ~70)

### Fase mockup (aprobada)
Maker entregó mockup desktop/mobile + plan (tokens `--cb-*`, grid `1fr auto`, lower-third con scrim, cartas legibles). Humano aprobó e
 respondió las 3 open questions: floating text se queda en viewport (migra en T-013); z-index se verifica (no hubo conflicto); enemigo `height:70% + width:auto`.

### Maker (Changed)
- `CinematicViewscreen.tsx` + **nuevo** `CinematicViewscreen.css`: 5 capas (atmósfera blur, scanlines, viñeta, sprite del enemigo contain anclado abajo, scrim + contenido).
- `Combat.tsx`: estructura `combat__stage` / `combat__deck`; PlayerHUD primero en el deck.
- `Combat.css`: reescrito — tokens `--cb-*`, grid `1fr auto`, overlay 2×2 (buffs top-right, stats bottom-left), name con clamp; **CSS legacy retirado** (`.combat__interface`, `.combat__skills*`, `.combat__skill-grid*` — grep 0 usos).
- `SkillCard.css`: disabled 0.5→0.75 + overlay `::after`.
- Z-index: sin conflicto real (modales 50/40 vs deck z-base) — no se tocó.

### Evidence (gates del LEAD)
tsc 0 · lint:css 0 · 254/254 · build OK. Screenshots en vivo vía Playwright (dev 3012, elite fight en The Docks):
- Desktop 1280×800: PlayerHUD visible, sin hueco, enemigo entero, deck ordenado. Sólido.
- Mobile 390×844: **overflow horizontal** (carta Taijutsu / CP / DEFENSIVE / END TURN cortados) + nombre solapando el retrato.

### Scoring (interactivo) — FAIL
El humano rechazó la dirección: **"no me gusta todavía, haz un overhaul completo, inspírate en tipo Buriedbornes. También habrá dos assets: el del enemigo y el del background"**.
Lecciones para el intento 2:
1. Rediseño completo estilo **Buriedbornes** (dark dungeon-crawler: fondo protagonista, enemigo como entidad central, paneles limpios y compactos, info alrededor del arte).
2. El stage debe componer **DOS assets separados**: background (asset propio) + enemigo (asset propio, sprite/retrato) — ya no un solo `enemy.image` haciendo de todo.
3. Además persisten los fixes objetivos: overflow horizontal mobile y solape del nombre.

---

## <a name="a2"></a>Attempt 2 — 2026-07-04T11:04:33Z — gate: FAIL (interactivo; PRE ~78)

### Fase mockup v2 (aprobada)
Buriedbornes pixel-arcade: grid 3 filas (stage/enemy-info/deck); doble asset (bg `location_*.png` de T-007 mapeado por slug de bioma + retrato-carta enmarcado con fade inferior); mobile scroll-snap. Humano decidió: retrato grande `min(360px,52vw)`, buffs stage top-right, fallback gradiente+scanlines.

### Maker (Changed)
`getBiomeSlug` compartido en colorHelpers (des-duplicado de LocationCardDisplay); CinematicViewscreen reescrito (props enemyImage/backgroundImage/buffContent, capas bg/gradiente/viñeta/scanlines/carta/buffs, dead code del a1 retirado); Combat.tsx grid 3 filas + `.combat__enemy-info`; App.tsx pasa `background` por bioma; mobile ≤480px con overflow-x en la mano.
Gates: tsc 0 · lint:css 0 · 254/254 · build OK.

### Evidencia visual del LEAD (Playwright, elite fight en Abandoned Manor)
- Desktop 1280: dirección clavada — bg pixel-art del bioma + retrato-carta + fila de info sin solape. Sólido.
- Mobile 390: **overflow horizontal PERSISTE** (nombre cortado, retrato empujado fuera, HP/CP/posturas cortadas; la página scrollea horizontal — hay un ancho fijo desbordando que el scroll-snap de la mano no ataca).

### Scoring (interactivo) — FAIL, nueva referencia visual
El humano aportó imagen de referencia (`docs/references/combat-enemy-panel-reference.png`) y pidió: **la parte de arriba así, manteniendo pixel-arcade** —
1. **Panel de info del enemigo FLOTANDO A LA IZQUIERDA** sobre la escena (no fila propia debajo): icono + nombre + badge `Lv. N`; tags CHUNIN/AFFINITY; 3 columnas de stats con iconos (PHYS/ELEM/NIN %); fila STATUSEFFECTS (slots vacíos visibles); bloque INFO (descripción corta del enemigo); HP con número grande `115 / 332` + barra roja.
2. **Enemigo INTEGRADO en el background a la derecha** — parte de la escena, sin marco de carta.
3. Autorización explícita: **"si tienes que regenerar assets para cortar al ninja o algo do it"** → vía libre para re-cortar los retratos a sprites transparentes (codex//generar-asset).
4. Sigue pendiente: matar el overflow mobile DE RAÍZ.

---

## <a name="a3"></a>Attempt 3 — 2026-07-04T11:55:00Z — gate: FAIL (interactivo; arriba aprobado, deck a rediseñar)

### Maker (Changed)
- Panel flotante izquierdo fiel a la referencia: 🎯+nombre+`Lv. N` (dangerLevel real, `Enemy.archetype`/`dangerLevel` añadidos a types + EnemySystem), tags CHUNIN/AFFINITY, 3 columnas PHYS/ELEM/MND coloreadas, STATUSEFFECTS 4 slots (absorben los buff chips), INFO por arquetipo (`constants/enemyArchetypes.ts`, dato no hardcodeado), HP número grande + barra roja.
- Enemigo INTEGRADO a la derecha: sin marco, `mask-image` radial en todos los bordes; variante `--cutout` lista para sprites transparentes (`enemyCutout` prop, sin cambio de código futuro).
- La fila `.combat__enemy-info` del a2 murió; grid vuelve a 2 filas.
- **Causa raíz del overflow mobile encontrada y matada**: `min-width: 9rem` + `white-space: nowrap` de la fila enemy-info; panel absoluto ya no aporta al flujo; `overflow-x:hidden` cinturón + `min-width:0` en flex children. Medido por el LEAD: `scrollWidth 390 = viewport` ✅.
- **Corrección intra-intento** (LEAD midió posturas cortadas): `PostureIndicator.css` ≤480px → botones icon-only 40px accesibles (labels con clip-path inset(50%)); fila 418px→174px; 3 botones alcanzables (medido: right máx 354 ≤ 390).
- Gates: tsc 0 · lint:css 0 · 254/254 · build OK.

### Scoring (interactivo)
ARRIBA APROBADO. Nuevo feedback: **"ahora hay que mejorar la parte de abajo: que se extienda, use bien el espacio; mi HUD de vida etc. más compacto; toma libertad para mejorar"** → intento 4 (extensión más allá de maxAttempts=3 AUTORIZADA por el humano en modo interactivo).

---

## <a name="a4"></a>Attempt 4 — 2026-07-04T12:00:46Z — gate: PASS (interactivo; ≥85 las 4)

### Maker (Changed) — deck de 5 filas → 2 bandas, ancho completo
- `PlayerHUD.tsx` + `character.css`: prop opt-in `compact` → franja densa 1 línea (avatar 2.25rem, Silkscreen, barras finas 0.45rem, XP como filete 3px); RegionMap/LocationExplore conservan el default.
- `Combat.tsx/.css`: banda 1 = command bar (HUD compacto flex-1 + clúster derecho AP·stance·"N Passives"·susurro `SPACE end · TAB auto`·AUTO·END TURN); banda 2 = mano borde a borde (cartas 8.5rem en ≥1024px). Fila de hints y fila de controles MUERTAS (`.combat__hints`, `.combat__hint`, `.combat__controls`, token `--cb-info-max-w` retirados, grep verificado). El stage absorbe el vertical liberado (row 1fr).
- Mobile: HUD 2 líneas, econ full-width space-between; overflow cero conservado.
- Gates: tsc 0 · lint:css 0 · 254/254 · build OK.

### Scoring (interactivo)
**PASS (≥85 las 4).** Escena Buriedbornes pixel-arcade completa: panel flotante + enemigo fundido + deck denso de 2 bandas.

### Decisión LEAD
**T-014 marcado `passed` (4 intentos; extensión autorizada).** Notas abiertas no bloqueantes: cap opcional ultra-wide (>1800px) para las cartas; HUD 1 línea en mobile ocultando números; **pipeline de sprites recortados `enemy_cut_<id>.png`** (autorizado por el humano, el código ya lo soporta) — candidato a correr junto a T-013.
