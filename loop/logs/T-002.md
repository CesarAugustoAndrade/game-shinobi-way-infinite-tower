# Log — T-002 · SkillCard muestra el coste de chakra

section: presentation · run: 2026-06-29-A · maxAttempts: 3

---

## <a name="a1"></a>Attempt 1 — 2026-06-29T13:43:27Z — gate: FAIL (91/90/82/95)

### Maker (Changed / Evidence)
- **SkillCard.tsx** — badge de coste de chakra en la columna de badges top-right (`{n} CP` / `FREE`), solo skills no pasivas; eliminó el texto CP redundante del bottom row; `aria-label` añadido.
- **SkillCard.css** — `.skill-card__cost-badge` (+`--free`): chip blocky 2px, `--sw-shadow-sm` (hard, sin blur), `--sw-font-mono` (pixel), color `--sw-chakra-color` (pago) / `--sw-risk-safe-light` (FREE), tamaño = badge LVL hermano. Borró las reglas muertas `.skill-card__cost--cp`/`--cp-zero`.

**Evidence:** tsc 0 · lint:css 0 · build 0 · vitest 163/163.

### Reviews

#### Lens: ARQUITECTURA — Score 91/100 — PASS
Campo `skill.chakraCost` tipado (types.ts:423), sin `any`, render condicional simple, BEM coherente, tokens del design-system, dead code borrado sin referencias colgantes. Gaps menores: (1) **bug latente** `--sw-space-0-5`/`--sw-space-1-5` no existen en `:root` → padding cae a initial (arrastrado de los badges hermanos); (2) cosmético: espacio colgante en className.

#### Lens: SISTEMA — Score 90/100 — PASS
Lee el campo correcto, respeta pasiva/no-pasiva y caso 0→FREE; reachable en Combat.tsx y TreasureHuntReward.tsx; bottom row conserva HP/upkeep; 163 tests verdes. Gap opcional: etiquetar upkeep para TOGGLE.

#### Lens: PRESENTACION — Score 82/100 — FAIL
Chip fiel a tokens pixel-arcade, reutiliza patrón de badges, borra dead code, mejora claridad. FAIL por: (1) **defecto real verificable**: el cost-badge tiene `box-shadow` que `level-badge`/`action-badge` NO tienen → rompe paridad de la columna; (2) sin screenshot no confirma contraste del azul #3b82f6 a 0.5rem ni el apilamiento de 3 badges. Banda 70-84.

#### Lens: BALANCE — Score 95/100 — PASS
Diff 100% UI; muestra `skill.chakraCost` existente sin tocar `src/game/`. Balance intacto by construction.

### Decisión LEAD
FAIL (PRE 82). Intento 2: maker corrige PRE#1 (unificar box-shadow de los 3 badges) + ARQ#1 (definir `--sw-space-0-5`/`--sw-space-1-5` o usar tokens existentes) + ARQ#2 (trim className). El LEAD intentará capturar la SkillCard en combate para confirmar contraste/layout (PRE#2/#3).

---

## <a name="a2"></a>Attempt 2 — 2026-06-29T13:50:24Z — gate: PASS (93/90/88/95)

### Maker (Changed / Evidence)
- **_variables.css** — definió `--sw-space-0-5: 0.125rem` y `--sw-space-1-5: 0.375rem` (arregla el padding fantasma de los badges + margin-top del type-row).
- **SkillCard.css** — `box-shadow: var(--sw-shadow-sm)` añadido a `.skill-card__level-badge` y `.skill-card__action-badge` (paridad de los 3 chips).
- **SkillCard.tsx** — `.trim()` en el className del cost-badge.

**Evidence:** tsc 0 · lint:css 0 · build 0 · vitest 163/163.

### Nota del LEAD sobre screenshot
Intenté navegar a combate con Playwright para capturar la SkillCard, pero las salas de combate están bloqueadas detrás de la exploración (varias salas de juego) — no alcanzable read-only sin jugar la run. Se cortó por riesgo de rabbit-hole. El re-review de PRESENTACION se hizo sin screenshot nuevo, pero el **defecto concreto** que motivó el FAIL ya estaba resuelto en el diff.

### Reviews

#### Lens: ARQUITECTURA — Score 93/100 — PASS
Verificó: `--sw-space-0-5/1-5` ahora existen en `:root` (padding real); los 3 badges comparten `--sw-shadow-sm`; className con `.trim()`; dead code eliminado, literal `#60a5fa` migrado a `--sw-chakra-color`. Descuento menor: `font-size: 0.5rem` literal (sin token de escala obvio).

#### Lens: SISTEMA — Score 90/100 — PASS (carried-forward del intento 1)
El diff del intento 2 (sombra en 2 badges + 2 tokens de spacing + trim className) no añade vectores de sistema; sin lógica de juego; vitest 163/163. Carry-forward registrado por el LEAD.

#### Lens: PRESENTACION — Score 88/100 — PASS
Paridad de sombra RESUELTA (3 chips con `--sw-shadow-sm`), padding real, chip coherente con tokens pixel-arcade ya validados en T-001, claridad mejorada vs texto plano, sin dead code. Residual no-confirmable (contraste exacto del azul a 0.5rem, apilamiento de 3 chips) de riesgo bajo, no penalizado como defecto. Gap opcional no bloqueante: confirmar contraste con screenshot cuando combate sea alcanzable.

#### Lens: BALANCE — Score 95/100 — PASS (carried-forward del intento 1)
Diff sigue 100% UI; muestra `skill.chakraCost` existente sin tocar `src/game/`. Balance intacto by construction. Carry-forward registrado.

### Decisión LEAD
**PASS** (las 4 lentes ≥85). Topic T-002 marcado `passed`. Commiteado por directiva del humano ("commitea todo, sigue con el loop"). Continúa al siguiente pending (T-003).
