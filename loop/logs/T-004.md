# Log — T-004 · Refactor de Combate (Cartas, Posturas, Action Points)

section: combat · run: 2026-06-29-A · maxAttempts: 3 · plan: `plan.md`

Ejecución por **5 fases** (F1..F5). Checkpoint humano antes de F2 (lógica core) y F4 (simulador).

---

## <a name="f1a1"></a>Fase F1 (datos/constantes) · Attempt 1 — 2026-06-29T15:34:44Z — gate: PASS (91/88/92/92)

### Maker (Changed / Evidence)
Aditivo, sin cambio de comportamiento:
- **types.ts** — `enum Posture`; `Skill.apCost?`/`stanceShift?` (opcionales); `DerivedStats.actionPointsPerTurn` (requerido).
- **StatSystem.ts** — pobla `actionPointsPerTurn = AP_BASE + floor(speed/AP_PER_SPEED_DIV)` (único factory de DerivedStats).
- **combat-types.ts** — `CombatState` + `currentAp/maxAp/posture/hand/deck/discard`.
- **CombatWorkflowSystem.ts** — `createCombatState` con defaults inertes.
- **featureFlags.ts** — `AP_BASE:3`, `AP_PER_SPEED_DIV:10`, `HAND_SIZE:4`, `POSTURE_SWITCH_AP_COST:1`, `POSTURE_DRAW_WEIGHTS`.
- **constants/combatCards.ts (NUEVO)** — helpers puros `getCardCategory`/`getApCost`/`weightFor` (consumidos por F2).

**Evidence:** tsc 0 · vitest 163/163 · build 0.

### Reviews
- **ARQUITECTURA 91 PASS** — sin import circular (re-mapeo type-safe Posture→pesos), enums/inmutable/sin `any`, scope contenido a F1 (F2-F5 no tocadas), diff aditivo (0 borrados → sin limpieza prematura). Smell menor: AP defaults como números mágicos en un switch.
- **SISTEMA 88 PASS** — aditivo confirmado por grep (campos solo escritos, nunca leídos); helpers correctos vs `Skill`/`EffectDefinition` reales; umbral de categoría calibrado al dato (Rotation 0.5→defensive, Senbon 0.8→offensive). Reserva: `combatCards.ts` (lógica load-bearing para F2) sin test propio.
- **PRESENTACION 92 PASS** — cero `.tsx`/`.css` tocados; `DerivedStatsPanel` usa lista curada (no itera keys) → el campo nuevo no crea fila. UI idéntica.
- **BALANCE 92 PASS** — simulador (`src/simulation/`) y math base (`calculateDamage`) intactos; nada consume lo nuevo → métricas idénticas al baseline.

### Decisión LEAD
**PASS a la primera.** F1 commiteada. **Loop PARADO para checkpoint humano antes de F2** (lógica core del turno: AP/DeckSystem/posturas/fin de turno — el cambio de comportamiento de mayor riesgo). Gaps menores no bloqueantes anotados (extraer AP defaults a constante; opcional test de `combatCards`).

---

## <a name="f2a1"></a>Fase F2 (lógica core) · Attempt 1 — 2026-06-29T17:03:54Z — gate: FAIL (playtest del LEAD)

### Maker (Changed / Evidence)
Implementó DeckSystem + PostureSystem + test de combatCards (12); AP/stanceShift/postureDamageMod en PlayerTurnSystem; robo de mano en processUpkeep; re-cableado de useCombat (economía AP, changePosture, fin de turno por AP=0/SPACE) y Combat.tsx (mano Z/X/C/V, barra AP, control de postura, SPACE; grids viejos eliminados). Math base intacta.
**Gates del maker:** tsc 0 · vitest **175/175** (163+12) · build 0 · lint:css 0 · simulate:quick corre.

### Gate del LEAD: PLAYTEST (screenshot) — FALLA CRÍTICA
El LEAD navegó al combate real (dev server :3010, clan Lee → sala Combat → Frontal Assault → Engage) y capturó `f2-combat-hand.png`. Resultado:
- ✅ La UI nueva renderiza: barra de AP (5/5 pips), control de postura (AGGRESSIVE/BALANCED/DEFENSIVE), teclas Z/X/C/V · SPACE · TAB.
- ❌ **`HAND • 0 CARDS` / "No cards left — end your turn"** → la mano de apertura NO se robó; el combate es **injugable** (solo se puede pasar turno).

**Por qué los gates verdes no lo atraparon:** no hay test de integración del robo de mano; es un bug de runtime. El playtest del LEAD es el gate que lo detectó (los reviewers read-only no pueden reproducirlo y SISTEMA habría pasado falsamente al ver el código del robo "correcto"). Por eso NO se despacharon los 4 reviewers sobre un build funcionalmente roto.

**Diagnóstico del LEAD (para el maker):** AP=5/5 computa (playerStats presente) pero `hand=0` en el MISMO `combatState` → `opening.hand` se robó vacía → `buildDeck(preparedPlayer.skills)` salió vacío al robar. `Combat.tsx:132` usa `?? card`, así que NO es drop por resolución de id; el prop `hand` llega vacío de verdad. Un jugador nuevo tiene ≥2-3 skills no-PASSIVE (App.tsx:496), así que el mazo no debería estar vacío. Sospechas: el player pasado a `startCombat` (`playerAfterCosts`/`preparedPlayer`) tiene `.skills` vacío en ese instante, o el redraw del upkeep pisa la mano. Requiere depuración en runtime.

### Decisión LEAD
**FAIL (intento 1/3).** Se devuelve al maker con repro + screenshot + diagnóstico para el intento 2. No se commitea F2 hasta que el combate sea jugable y pase el gate.

---

## <a name="f2a2"></a>Fase F2 · Attempt 2 — 2026-06-29T17:27:47Z — gate: FAIL (89/82/85/88)

### Maker (Changed / Evidence)
Intento 2 = fix del bug de mano vacía (2 sub-pasos):
- **Camino sala+approach:** `handleBranchingApproachSelect` (App.tsx) enrutado por el único `startCombat` (antes llamaba `createCombatState` con defaults vacíos y el upkeep robaba de mazo vacío → AP 5/5 pero mano 0). Imports/bloque inline muertos eliminados.
- **Camino evento:** `handleEventChoice` (useActivityHandlers, rama ENABLE_MANUAL_COMBAT) enrutado también por `startCombat` con shim neutro FRONTAL_ASSAULT. `setEnemy/setTurnState` muertos eliminados del hook.
**Gates:** tsc 0 · vitest 175/175 · build 0 · lint:css 0 · simulate:quick corre.

### Playtest del LEAD (verificación del fix) — JUGABLE ✅
Capturas `f2v2-combat.png` (abre con HAND • 3 CARDS, AP 5/5, postura, Z/X/C/V) y `f2v2-afterplay.png` (jugar Z → enemigo 122→101, AP 5/5→3/5, mano 3→2). Bucle AP/cartas verificado end-to-end. Ambos caminos siembran la mano.

### Reviews
- **ARQUITECTURA 89 PASS** — DeckSystem/PostureSystem puros, separación dual intacta, dedup de los 2 caminos en `startCombat` (mejora real, sin dead code), restos legacy (`TurnPhaseState`) aislados para F5. Smell menor: `terrain: any` heredado.
- **SISTEMA 82 FAIL** — mecánica correcta, cableada, alcanzable (playtest), sin regresión (175). FALLA por: (1) `DeckSystem`/`PostureSystem` sin tests (la banda ≥85 exige edge cases en test); (2) **DEFENSIVE estrictamente peor** (−15% saliente sin beneficio, `postureDefenseMod` no consumido, EnemyTurn congelado) → viola "trade-off legible".
- **PRESENTACION 85 PASS** — UI funcional y legible (screenshots), reutiliza SkillCard, coherente-base con pixel-arcade. Gaps F3: solape título/atajo en cartas; ancho del valor de daño. ("Scene Background" es placeholder preexistente.)
- **BALANCE 88 PASS** — math base byte-idéntica, simulador sin tocar (válido hasta F4). Nota: baseline con win-rates extremos PRE-existentes (varias clases 1.0, Glass Cannon 0.38) — no de F2.

### Decisión LEAD
**FAIL (intento 2/3).** Gaps para intento 3: (a) FIX gameplay — que DEFENSIVE no sea estrictamente peor (neutralizar el −daño hasta F4 o cablear el −daño-entrante); (b) tests de DeckSystem/PostureSystem (requiere autorización del humano por CLAUDE.md — pendiente de consultar). Checkpoint pre-F4: se consulta al humano.
**Decisiones del humano:** (1) sí, añadir los tests; (2) cablear el −daño-recibido ahora.

---

## <a name="f2a3"></a>Fase F2 · Attempt 3 — 2026-06-29T18:04:49Z — gate: PASS (93/90/85/88)

### Maker (Changed / Evidence)
- **Tests (autorizados):** `deckSystem.test.ts` (12) + `postureSystem.test.ts` (10) → **197/197**. Cubren robo ponderado (Math.random mockeado), reshuffle por invariante de conteo de ids, deck<HAND_SIZE, mods de postura y la reciprocidad deal==take.
- **Fix DEFENSIVE:** `postureDefenseMod(combatState.posture)` cableado en `EnemyTurnSystem.executeEnemyAction` (ataque directo del enemigo, tras `applyMitigation` — base intacta). Trade-off simétrico: AGGR ×1.15/×1.15, BAL ×1.0, DEF ×0.85/×0.85. DoT/terreno/reflexión ×1 hasta F4 (acotado, documentado).

**Evidence:** tsc 0 · vitest **197/197** · build 0 · simulate corre. Math base intacta.

### Reviews (re-review de las 2 lentes que importaban; PRE/BAL carried del intento 2)
- **ARQUITECTURA 93 PASS** — `postureDefenseMod` ya consumido (smell resuelto; los 5 exports de PostureSystem usados); modificador externo tras mitigación, separación dual intacta; tests con patrón del repo sin `any`; sin dead code nuevo.
- **SISTEMA 90 PASS** — ambos gaps cerrados con evidencia: edge cases fijados por test; DEFENSIVE con −daño-recibido real (reciprocidad bloqueada por test). Mecánica correcta/alcanzable/sin regresión. (No llega a 95+ porque el −entrante solo cubre el ataque directo; DoT/terreno a F4.)
- **PRESENTACION 85 PASS** (carried — el intento 3 no tocó UI). Gaps F3 anotados: solape título/atajo, ancho del daño.
- **BALANCE 88 PASS** (carried — el simulador usa su propio `executeEnemyTurn`; el mod de daño entrante solo afecta el juego real, no el sim → baseline sin cambios hasta F4).

### Decisión LEAD
**PASS (las 4 ≥85).** F2 commiteada. **Loop PARADO para checkpoint humano antes de F4** (reescritura del simulador — el otro punto de alto riesgo). Pendientes acumulados para fases siguientes: F3 (pulido UI: solape de cartas, Hand/PostureIndicator), F4 (simulador modela AP/cartas/posturas + valida ventanas de balance + DoT/terreno con postura), F5 (limpieza de `TurnPhaseState` y restos legacy).

---

## <a name="f3a1"></a>Fase F3 (pulido UI) · Attempt 1 — 2026-06-29T19:32:12Z — gate: FAIL (84/93/83/88)

### Maker (Changed / Evidence)
Pulido + extracción: fix solape título/atajo (padding-left) y ancho del daño en `SkillCard`; extracción de `Hand.tsx`(+css) y `PostureIndicator.tsx`(+css) desde `Combat.tsx` (−263 líneas, dead imports/CSS borrados); pips de AP pulidos a pixel-arcade; PostureIndicator con iconos + tooltip de trade-off; hack `|| false` reemplazado por boolean estricto. Sin lógica tocada.
**Gates:** tsc 0 · lint:css 0 · build 0 · vitest 197/197.

### Playtest del LEAD — JUGABLE ✅ (con 1 defecto preexistente)
`f3-combat.png` (postura con iconos, pips pulidos, badge-overlap arreglado, daño 138 sin colisión) y `f3-afterplay.png` (jugar Z → enemigo 134→80, AP 5/5→3/5, mano 3→2). La `Hand` extraída funciona end-to-end.

### Reviews
- **ARQUITECTURA 84 FAIL** — extracción de libro (−263 líneas, sin duplicación, dead imports/CSS borrados, `HAND_SHORTCUTS` single-source, hack `|| false` bien tipado). Único defecto: `Hand.tsx` declara prop `currentAp` con JSDoc que afirma controlar el dimming pero **nunca la usa** (dead prop + comentario engañoso).
- **SISTEMA 93 PASS** — extracción provadamente behavior-preserving (`Boolean()` truthiness-idéntico; gate de Hand equivalente por `turnState` de 2 valores; teclado intacto; playtest-verificado). 197 tests.
- **PRESENTACION 83 FAIL** — todo el pulido F3 aterriza (overlap, ancho daño, pips, postura, extracción). FALLA por el **título fantasma**: la `<img alt={skill.name}>` decorativa de fondo (`SkillCard.tsx:136-140`) pinta su alt-text (assets 404) detrás del `<h3>` → "Taijutsu Taijutsu". **Preexistente (F2)**, no de F3, pero en la superficie que F3 pulió.
- **BALANCE 88 PASS** (carried — math de combate intacta, `git diff` solo presentación).

### Decisión LEAD
**FAIL (intento 1/3).** Gaps para intento 2 (ambos quirúrgicos): (a) PRE — `SkillCard.tsx` img decorativa `alt=""` + `aria-hidden` (mata el fantasma + arregla el accessible name); (b) ARQ — quitar la prop muerta `currentAp` de `Hand.tsx` (interface+JSDoc+destructuring).

---

## <a name="f3a2"></a>Fase F3 · Attempt 2 — 2026-06-29T19:39:04Z — gate: PASS (90/93/89/88)

### Maker (Changed / Evidence)
2 fixes quirúrgicos:
- **PRE:** `SkillCard.tsx` img decorativa de fondo → `alt=""` + `aria-hidden="true"` + `onError` que oculta el nodo (mata el título fantasma del alt-text y el accessible name duplicado, de forma determinista).
- **ARQ:** eliminada la prop muerta `currentAp` de `Hand.tsx` (interface+JSDoc+destructuring) y su paso desde `Combat.tsx` (el `currentAp` de escena se conserva — lo usan pips/canUseSkill/PostureIndicator).
**Gates:** tsc 0 · lint:css 0 · build 0 · vitest 197/197.

### Reviews (re-review PRE+ARQ; SIS 93 / BAL 88 carried del intento 1)
- **ARQUITECTURA 90 PASS** — `grep currentAp Hand.tsx` = 0; sin referencias colgantes; resto de la extracción limpio.
- **SISTEMA 93 PASS** (carried — el intento 2 solo tocó alt + quitó prop muerta, sin lógica).
- **PRESENTACION 89 PASS** — fantasma eliminado de forma determinista (alt vacío = sin texto que renderizar); resto del pulido ya validado por screenshots. Nota no bloqueante: queda `alt="Scene Background"` preexistente en el viewscreen (otro componente, topic futuro).
- **BALANCE 88 PASS** (carried — math intacta).

### Decisión LEAD
**PASS (las 4 ≥85).** F3 commiteada. **Loop PARADO para checkpoint humano antes de F4** (reescritura del simulador). Pendientes: F4 (simulador modela AP/cartas/posturas + DoT/terreno con postura + re-valida ventanas de balance), F5 (limpieza `TurnPhaseState` y restos legacy). Candidatos a topics futuros anotados por reviewers: `alt` del viewscreen; win-rates extremos pre-existentes del baseline.

---

## <a name="f4a1"></a>Fase F4 (simulador) · Attempt 1 — 2026-06-29T21:08:18Z — gate: PASS (91/91/92/89)

### Maker (Changed / Evidence)
Reescribió el turno del jugador del sim a la economía AP/cartas/posturas, REUTILIZANDO los módulos puros del juego (`DeckSystem.buildDeck/drawNewTurnHand`, `PostureSystem`, `combatCards.getApCost`, `calculateDamage`): AP/turno, robo ponderado, jugar hasta agotar AP, `postureDamageMod` saliente + `postureDefenseMod` entrante, postura BALANCED, `stanceShift` al jugar, `liveSkill()` resolver (deck/hand/discard = particiones puras). `SkillSelectionAI.selectBestCard` (filtra mano por AP). Métricas de AP nuevas. Borró el fallback legacy (`basic_atk`/`fallbackSkill`). Enemigo intacto (1 acción/turno).
**Evidence:** tsc 0 · vitest 197 · build 0 · simulate:quick y :progression:quick corren.
**Balance:** comparable al baseline (Glass Cannon 0.37→~0.63 mejoró; resto comparable; ninguna clase de sana a extrema). `basic_atk` 0-uso es PRE-existente (git-stash A/B). Sin skills muertas nuevas. Extremos absolutos (presets 1.0, dip de Lee por chakra-burst) → T-006.

### Reviews
- **BALANCE 89 PASS** — fidelidad confirmada (sim reutiliza módulos reales, math congelada); sin regresión; sin skills muertas nuevas. Extremos absolutos fuera de alcance (T-006).
- **SISTEMA 91 PASS** — el turno del sim espeja `useCombat`/`PlayerTurnSystem` (orden, postureMods, isFirstTurn); selección de carta correcta (filtro AP, null-to-end, `liveSkill` evita snapshots viejos); enemigo y 197 tests intactos.
- **ARQUITECTURA 91 PASS** — reutilización sin duplicación/divergencia; math base intacta; dead code legacy borrado sin huérfanos; métricas tipadas; sin `any` nuevo. Smell menor: acoplamiento snapshot↔vivo de `liveSkill()` (documentado).
- **PRESENTACION 92 PASS** — cero `.tsx`/`.css` tocado; solo el reporte CLI (sección AP Economy on-pattern). UI de juego idéntica.

### Decisión LEAD
**PASS a la primera.** F4 commiteada. **Loop PARADO para checkpoint humano antes de F5** (última fase: limpieza legacy `TurnPhaseState`/`sideActionLimit` y restos del modelo viejo). Carry-forwards a T-006 (balance): tunear presets en 1.0, dip de Lee/chakra-burst; opcional IA de cambio de postura en el sim.

---

## <a name="f5a1"></a>Fase F5 (limpieza legacy) · Attempt 1 — 2026-06-30T08:41:05Z — gate: PASS (95/95/92/92)

### Maker (Changed / Evidence)
Borrado PURO (78 deletions, 0 insertions, 2 archivos), grep-verificado 0 consumidores:
- **types.ts** — interface `TurnPhaseState`, factory `createInitialTurnPhaseState`, campo `Skill.sideActionLimit`.
- **Combat.css** — bloque huérfano de la phase-bar SIDE/MAIN (10 clases `.combat__phase-*`/`.combat__side-actions*`).
- **Conservado:** `ActionType` (categoría de carta, 171 usos) y `upkeepProcessedThisTurn` (useState del flujo nuevo, distinto del borrado).

**Evidence:** tsc 0 · vitest 197/197 (sin tocar tests) · build 0 · lint:css 0 · simulate corre.

### Reviews (todos PASS, los scores más altos del épico)
- **ARQUITECTURA 95 PASS** — borrado quirúrgico, 0 referencias colgantes, ActionType intacto, cero restos del modelo viejo, mejora la salud del código.
- **SISTEMA 95 PASS** — comportamiento idéntico (borrado de tipos/CSS sin uso); 197 tests verdes sin tocarse.
- **PRESENTACION 92 PASS** — CSS muerto sin referencias; UI byte-idéntica; sin llaves huérfanas.
- **BALANCE 92 PASS** — cero archivos de balance/sim tocados; métricas idénticas.

### Decisión LEAD
**PASS a la primera.** F5 commiteada. **T-004 COMPLETO (5/5 fases).** Topic marcado `passed`. Carry-forwards documentados a T-006 (balance) y topics futuros (alt del viewscreen; comentarios obsoletos en skills.ts).
