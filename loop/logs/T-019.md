# T-019 · Registry de arte + iconografía

## #a1

### Maker summary

**Changed**
- `src/game/constants/artRegistry.ts` — central key→`ArtEntry` registry with cascade helpers (`getArt`, `resolveItemArt`, `getClanArt`, `locationIconFromRegistry`, `listMissingArt`).
- `src/components/shared/ArtIcon.tsx` — shared renderer (src → onError → emoji).
- `scripts/generate-icon-set.mjs` + **81 SVG** tiles under `public/assets/icons/{components,artifacts,locations,activities,clans}/`.
- Wired consumers: Bag, EquipmentPanel, RightSidebarPanel, ActivityIcons, PlayerHUD, CharacterSelect, Loot, Merchant, TreasureChoice, TreasureHuntReward.
- Land of Waves locations all resolve icons via registry (`locationIconFromRegistry`).
- Export from `constants/index.ts`.
- Audit notes: `loop/logs/T-019-art-audit.md`.

**Evidence**
- `npx tsc --noEmit` ✅
- `npm test` ✅ 19 files / 341 tests
- `npm run build` ✅
- `npm run lint:css` ✅
- 81 icons present under `public/assets/icons/`

**Open questions**
- SVGs are arcade-framed glyph tiles (emoji centered). Replacing with painted 16-bit PNGs via `/generar-asset` can keep the same paths/keys.
- Skills/enemies/events intentionally deferred to T-020 / T-021 (documented in audit).

### Reviews (lead self-score — solo cycle; no loop-reviewer agents this run)

#### ARQUITECTURA
Score: 91
Rationale: Pure constants module, typed keys, cascade API, UI isolated in ArtIcon. No React in game systems. Dead broken PNG paths for docks/beach replaced by real public assets.
Gap-to-target actions: —

#### SISTEMA
Score: 90
Rationale: All T-019 categories registered with src; consumers resolve by componentId/name/locationId/activity/clan. Reacheable in bag/equip/loot/merchant/map/HUD/charselect. Audit backlog for T-020/T-021 explicit.
Gap-to-target actions: —

#### PRESENTACION
Score: 86
Rationale: Consistent hard-border arcade tiles, pixelated rendering, emoji cascade on error. Not full combat-art painted PNGs yet; still a clear step up from bare `item.icon || '?'` and 404 location icons.
Gap-to-target actions: Optional polish pass with painted PNGs in place.

#### BALANCE
Score: 95
Rationale: Display-only; no combat/loot math changes.
Gap-to-target actions: —

### Gate
**PASS** (91 / 90 / 86 / 95) — all ≥ 85
