# Log — T-016 · Limpieza de campos muertos del motor de eventos

section: architecture · run: 2026-07-02-A · modo: auto · reviewers en Sonnet (directiva del humano: LEAD en Fable, ejecución en Sonnet)

---

## <a name="a1"></a>Attempt 1 — 2026-07-03T21:33:55Z — gate: PASS (93/90/88/85)

### Maker (Changed)
El maker (agente `maker-T016`) no entregó reporte por mensaje (solo idle pings); el LEAD verificó el trabajo directamente del diff en stage (10 archivos, +153/−66). Decisiones por campo:

| Campo | Decisión | Detalle |
|-------|----------|---------|
| `EventOutcome.effects.items` | RETIRAR | Borrado de `types.ts` + coloreado de preview en `Event.tsx`. Sin ruta viva de otorgar objetos. |
| `EventOutcome.effects.skills` | RETIRAR | `grantSkillById` queda como única vía viva. |
| `GameEvent.rarity` | IMPLEMENTAR | Ahora pondera la aparición: `EVENT_RARITY_WEIGHTS` (constants: COMMON 100 / RARE 45 / EPIC 18 / LEG·CURSED 7) + funciones puras `getEventSelectionWeight` / `selectWeightedEvent(events, roll)` en `EventSystem.ts`, cableadas en `LocationSystem.generateEventActivity` (reemplaza pick uniforme). |
| `EventChoice.clanBonus` | RETIRAR | Era no-op matemático (escalaba todos los pesos y renormalizaba). Vía viva de clan: `requirements.requiredClan`. |

Además: `EventSystem.test.ts` (test clanBonus → normalización ≠100; suite nueva `rarity-weighted event selection (T-016)` con 4 casos), `.agents/skills/event-creator/SKILL.md` sincronizada (Notas de discrepancia #2/#4/#5 → Resuelto), CHANGELOG bajo [Unreleased].

### Evidence (gates del LEAD)
- `npx tsc --noEmit` → 0 errores.
- `npm test` → 15 suites, 252/252 (antes 248; +4 rarity).
- Grep en `src/**` (incl. `constants/events/**`): 0 referencias restantes a `clanBonus` / `effects.items` / `effects.skills`.

### Reviews (4× loop-reviewer, Sonnet)

#### ARQUITECTURA — Score: 93/100 — PASS
Dead code confirmado eliminado (grep exit 1 en toda la base); `EVENT_RARITY_WEIGHTS` en constants (pilar balance-data-in-constants); funciones puras sin React, roll inyectado → testeable; `rollOutcome` sin la copia mutada del bloque clanBonus; sin `any` en producción; enums correctos; SKILL.md sincronizada. Smell menor: `_player` en `rollOutcome` como forward-compat especulativo (documentado en JSDoc). Gap-to-target: (ninguna).

#### SISTEMA — Score: 90/100 — PASS
Mecánica cableada y alcanzable (traza `generateActivityForKey` → `generateEventActivity` → `selectWeightedEvent`, LocationSystem.ts:395-397); campos retirados sin uso vivo; bordes cubiertos por test (pool vacío, rarity ausente→COMMON, roll 0/0.5/0.99, pesos ≠100, distribución N=2000); cumple RULES.md·SISTEMA (tests junto a la lógica pura load-bearing). Gap-to-target: 1) añadir caso determinista `roll=1.0` en `EventSystem.test.ts` para cubrir la rama fallback por aserción.

#### PRESENTACION — Score: 88/100 — PASS
Único toque de UI: 2 ramas muertas eliminadas de `getOutcomeType` (Event.tsx); ningún evento real usaba esos campos → ningún outcome se reclasifica; `formatOutcomeText` nunca los referenció. Cambio puramente sustractivo, sin riesgo de regresión visual. Gap-to-target: (ninguna; los puntos no capturados son conservadurismo por ausencia de screenshot).

#### BALANCE — Score: 85/100 — PASS
Combate intacto (archivos de matemática ausentes del diff); audit del contenido: 25 eventos, 22 declaran rarity (6 COMMON + 14 RARE + 1 EPIC), 3 legacy → COMMON, nadie suprimido; LEGENDARY/CURSED inertes (0 eventos); retiro de clanBonus verificado como no-op matemático; agencia intacta (todo evento alcanzable, peso > 0). Outlier menor pre-existente que T-016 hizo visible: el pool `genericEvents` (3 RARE + 1 EPIC, 0 COMMON) hace que `hidden_shrine_blessing` (EPIC) salga a ~11.8% del pool en vez del ~7% implícito. Gap-to-target: 1) anclar `genericEvents` con ≥1 evento COMMON — **derivado a T-012** (pase de tuning designado).

### Decisión LEAD
**PASS (93/90/88/85). T-016 marcado `passed`.** Diff en stage, pendiente de decisión del humano (commit a develop). Advisory heredada para T-012: anclar genericEvents con un COMMON + considerar el test de borde roll=1.0.
