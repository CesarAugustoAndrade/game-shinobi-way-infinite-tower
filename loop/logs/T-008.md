# Log — T-008 · Motor de Eventos 2.0 (Cadenas, Flags y Efectos)

section: architecture · run: 2026-06-29-A · modo: auto

---

## <a name="a1"></a>Attempt 1 — 2026-07-01T11:30:40Z — gate: PASS (91/89/87/90)

### Maker (Changed / Evidence)
Extensión ADITIVA del motor de eventos (ningún evento/test existente tocado salvo ampliar el test file):
- **`types.ts`:** `Player.eventFlags: Record<string,number>` (requerido); `EventOutcome.effects` +opcionales `chainTo`/`setFlags`/`grantSkillById`/`curse {value?,duration?}`/`removeRandomItem`; `requiresFlags`/`excludesFlags` en `GameEvent` y `EventChoice`.
- **`EventSystem.ts`** (puro, inmutable): `checkEventFlags`, `isEventAvailableForPlayer`, `getAvailableEventsForPlayer`, `getAvailableChoices`; `applyOutcomeEffects` (setFlags merge, grantSkillById dedupe, curse = Buff `EffectType.CURSE` → pipeline de mitigación existente, removeRandomItem con PRNG `pick`); `resolveEventChoice` gatea por flags y devuelve `nextEventId` (=chainTo).
- **Factories:** `eventFlags: {}` en Player.ts, App.tsx, BattleSimulator.ts, testFixtures.ts.
- **Cableado:** `useActivityHandlers.handleEventChoice` abre el siguiente evento en cadena (setActiveEvent + GameState.EVENT), completa la sala solo en el eslabón final; flags persisten vía el `player` en setPlayer. `LocationSystem.generateEventActivity` filtra la oferta por flags. `Event.tsx` oculta choices gateados (useMemo).
- **Tests:** `EventSystem.test.ts` +26 (48 en el archivo; 232 en total).

**Evidence:** tsc 0 · vitest **232/232** · build OK. Diff: 10 archivos, +480/−12, todo `src/`. No se corrió simulate (architecture); BALANCE lo re-corrió igual (verde, sin regresión).

### Reviews (attempt 1)
- **ARQUITECTURA 91 PASS** — aditivo, tipos opcionales (eventos existentes tipan sin cambios), `EventSystem` puro/inmutable, funciones componibles, `curse` reusa `EffectType.CURSE`, reachability real en los 3 puntos de consumo (offering/choices/chaining), `eventFlags` en las 4 factories. Sin `any` de producción nuevos. Sin gaps.
- **SISTEMA 89 PASS** — mecánica alcanzable en rutas vivas: cadenas (resolveEventChoice→nextEventId→useActivityHandlers abre el siguiente, sala se completa al final), flags persistidos antes del branch de gating, curse fluye por el pipeline de mitigación real, grantSkillById no rompe el deck de T-004 (buildDeck usa todo el loadout, dedupe verificado), removeRandomItem con PRNG del juego + no-op en vacío. 26 tests de comportamiento con inmutabilidad, 0 regresión.
- **PRESENTACION 87 PASS** — solo `Event.tsx` toca UI (lógica de filtrado, sin CSS/estilo), build verde, no-op sobre el contenido actual (0 choices gateados), teclado y render comparten la lista filtrada (sin off-by-one).
- **BALANCE 90 PASS** — balance-neutral: no toca constantes/skills/enemigos/config ni math de combate; efectos nuevos inertes sin data (T-010). Sim re-corrido verde, métricas vs BALANCED idénticas pre/post. `curse` default 0.5 == convención existente.

### Gap unánime (PRE + SIS, no bloqueante) — guardia anti-softlock de choices
`Event.tsx:~353`: `availableChoices` puede quedar `[]` si un evento futuro gatea TODOS sus choices → evento sin opciones seleccionables. El maker creyó haber puesto el fallback pero solo está a nivel de evento (`LocationSystem`), no de choice. Inofensivo hoy (0 eventos gatean choices). Fix de 1 línea (ambos reviewers lo dieron): `const availableChoices = gated.length ? gated : activeEvent.choices;`.
Advisories de BALANCE para T-012 (al aterrizar contenido): gatear `grantSkillById` (rareza/coste), tunear `curse`/`removeRandomItem` como trade-off legible.

### Decisión LEAD
**PASS (91/89/87/90).** T-008 marcado `passed`. PRE+SIS coincidieron en un hueco de robustez trivial (guardia anti-softlock de choices) a blindar antes de que T-010/T-011 añadan choices gateados.

**Decisión del humano:** aplicar la guardia y commitear. **Guardia aplicada** por el LEAD (fix exacto de los reviewers) en `Event.tsx`: `const availableChoices = gated.length > 0 ? gated : activeEvent.choices` → un evento nunca abre sin choices seleccionables aunque el contenido futuro gatee todos. Re-gates verdes (tsc 0 · test 232/232 · build OK). T-008 commiteado (commit único, autorizado por el humano).