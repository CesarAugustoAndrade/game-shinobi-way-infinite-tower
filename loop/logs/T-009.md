# Log — T-009 · Skill local event-creator + section 'events'

section: architecture · run: 2026-06-29-A · modo: auto

---

## <a name="a1"></a>Attempt 1 — 2026-07-01T15:12:27Z — gate: PASS (88/90/91/90)

### Maker (Changed / Evidence)
Autoría de skill (documentación pura, sin tocar `src/**`), apoyada en `skill-creator`. Documenta el modelo de eventos REAL post-T-008 para que el maker escriba contenido en T-010.
- **`.agents/skills/event-creator/SKILL.md`** — frontmatter válido + body: cuándo usarla, modelo de datos REAL (tablas de `GameEvent`/`EventChoice`/`EventOutcome`/`effects` con los campos de T-008), 8 reglas de oro, checklist de registro (en `EVENTS`), errores comunes/YAGNI, y **Notas de discrepancia** (código vs brief).
- **`.agents/skills/event-creator/references/templates.md`** — 3 plantillas TS copiables (evento simple, cadena multi-escena `chainTo`+`setFlags`, evento gateado `requiresFlags`), tipando contra el modelo real (`phoenix_flower` es un `Skill.id` real).

**Evidence:** tsc 0 · vitest 232/232 (sin cambios de código). El maker verificó la semántica contra `EventSystem.ts`/`useActivityHandlers.ts` y documentó 4 discrepancias reales (agregador `EVENTS` no `ALL_EVENTS`; `effects.items`/`skills` declarados pero no aplicados; `intelGain` en el cierre; `rarity` no pondera).

### Reviews (attempt 1)
- **ARQUITECTURA 88 PASS** — fidelidad altísima: ~20 claims de campo verificados vs `types.ts`, semántica vs `EventSystem.ts`, las 4 discrepancias ciertas, plantillas fieles (import/id reales), formato a la par de `combat-art`. 2 imprecisiones menores flagged.
- **SISTEMA 90 PASS** — reglas de oro, tabla de efectos y las 3 plantillas coinciden con el comportamiento real (resolveEventChoice/applyOutcomeEffects/getAvailableChoices); producirían contenido mecánicamente válido (pesos=100, chainTo a id existente, gating coherente, salida SAFE). 2 correcciones de precisión flagged.
- **PRESENTACION 91 PASS** — doc bien estructurado y escaneable, tablas legibles (pipes escapados), plantillas en bloques ```typescript copiables, frontmatter válido; a la par de `combat-art`. Sin gaps.
- **BALANCE 90 PASS** — la guía de balance es sana y alineada con VISION (agencia: siempre un choice SAFE; trade-off legible; riesgo-recompensa); difiere el tuning fino a T-012 sin sobre-especificar; plantillas con valores razonables. Nota pedagógica menor.

### Correcciones de precisión aplicadas por el LEAD (post-review, doc-only)
Los reviewers (ARQ+SIS coincidiendo en #1) flaggearon 3 imprecisiones del doc; como la fidelidad ES el valor de una skill de doc, se corrigieron:
1. `SKILL.md` puntero de fuente-de-verdad: `getEventForArc` (inexistente) → `generateEventActivity` (`LocationSystem.ts:378`) + `getAvailableEventsForPlayer`.
2. `clanBonus`: el gloss decía que sesga; `rollOutcome` escala todos los pesos por el mismo factor y renormaliza → **no-op** de probabilidad. Corregido + añadido como discrepancia #5.
3. `intelGain`: **reemplaza** el default de la sala (`INTEL_GAIN.EVENT_DEFAULT`), no se suma. Corregido.

### Discrepancias del código surgidas (advisory para el humano — posibles topics)
El maker/reviewers destaparon smells reales (documentados, NO tocados por ser doc-only): `effects.items`/`effects.skills` declarados pero sin aplicar; `GameEvent.rarity` no pondera la selección; `clanBonus` es no-op. Candidatos a topics de arquitectura si se quieren implementar/retirar.

### Decisión LEAD
**PASS (88/90/91/90). T-009 marcado `passed`.** Correcciones de precisión aplicadas (doc-only, sin gates que re-correr). Se presenta al humano para commit + decisión sobre levantar topics para las discrepancias de código. No auto-commit.