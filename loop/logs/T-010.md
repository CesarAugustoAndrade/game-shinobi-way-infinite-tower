# Log — T-010 · Contenido de Eventos: relleno de arcos + cadenas

section: events · run: 2026-06-29-A · modo: auto

---

## <a name="a1"></a>Attempt 1 — 2026-07-01T18:51:16Z — gate: PASS (95/88/91/90)

### Maker (Changed / Evidence)
Contenido de eventos nuevo, guiado por la skill `event-creator` (T-009) sobre el motor de T-008. Solo DATOS (no tocó `EventSystem.ts`, tipos ni el agregador `EVENTS`).
- **`warArcEvents.ts`** WAR 2→6: **Cadena A** (`reanimated_envoy` → `reanimated_envoy_fate`, branch por flags `envoy_freed`/`envoy_bound`); **consecuencia persistente** `envoy_gratitude_repaid` (`requiresFlags: envoy_freed`, `excludesFlags: envoy_debt_settled`, dispara una vez); standalone `scavengers_field` con `removeRandomItem`.
- **`rogueArcEvents.ts`** ROGUE 3→5: **Cadena B** (`orochimaru_experiment` → `orochimaru_experiment_result`, branch `subject_freed`/`subject_harvested`).
- **`events/__tests__/eventContent.test.ts`** (nuevo): 16 tests de integridad sobre TODO el pool (ids únicos, pesos=100/choice, `chainTo` a id real, `chainTo` XOR `triggerCombat`, `grantSkillById`∈SKILLS, sin flag-gates muertos, ≥1 choice SAFE ungated) + smoke que resuelve las cadenas y ramifica por flag.

**Evidence:** tsc 0 · vitest **248/248** (eventContent 16/16; EventSystem 48/48 intacto). Skills otorgadas (`suijinheki`/`dragon_flame`/`poison_fog`) son ids reales de `skills.ts`. Efectos nuevos usados: `grantSkillById`, `curse` (0.4-0.5/3), `removeRandomItem`.

### Reviews (attempt 1)
- **ARQUITECTURA 95 PASS** — estrictamente aditivo (2 arrays + test file), `types.ts`/`EventSystem.ts`/agregador con diff vacío, enums correctos, sin `any`, test file co-locado que guarda contra chains/flags muertos. Ejemplar.
- **SISTEMA 88 PASS** — cumple todos los requisitos (2 cadenas ramificadas, consecuencia persistente leída por evento posterior, 3 efectos con trade-off), reachability cableada, 16 tests no tautológicos, 0 regresión. Menor: las escenas-2 carecen de `excludesFlags` → pueden reaparecer standalone tras la cadena (repetición de bajo valor, sin softlock).
- **PRESENTACION 91 PASS** — prosa evocativa y sin errores, tono coherente con cada arco, cadenas que se leen como historia continua, payoff persistente explícito, uso correcto de "Ryō".
- **BALANCE 90 PASS** — trade-offs legibles, agencia preservada (SAFE ungated en cada evento, verificado por test), castigos (curse real que persiste fuera de combate, HP, removeRandomItem) emparejados a recompensa; skills fuertes bien gateadas por stat+riesgo+prob+curse; montos dentro del sobre existente.

### Advisories para T-012 (balance de eventos) — no bloqueantes
- Vías "freed/mercy" de ambas cadenas encadenan a recompensa 100% limpia sin coste (riesgo efectivo ~nulo); bajar la etiqueta a LOW o añadir un peso de fallo menor. `envoy_gratitude_repaid` (SAFE ryo 450+exp 100+intel 20) es payoff diferido de riesgo cero — confirmar que el combo freed→debt no supera la vía HIGH/EXTREME en ryo total.
- (Hygiene, T-012) dar `excludesFlags`/`requiresFlags` de ciclo-de-vida a las escenas-2 para que no reaparezcan standalone tras la cadena.

### Decisión LEAD
**PASS (95/88/91/90). T-010 marcado `passed`.** Advisories a T-012 (que es justo el pase de balance de eventos). Se commitea a `develop` (patrón de la sesión); el merge develop→main lo conserva el humano.