# T-021 · Retratos de enemigos + ilustraciones de eventos

## #a1

### Maker summary

**Changed**
- `scripts/generate-enemy-event-art.mjs` — generates enemy/event tiles + manifests.
- `enemyArtManifest.ts` / `eventArtManifest.ts` — auto catalogs.
- `artRegistry.ts` — registers enemy/event keys; `getEnemyArt` / `resolveEnemyImageSrc` / `getEventArt` with cascade:
  - Enemy: pool → boss keyword → job keyword → archetype → emoji
  - Event: dedicated id → category → emoji
- `EnemySystem.ts` — assigns `image` via registry (no hard-coded map only; **offline, no GenAI required**).
- `Combat.tsx` — removed `/assets/image_3b2b13.jpg` fallback; uses archetype SVG.
- `Event.tsx` + `Event.css` — illustration banner from `getEventArt`.
- Painted enemy PNGs (+ cutouts) copied to `public/assets/`.
- SVG tiles: `public/assets/icons/enemies/*`, `public/assets/icons/events/*`.

**Coverage**
- Enemy registry entries: **52** (5 archetypes + 4 jobs painted + 2 bosses painted + 40 Waves pool ids)
- Event entries: **31** (26 event ids + 5 category fallbacks)
- All with `src` present.

**Evidence**
- `tsc` ✅ · `npm test` 341 ✅ · `build` ✅ · `lint:css` ✅

**Open questions**
- Location `enemyPool` ids still not used by `generateEnemy` (name/job generation). Pool art is registered for future CONNECT when pools drive spawns.
- GenAI `generateEnemyImage` remains optional polish; combat no longer depends on it.
- SVG tiles are arcade placeholders; painted combat-art can replace in place.

### Reviews (lead self-score)

| Lente | Score |
|-------|-------|
| ARQUITECTURA | 90 |
| SISTEMA | 88 |
| PRESENTACION | 86 |
| BALANCE | 95 |

### Gate
**PASS** (90 / 88 / 86 / 95)
