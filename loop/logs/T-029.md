# T-029 · Combate sin fallback SVG + cascade de arte

## #a1 — PASS

### Maker summary

**Context (Mode B discovery post T-028)**
- After CERO SVG, `Combat.tsx` still fell back to `archetype_*.svg` → 404 stage art.
- SkillCard hid failed images with no emoji cascade.

**Done**
1. `Combat.tsx`: `enemyImage` = `enemy.image` || `getEnemyArt({ name, archetype, isBoss }).src` (Imagine jpg).
2. Grep `src/**/*.{ts,tsx}`: **zero** `.svg` asset path references.
3. `SkillCard.tsx`: state `bgFailed`; onError → emoji from `getSkillArt` / skill.icon; CSS `.skill-card__bg--emoji`.
4. Gates: tsc / test 355 / build OK.

**Not in scope:** secret discovery, clan traits, synthesis modal, RewardModal.

### Reviews (lead self-score)

| Lente | Score | Note |
|-------|-------|------|
| ARQUITECTURA | 92 | combat uses registry; no dual SVG paths |
| SISTEMA | 90 | display resolution only; no combat math change |
| PRESENTACION | 91 | stage + cards never blank/404 on missing art |
| BALANCE | 95 | no balance impact |

### Gate
**PASS** (all lenses ≥ 85)
