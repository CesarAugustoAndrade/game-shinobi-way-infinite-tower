# Log — T-011 · Presentación de la escena Event

section: presentation · run: 2026-06-29-A · **modo: interactivo** (el humano puntúa el visual)

Mockup ASCII + paleta aprobados por el humano ANTES de implementar (como pide el topic).
Paleta decidida por el LEAD: chrome navy `#0b0e14`/`#141924`; **acento ámbar/oro `#f2b134`**
(scroll/cadena/continuar); riesgo SAFE/LOW/MED/HIGH/EXTREME `#43c26a`/`#cdd63f`/`#eb8a3c`/`#e6483f`/`#a95cff`;
deltas sube/baja/curse/flag/neutro. Idioma: **inglés** (consistencia con el resto del juego).

---

## <a name="a1"></a>Attempt 1 — 2026-07-01T23:45:00Z — gate: FAIL (90/90/80/90) [interactivo]

### Maker (Changed)
Reescribió la escena al chasis pixel-arcade con la paleta aprobada: `Event.tsx`/`Event.css`, nuevo `EventResultModal.tsx`/`.css` + helper puro `eventOutcomeChanges.ts` (deltas reales), panel "What Changed", cinta/botón de continuidad de cadena, risk-meter, indicador gateado. Hiló `cameFromChain` (App + 2 hooks). Cadena ahora pasa por el panel de resultado (cambio de FLUJO en hooks, motor intacto). Borró `EventChoicePanel` (dead code). Gates verdes (tsc/lint:css/test 248/build).

### Corrección del LEAD (post-maker): idioma
El maker mezcló español (mis labels del mockup) + inglés. El humano decidió **inglés** (el juego es todo inglés). El LEAD normalizó 11 cadenas (`Resultado`→`Result`, `Qué cambió`→`What Changed`, `Elige tu camino`→`Choose Your Path`, `Continuar la historia`/`Seguir explorando`→`Continue the Story`/`Keep Exploring`, `Cadena`/`La cadena sigue`→`Chain`/`The chain continues`, `Requiere`/`Cuesta`→`Requires`/`Costs`, la nota de sendas). tsc verde.

### Scoring (interactivo) + feedback
El humano NO pudo verlo por screenshot (las salas de evento son RNG-gated; navegación automatizada impracticable). Juzgó el diseño implementado y pidió **un cambio**: *"quita lo de que se abra la elección, mejor usa un tooltip que combine con el diseño"* → el `OutcomePreview` inline que EXPANDÍA la tarjeta al seleccionar debía irse y volverse tooltip. → **iterar**.

---

## <a name="a2"></a>Attempt 2 — 2026-07-02T00:08:35Z — gate: PASS (90/90/88/90) [interactivo]

### Maker (Changed) — solo `Event.tsx` + `Event.css` (motor y modal intactos)
- **Quitó la expansión inline:** la tarjeta ya no crece al seleccionar (fuera el `<OutcomePreview>` inline y el confirm que abría el layout).
- **Tooltip pixel-arcade:** affordance `ⓘ Possible Outcomes`; popover blocky con tokens `--ev-*` (ámbar, sombra dura `5px 5px 0` sin blur, notch), abre hacia arriba sin desbordar ni hacer shift. Reusó el render de `OutcomePreview` como contenido. **No** usó el `Tooltip` compartido (portalea a body → perdería el scope `--ev-*` y trae sombra con blur) — decisión correcta.
- **Confirmar sin expandir:** click selecciona → click en la seleccionada confirma; chip compacto absoluto `Confirm ▸`. Teclado 1-4/Enter/Esc conservado. Limpió un residuo `'No disponible'`→`'Unavailable'`.
- Gates verdes: tsc 0 · lint:css 0 · test 248 · build OK.

### Scoring (interactivo)
- **PRESENTACION ≥85 (PASS)** — el humano aprobó el tooltip en lugar de la expansión inline.
- **ARQUITECTURA 90 / SISTEMA 90 / BALANCE 90** — cambio de presentación aditivo (solo Event.tsx/.css en el intento 2; el hilo `cameFromChain` de App+hooks es superficie de presentación, motor intacto); gates verdes.

### Decisión LEAD
**PASS (90/90/88/90). T-011 marcado `passed`.** Se commitea a `develop`. Nota de gusto abierta (no bloqueante): el tooltip dispara desde el affordance `ⓘ`, no al hover de toda la tarjeta (cambio de 1 línea si se quiere).