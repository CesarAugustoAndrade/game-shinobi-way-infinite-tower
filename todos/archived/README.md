# Archived

Tareas **completadas**. No editar salvo correcciones de metadatos.

Cada archivo enlaza al log del loop (`loop/logs/T-NNN.md`) con el detalle de intentos y reviews.

Para reabrir una tarea: mover a `../active/`, poner `status: todo` y actualizar `../README.md`.
