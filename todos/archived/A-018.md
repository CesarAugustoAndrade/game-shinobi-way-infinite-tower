# A-018 · Event RNG seedable + rollOutcome usa utils/rng

| Campo | Valor |
|-------|--------|
| **status** | `done` |
| **priority** | **P2** |
| **section** | events |
| **source** | docs/analisis-estado-actual-multiagente.md §11 |
| **created** | 2026-07-18 |
| **completed** | 2026-07-18 |

## Objetivo

rollOutcome usa Math.random; el resto del juego tiene rng seedable. Migrar para reproducibilidad en tests/sim y consistencia de runs.

## Criterios de hecho

- [x] rollOutcome usa rng del proyecto
- [x] Test con seed fija reproduce outcome
- [x] Sin Math.random en EventSystem core

## Cambios

- `rollOutcome(choice, player, rng?)` uses `random(rng)` from `utils/rng`
- Injectable `RandomGenerator` for deterministic tests
- Tests: fixed-seed sequence reproducibility + global seeded rng

## Entry points

- `src/game/systems/EventSystem.ts`
- `src/game/utils/rng.ts`
- `src/game/systems/__tests__/EventSystem.test.ts`
