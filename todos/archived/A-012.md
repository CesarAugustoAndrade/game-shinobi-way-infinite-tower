# A-012 · Limpieza feature flags muertos + defaults debug

| Campo | Valor |
|-------|--------|
| **status** | `done` |
| **priority** | **P2** |
| **section** | architecture |
| **source** | docs/analisis-estado-actual-multiagente.md §20 |
| **created** | 2026-07-18 |
| **completed** | 2026-07-18 |

## Objetivo

Purgar o cablear: DEBUG_OVERLAY, DEBUG_BOOSTED_STATS, ENABLE_COMBAT_ANIMATIONS, EXPERIMENTAL_COMBAT_UI, EXPERIMENTAL_LOOT_PREVIEW, DEV_MODE, STARTING_HP_PERCENT, MAX_ACTIVITIES_PER_ROOM. Helpers isFeatureEnabled/getProperty sin callers. DEBUG_STATE_TRANSITIONS default true → false en mainline. Renombrar vocabulario auto-combat vs auto-pass.

## Criterios de hecho

- [x] Sin flags definidos sin uso (o documentados como WIP con issue)
- [x] DEBUG_STATE_TRANSITIONS false por defecto
- [x] Helpers usados o eliminados

## Qué se hizo

- Purgados flags/props muertos: `DEBUG_OVERLAY`, `DEBUG_BOOSTED_STATS`, `ENABLE_COMBAT_ANIMATIONS`, `EXPERIMENTAL_*`, `DEV_MODE`, `STARTING_HP_PERCENT`, `MAX_ACTIVITIES_PER_ROOM`.
- `DEBUG_STATE_TRANSITIONS` default `false`.
- Helpers `isFeatureEnabled` / `getProperty` documentados como API preferida (siguen exportados).
- Vocabulario: Auto en combate = **auto-pass** (end turn delay); `ENABLE_MANUAL_COMBAT=false` = full auto-resolve batch.

## Entry points

- `src/config/featureFlags.ts`
- `src/game/config.ts`
- `src/game/utils/explorationDebug.ts`
- `src/hooks/useCombat.ts`
